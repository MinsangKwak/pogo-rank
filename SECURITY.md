# 보안 정책

[프로젝트 소개](README.md) · [기여 안내](CONTRIBUTING.md) · [저작물 고지](NOTICE.md)

**취약점은 비공개로 제보해 주세요.** 접수 후 3일 이내 회신하고, 90일 이내 수정·공개를 목표로 합니다. 일반적인 기능 버그는 공개 이슈로 제보할 수 있습니다.

## 제보 창구

- 서비스 푸터의 문의 이메일을 이용합니다. 해당 주소는 빌드 설정 `CONTACT_EMAIL`로 관리합니다.
- GitHub Security 탭의 **Report a vulnerability** 기능을 사용할 수 있으면 비공개 보고로 제출해도 됩니다.
- 공개 이슈에 인증 정보, 사용자 데이터, 재현 가능한 공격 내용을 게시하지 마세요.

제보에는 영향받는 주소·기능, 재현 절차, 예상 영향과 필요한 최소 증거를 포함해 주세요. 실제 사용자 데이터나 비밀 값은 첨부하지 않습니다.

## 대상 범위

| 영역 | 제보 예시 |
| --- | --- |
| `server/` 인증·인가 | OAuth 검증 우회, 토큰 재사용 문제, 관리자 권한 우회 |
| 사용자 데이터 API | 다른 사용자의 즐겨찾기 접근·변경, 계정 삭제 실패, 트레이너 코드 권한 문제 |
| `web/` | 외부 데이터에 의한 XSS, 서비스워커 캐시 오염, 민감 정보 노출 |
| `backend/` · `.github/workflows/` | 외부 자료 처리 중 코드 실행, 배포 자격 증명 노출, 워크플로 권한 남용 |
| 이전 Firebase 구성 | 아직 사용 중인 복구 환경의 `firestore.rules` 접근 제어 문제 |

OAuth 클라이언트 ID, GA 측정 ID, 이전 Firebase API 키가 공개되어 있다는 사실만으로 취약점에 해당하지는 않습니다. 반면 **OAuth 클라이언트 시크릿, JWT 서명 키, DB 접속 정보, 서비스 계정 키**는 비밀입니다. 실제 접근 제어 우회나 정보 노출이 있으면 공개 식별자와 관련된 경우에도 제보 대상입니다.

현재 인증·권한의 기준은 [서버 인증 설계](server/AUTH.md)입니다. 이전 Firebase 규칙이 현재 서비스 전체의 접근 제어를 담당하지는 않습니다.

## 처리 흐름

1. 3일 이내 접수 확인을 회신합니다.
2. 재현 여부와 영향 범위를 판단하고, 긴급한 문제는 우선 완화합니다.
3. 수정 릴리스 후 [변경 이력](CHANGELOG.md)에 기록합니다. 제보자 이름 공개 여부는 제보자가 정합니다.
4. 접수 후 90일이 지나면 제보자가 공개할 수 있습니다.

## 지원 버전

운영용 `deploy` 브랜치의 최신 서비스 버전을 지원합니다. 이전 릴리스는 별도 유지보수하지 않습니다.
