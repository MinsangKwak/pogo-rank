# 보안 정책 (SECURITY.md)

**취약점을 발견하면 공개 이슈 대신 이메일로 먼저 알려 주세요.** 접수 후 3일 안에 회신하고, 90일 안에 수정·공개를 목표로 합니다. (2026-09-07 v2.18.0 신설)

## 제보 창구

- 이메일: 사이트 푸터의 문의 주소 (빌드 설정 `CONTACT_EMAIL`)
- 이메일이 어려우면 GitHub 의 **Private vulnerability reporting** (Security 탭 → Report a vulnerability)
- 공개 이슈는 취약점이 아닌 일반 버그에만 써 주세요

## 범위

| 대상 | 포함 |
|---|---|
| `firestore.rules` | 다른 사용자의 즐겨찾기·내 포켓몬·가입 요청을 읽거나 쓸 수 있는 경우, 승인 없이 저장이 되는 경우 |
| 로그인 흐름 (`frontend/scripts/components/auth.js`) | 승인 우회, 관리자 판정 우회, 계정 삭제 오작동 |
| 빌드 파이프라인 (`backend/` · `.github/workflows/`) | 외부 데이터로 인한 코드 실행, 워크플로 권한 남용 |
| 배포 결과 (`dist/`) | XSS (외부 데이터가 화면에 그대로 들어가는 경우), 서비스워커 캐시 오염 |

**범위 밖**: Firebase apiKey·GA 측정 ID·관리자 uid 는 `dist/index.html` 에 그대로 들어가는 **공개 식별자**이며 비밀이 아닙니다. 접근 제어는 전부 `firestore.rules` 가 담당합니다 (docs/DEVELOPMENT.md 7장).

## 처리 흐름

1. 접수 확인 회신 (3일 이내)
2. 재현·영향 범위 판단 → 심각하면 규칙 게시나 배포로 즉시 차단
3. 수정 릴리스 후 CHANGELOG 에 기록 (제보자 표기 여부는 제보자가 정함)
4. 90일이 지나면 제보자가 공개해도 됩니다

## 지원 버전

`deploy` 브랜치(실서비스)의 최신 버전만 지원합니다. 이전 버전은 서비스워커가 자동 갱신하므로 별도 지원이 없습니다.
