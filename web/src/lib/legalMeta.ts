// ─────────────────────────────────────────────────────────────────────────────
// lib/legalMeta.ts — 약관·방침의 **날짜만** (본문은 screens/Legal.tsx)
//
// **왜 갈랐나.** lib/terms.ts 가 날짜 한 줄을 쓰려고 Legal 화면을 static import 했다.
// 그 한 줄 때문에 약관·개인정보 본문 200줄이 **첫 묶음에 딸려 들어왔고**, 화면을 lazy 로
// 나눠 둔 것이 무효가 됐다(빌드가 "dynamic import will not move module" 로 경고하던 것).
// 날짜는 본문이 아니다 — 여기 따로 두면 본문은 그 화면을 열 때만 온다.
//
// 약관을 개정해 날짜를 올리면 다음 로그인 때 동의를 다시 묻는다.
//
// 2026-09-28 (v4.7.1) 검색어 기록이 늘어 개인정보처리방침을 개정했다 — **수집 항목이 늘어나는
// 변경**이라 방침 10번의 약속대로 다시 동의를 받는다. 날짜를 오늘이 아니라 7일 뒤로 잡은 것도
// 같은 약속이다("시행 7일 전에 패치노트로 알린다"). 그날까지는 저장소 변수 COLLECT_URL 을
// 비워 두어 한 건도 쌓이지 않는다 (docs/OPERATIONS.md §16).
//
// **2026-09-23 에 시행일을 앞당겼다** (v4.9.9). 주인이 수집을 바로 켜기로 했고(CLAUDE.md §3),
// 새 화면이 dev 에 뜨는 날부터 쌓인다. 적힌 날짜와 실제가 어긋나는 것이 제일 나쁘다 —
// 값이 바뀌므로 9/28 판에 동의한 사람에게도 한 번 더 묻는다 (lib/terms.ts 는 글자까지 견준다)
//
// **같은 날 한 번 더 올렸다** (v5.0.0 운영 전환). 계정 저장소가 Firebase(서울)에서 moncamp 서버와
// Neon(싱가포르)으로 바뀌어 **계정 정보가 국외로 이전된다** — 동의를 다시 받는다.
// 날짜가 같아 글자로 가르려고 판 꼬리(-v5)를 붙였다. lib/terms.ts 는 글자까지 견주므로 이것으로 다시 묻는다
// ─────────────────────────────────────────────────────────────────────────────
export const PRIVACY_VER = '2026-09-23';
export const TERMS_VER = '2026-09-23-v5';
