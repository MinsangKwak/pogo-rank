// ─────────────────────────────────────────────────────────────────────────────
// lib/base.ts — 자산이 놓인 자리 하나 (v5 Phase 6)
//
// Vite 는 `import.meta.env.BASE_URL` 로 이 값을 줬다. Next 에는 그 이름이 없고,
// 저장소 페이지처럼 하위 경로에 얹지도 않으므로 뿌리 하나다.
//
// **상수로 두는 이유.** 여섯 자리에 흩어져 있던 것을 한 곳으로 모은다 — 배포 자리가
// 또 바뀌면 고칠 곳이 하나여야 한다 (CLAUDE.md §5 가 판 번호에 한 것과 같은 생각).
// ─────────────────────────────────────────────────────────────────────────────
export const BASE = '/';
