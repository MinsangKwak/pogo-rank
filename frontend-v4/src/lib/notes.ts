// ─────────────────────────────────────────────────────────────────────────────
// lib/notes.ts — 화면마다의 '기준 안내'
//
// **이 글은 본문이 아니라 ☰ 메뉴 안에 산다** (v3 #note-acc).
// 처음에 화면 꼬리에 `<p class="detail__foot">` 로 붙였더니, v3 에는 없는 문단이
// 목록 끝마다 하나씩 더 생겨 화면이 길어졌다. v3 는 $note 한 곳에 갈아 끼운다.
//
// 라우트 id 로만 고른다 — 글이 화면을 설명하지, 고른 칩을 설명하지 않아서다.
// ─────────────────────────────────────────────────────────────────────────────

const HOME_NOTE = '뭘 키울지 여기서 정해요. 도감에서 포켓몬을 알아보고, 랭킹에서 추천 개체를 고른 뒤, 육성 플래너에 내 개체를 기록하면 돼요.';

export const ROUTE_NOTE: Record<string, string> = {
  dmax: '티어표 행을 누르면 선정 근거가 펼쳐져요. 티어표는 pogomate와 같은 기준: 공격 종족값 × 맥스무브 위력(거다이 450 · 다이 350) × 자속 1.2, 내구 미반영, 다이맥스·거다이맥스는 별도 항목이며 %는 그 목록 1위 대비예요. 속성 칩은 그 타입 맥스무브를 쓰는 개체를 모아요(포켓몬 자체 타입이 아님). 출시된 다이맥스·거다이맥스만 포함. 포켓몬을 누르면 상세 정보가 열려요.',
  pve: '구하기 쉬운 일반 개체만 모은 레이드 티어표 (자체 계산). 속성 탭은 그 속성 포켓몬만 표시. 점수는 같은 속성 최강 어태커(전설·메가 포함) 대비 %, 티어는 목록 안 상대 등급. 포켓몬을 누르면 상세 정보가 열려요.',
  pvp: 'PvPoke 시뮬레이션 점수(100점 만점). 속성 필터 안의 순위는 그 속성 안에서의 순위라, 전체 순위를 옆에 같이 적어요.',
  'pvp-deck': '실험 기능. 추천 덱 3종은 상대 입력 없이 리그 메타 기준으로 뽑아요 — 정석 코어(점수 + 약점 상호 보완 그리디), 안티 메타(상위 10마리 상대 평균 상성순), 타입 분산(방어 타입 안 겹치게). 커스텀 덱 짜기는 PvPoke 리그 순위 × 타입 상성(공격 최대 배율 ÷ 피격 최대 배율)의 근사 추천 — 실드·기술 사이클·CP 최적화는 반영하지 않아요. GO배틀리그 규칙상 같은 종은 파티에 1마리만(섀도우·일반도 같은 종)이라 모든 추천이 종 단위로 중복을 제거해요. 슬롯 3칸을 다 채우면 상대 덱 분석과 구성 가이드가 나와요.',
  'pve-solo': '프로토타입 가정: 자체 계산 PvE 수치(개체값 15/15/15) 기반에 선택 보스의 실제 방어·공격 종족값을 반영해 보정. 운용은 실측 제공자식 — 최정예 1~2마리를 기절 직전 이탈 → 부활(5~6초) → 재진입으로 돌려쓰는 방식 기준. 풀강50 토글은 딜 +6.3%·TDO +20%, 버프는 메가부스트 +30% / 풀버프(메가+날씨+친구) +60%. 실측 보정: 생존 3배 · DPS +20%. 기절 → 부활약 → 재진입 운용을 반영해 정예 1~6마리 중 가장 빨리 깎는 구성을 골라요 (교체 1초 · 전멸 후 재진입 13초). 난이도는 보스별로 자동 판정(메가·원시 → 메가, 전설·환상·울트라비스트 → 4성, 최종 진화 → 3성, 그 외 1성)이며 선택된 보스의 난이도 배지를 탭하면 수동 변경돼요. 레이드 표시 CP는 개체 종족값 기반 계산값(공식 검증: 뮤츠 5성 54,148), 전투 체력은 게임 구조상 티어 고정 — 1성 600 · 3성 3,600 · 4성 9,000 · 5성/메가 15,000, 제한 1·3성 180초 / 그 외 300초. 복합 타입 보스의 두 번째 타입은 그 포켓몬이 실제로 쓰는 기술 타입으로 보정하며, 스피드기와 차지기가 한 사이클에서 넣는 피해량으로 가중해요. 포켓몬을 누르면 상세 정보가 열려요.',
};

export function routeNote(id: string): string {
  return ROUTE_NOTE[id] ?? HOME_NOTE;
}

/** 게임 업데이트 갈래 — v3 components/updates.js UPDATE_CATS */
export const UPDATE_CATS: Record<string, string> = {
  gym: '체육관',
  raid: '레이드·맥스',
  pvp: 'PvP',
  catch: '포획·육성',
  reward: '보상·편의',
  bugfix: '오류 수정',
};
