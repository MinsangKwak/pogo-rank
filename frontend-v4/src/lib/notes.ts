// ─────────────────────────────────────────────────────────────────────────────
// lib/notes.ts — 화면마다의 '기준 안내'
//
// **이 글은 본문이 아니라 ☰ 메뉴 안에 산다** (v3 #note-acc).
// 처음에 화면 꼬리에 `<p class="detail__foot">` 로 붙였더니, v3 에는 없는 문단이
// 목록 끝마다 하나씩 더 생겨 화면이 길어졌다. v3 는 $note 한 곳에 갈아 끼운다.
//
// 라우트 id 로만 고른다 — 글이 화면을 설명하지, 고른 칩을 설명하지 않아서다.
// ─────────────────────────────────────────────────────────────────────────────

const HOME_NOTE = '도감에서 포켓몬 정보를 확인하고 순위표에서 배틀 후보를 비교해 보세요. 즐겨찾기에 담으면 내 포켓몬 화면에서 관련 일정을 확인할 수 있어요.';

export const ROUTE_NOTE: Record<string, string> = {
  dmax: '등급은 전체 포켓몬의 최고 점수를 기준으로 정해요. 최고 점수 대비 90% 이상은 S, 80% 이상은 A, 70% 이상은 B, 그 미만은 C예요. 타입 탭을 바꿔도 등급은 같고, 순위만 해당 탭 안에서 다시 표시해요. 점수 = 공격 × 맥스무브 위력(거다이 450 · 다이 350) × 자속 1.2 × 내구 보정(방어 × 체력 ÷ 1000의 네제곱근). 맥스 페이즈를 반복해서 사용할 수 있는 생존력을 고려해 내구를 일부 반영해요. 속성 칩은 해당 타입의 맥스무브를 사용하는 포켓몬을 모아요. 포켓몬 자체 타입과는 다를 수 있어요. 포켓몬을 선택하면 상세 정보를 확인할 수 있어요.',
  pve: '구하기 쉬운 일반 개체만 모은 레이드 티어표 (자체 계산). 속성 탭은 그 속성 포켓몬만 표시. 점수는 같은 속성 최강 어태커(전설·메가 포함) 대비 %, 티어는 목록 안 상대 등급. 포켓몬을 누르면 상세 정보가 열려요.',
  pvp: 'PvPoke 시뮬레이션 점수(100점 만점). 속성 필터 안의 순위는 그 속성 안에서의 순위라, 전체 순위를 옆에 같이 적어요.',
  'pvp-deck': '실험 기능. 추천 덱 3종은 상대 입력 없이 리그 메타 기준으로 뽑아요 — 정석 코어(점수와 약점 보완 기준), 안티 메타(상위 10마리 상대 평균 상성순), 타입 분산(방어 타입 중복 최소화). 맞춤 덱은 PvPoke 리그 순위 × 타입 상성(공격 최대 배율 ÷ 피격 최대 배율)의 근사 추천 — 실드·기술 사이클·CP 최적화는 반영하지 않아요. GO배틀리그 규칙상 같은 종은 파티에 1마리만(섀도우·일반도 같은 종)이라 모든 추천이 종 단위로 중복을 제거해요. 슬롯 3칸을 다 채우면 상대 덱 분석과 구성 가이드가 나와요.',
  'pve-solo': '실험 기능의 계산 조건: 자체 계산 PvE 수치(개체값 15/15/15) 기반에 선택 보스의 실제 방어·공격 종족값을 반영해 보정. 출전 방식은 실측 사례를 참고하며, 최정예 1~2마리를 기절 직전 이탈 → 부활(5~6초) → 재진입으로 돌려쓰는 방식 기준. 풀강50 토글은 딜 +6.3%·TDO +20%, 버프는 메가부스트 +30% / 풀버프(메가+날씨+친구) +60%. 실측 보정: 생존 3배 · DPS +20%. 기절 → 부활약 → 재진입 운용을 반영해 정예 1~6마리 중 예상 클리어 시간이 가장 짧은 구성을 골라요 (교체 1초 · 전멸 후 재진입 13초). 난이도는 보스별로 자동 판정(메가·원시 → 메가, 전설·환상·울트라비스트 → 4성, 최종 진화 → 3성, 그 외 1성)이며 선택된 보스의 난이도 배지를 탭하면 수동 변경돼요. 레이드 표시 CP는 개체 종족값 기반 계산값(공식 검증: 뮤츠 5성 54,148), 전투 체력은 게임 구조상 티어 고정 — 1성 600 · 3성 3,600 · 4성 9,000 · 5성/메가 15,000, 제한 1·3성 180초 / 그 외 300초. 복합 타입 보스의 두 번째 타입은 그 포켓몬이 실제로 쓰는 기술 타입으로 보정하며, 스피드기와 차지기가 한 사이클에서 넣는 피해량으로 가중해요. 포켓몬을 누르면 상세 정보가 열려요.',
};

export function routeNote(id: string): string {
  return ROUTE_NOTE[id] ?? HOME_NOTE;
}

/** 게임 업데이트 갈래 — v3 components/updates.js UPDATE_CATS */
export const UPDATE_CATS: Record<string, string> = {
  gym: '체육관',
  raid: '레이드·맥스',
  pvp: 'PvP',
  catch: '포획·육성',
  reward: '보상·편의',
  bugfix: '오류 수정',
};
