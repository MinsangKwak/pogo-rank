# 변경 이력

**버전을 눌러 펼쳐 보세요.** 114개 판이 쌓여 한눈에 훑기 어려워, 각 버전을 접어 두었습니다.

각 줄은 `버전 — 날짜 · 그 판에서 한 일` 순서입니다. 최신이 위로 옵니다.
형식은 [Keep a Changelog](https://keepachangelog.com/ko/1.1.0/)를 따릅니다.

**버전 규칙**은 `vMAJOR.MINOR.PATCH` — 큰 기능은 MINOR, 상세 기능·버그 수정은 PATCH 를 올립니다.
**항목 종류**는 넷입니다 — `추가` 새 기능 · `변경` 기존 동작 변경 · `수정` 버그 수정 · `데이터` 수동 데이터 갱신.

> 사용자가 읽는 패치노트는 서비스 안 [🎉 패치노트](https://minsangkwak.github.io/pogo-rank/#/release) 화면에 있습니다(영문판 포함).
> 이 파일은 **왜 그렇게 고쳤는지**까지 남기는 개발 기록이라 더 깁니다.


<details open>
<summary><b>v3.14.0</b> — 2026-09-12 · <code>변경</code> 개발 순환 시간 단축 — 회귀 162→117초 · 빌드 단계화 · 죽은 코드 정리 · dev_up/ship_dev</summary>

### 왜

클라우드 세션에서 한 판을 올리는 데 30분이 넘게 걸렸다. 회귀는 3분이었고 나머지는 **사이사이** 였다 — 컨테이너가 새로 뜨면 서버를 다시 띄우고, dev 머지 명령 여덟 개를 하나씩 치고, 미리보기에 버전이 뜨기를 손으로 폴링하고, 명령마다 권한을 물었다. 화면은 한 픽셀도 바뀌지 않는 판이라 패치노트(`release.js`)는 올리지 않는다.

### 변경 — 회귀 162초 → 117초

**없는 버튼을 기다리던 1.5초.** 스위트마다 화면을 옮길 때 `#consent .consent__deny` 를 `click({ timeout: 1500 }).catch()` 로 누르고 있었다. 배너는 첫 화면에만 뜨므로 두 번째 이동부터는 **없는 버튼을 1.5초씩 기다리다 포기하는 줄** 이었다. 화면 이동 30번이면 45초 — `shell.js` 100초의 절반이 이것이었다. `newContext()` 가 `pogo_consent=denied` 를 미리 넣어 배너 자체가 뜨지 않게 하고 그 줄 32개를 지웠다. 배너를 검사하는 `legal.js` · `bot-filter.js` 는 `{ banner: true }`.

| 스위트 | 전 | 후 |
| --- | --- | --- |
| shell | 101s | 58s |
| nav | 60s | 25s |
| router | 61s | 28s |
| fingerprint | 43s | 19s |
| i18n | 28s | 12s |
| 전체 (일꾼 4) | 162s | 117s |

**공통 조각 `tests/e2e/_lib.js`.** 브라우저 경로 · `ok()` 집계 · `newContext()`(가입 권유 팝업 '본 적 있음' · 배너 거부 · 바깥 요청 차단) · `go()` · `finish()` · `suite()`. 스위트 22개가 같은 머리말을 각자 들고 있었다(4,295줄 → 3,608줄). 이름이 `_` 로 시작하는 파일은 `test.sh` 가 건너뛴다.

**`wait` 가 서버까지 기다렸다.** `test.sh` 가 서버를 직접 띄운 경우 마지막 `wait` 가 그 서버 프로세스도 기다려 스위트가 다 끝나고도 돌아오지 않았다. 일꾼 pid 만 기다린다.

**`-j 8` 은 답이 아니다.** 4코어에서 부하 20을 찍으며 세 스위트가 시간 초과로 실패했다. 대기 시간은 애니메이션·디바운스에 맞춘 값이라 CPU 가 밀리면 어긋난다.

### 변경 — 빌드를 단계별 함수로

`backend/build.py` 는 한 덩어리 502줄 스크립트였다. `read_config → build_pvp_tables → copy_sprites → apply_rank_delta → render_data_js → render_index_html → write_site_files` 로 나누고 `main()` 이 그 순서대로 부른다. 산출물은 바이트 단위로 같다(버전 문자열 제외, `index.html` · `data.js` 대조).

- **스프라이트 복사 생략** — 크기·수정 시각이 같은 파일은 건너뛴다. 화면만 고친 빌드에서 2,344장을 매번 다시 쓰고 있었다. 0.5초 → 0.3초.
- **(수정) `FORM_LABELS` 가 빌드마다 달랐다** — `sorted(set, key=len)` 은 같은 길이 안의 순서를 집합 순서에 맡기는데, 그건 파이썬 해시 무작위화로 실행마다 다르다. `data.js` 가 바뀐 것 없이 바뀌었다. `(-len, label)` 로 재현 가능하게.

### 변경 — 죽은 코드

| 조각 | 왜 죽어 있었나 | 조치 |
| --- | --- | --- |
| `isFav` · `toggleFav` · `favBtn` · `refreshFavUi` (auth.js) | v3.4.0 에 ★ 즐겨찾기를 걷어낸 뒤 부르는 곳 없음 | 삭제. `AUTH.favs` 는 Firestore 필드라 그대로 읽는다 |
| `setRole` (auth.js) | 역할 보정 UI 가 없다 | 삭제. `AUTH.roles` 는 그대로 |
| `savePlanLast` · `planLastMode` · `PLAN_LAST_KEY` (planner/shell.js) | 저장만 하고 읽는 곳 없음 (첫 진입은 늘 홈) | 삭제. 브라우저에 남은 `pogo_plan_last` 는 아무 일도 하지 않는다 |
| `.fav*` CSS (list.css · modal.css · base.css) | 그 버튼을 만드는 함수가 위에서 죽었다 | 삭제 |

정의 외 참조가 0인 최상위 함수를 스크립트로 전수 조사한 결과다(주석 속 이름 제외). 회귀 22묶음 통과. 지문(fingerprint)은 이 환경에서 같은 빌드끼리도 그림 로딩 시점에 따라 흔들려 증거로 쓰지 않았다.

### 추가 — 세션 시작 · dev 머지를 한 명령으로

- `scripts/dev_up.sh` — 빌드 + `localhost:5503` 서버 (이미 떠 있으면 그대로). SessionStart 훅에 두면 세션이 열릴 때 이미 준비돼 있다.
- `scripts/ship_dev.sh` — 현재 브랜치 푸시 · dev `--no-ff` 머지 · 푸시 · 원래 브랜치 복귀 · 미리보기에 그 버전이 뜰 때까지 대기 · `verify_deploy.sh`. main → deploy 는 사람이 정하는 단계라 넣지 않았다.
- `.claude/settings.json` 권고안은 docs/DEVELOPMENT.md 에 — 에이전트가 제 권한을 넓히는 파일이라 사람이 넣는다.

</details>

<details open>
<summary><b>v3.13.0</b> — 2026-09-12 · <code>변경</code> UX 흐름 점검 — 죽은 조각 · 낡은 문구 · 겹치는 문을 걷어냈다</summary>

### 무엇을 찾았나

열아홉 화면을 처음 온 사람의 순서(홈 → 메뉴 → 티어표 → 도감 → 플래너 → 계정)로 따라가며 세 가지를 찾았다 — **지금 없는 것을 말하는 문구**, **같은 곳으로 두 번 가는 문**, **아무것도 그리지 않는 조각**. 기능을 더하지 않는 판이라 `변경` 하나로 적는다.

### 변경 — 죽은 조각을 지웠다

| 조각 | 상태 | 조치 |
| --- | --- | --- |
| `.tagline-row` · `#mode-toggle` | v2.15.0 모드 배지. v2.61.0 부터 부모에서 떼어져 화면엔 없는데 리스너와 `updateModeBadge()` 본문은 매 라우팅마다 돌았다 | 마크업 · 리스너 · CSS 삭제. `updateModeBadge()` 는 빈 함수로 남긴다 — 플래너 셸이 부르는 자리를 바꾸지 않으려고 |
| `#tabs` · `renderTabs()` · `$tabs` | v2.61.0 랭킹 탭 줄, v2.66.0 플래너 탭 줄까지 걷어낸 뒤 늘 비우고 숨기기만 했다 | 마크업 · 함수 · `.tabs*` CSS 삭제. `tabs.css` 의 `.controls__row*` 는 살아 있어 남긴다 |

`#note` 는 지우지 않는다 — 비어 보여도 드로어 '기준 안내' 아코디언이 그 안을 채운다. 드로어의 동의 줄도 남긴다 — legal.js 가 `#menu-consent` 를 눌러 설정을 연다. `favBtn` 계열은 죽었지만 화면에 안 보이는 코드라 이번 범위 밖이다.

### 변경 — 없는 것을 말하던 문구를 고쳤다

★ 즐겨찾기는 v2.51.0 에 내 포켓몬으로 합쳐졌는데 네 곳이 아직 "즐겨찾기가 계정에 저장돼요" 라고 했다 — 로그인 유도 3단계, 계정 삭제 목록, 익명 계정 카드, 승인 대기 안내. 도감 상단 `loginHint` 도 같은 문장이라 빈 문자열로 둔다. 홈 각주의 '상성' 은 `#note` 가 한 번도 그린 적 없는 것이었다. 새 문장은 지금 있는 것(내 포켓몬 · 검색식 · 화면 설정)만 말하고, 영문 사전 키를 함께 옮겼다.

### 변경 — 플래너 홈의 겹치는 문을 줄였다

`#/planner/collection` 으로 가는 문이 한 화면에 다섯이었다 — 히어로 [개체 등록하기], 요약 카드 [내 포켓몬 바로가기 →], 다음 걸음 두 칸, 최근 목록 [전체 보기]. 요약 카드는 숫자를 보여 주는 자리라 그 문을 뺀다(5 → 4). 맨 아래 "다음에 붙을 것" 로드맵 줄도 뺐다 — 할 일 목록은 사용자가 볼 것이 아니고, 그 안의 '게임 검색식 생성기' 는 v2.58.0 에 이미 붙어 있었다.

### 회귀

22묶음 그대로 통과. 화면이 바뀌지 않아 새 검사는 없다.

</details>

<details open>
<summary><b>v3.12.0</b> — 2026-09-12 · <code>변경</code> 검색 팝업을 걷어냈다 — 검색은 도감에서 · 타입 칩을 도감으로 · 활용처 순위를 홈으로 외 1건</summary>

### 변경 — 검색 팝업을 걷어냈다. 검색은 도감에서 한다

v3.9.0 은 결과를 도감으로 넘겼고 v3.10.0 은 패널에서 결과 줄을 뺐지만, 패널 자체는 남아 있었다. 그러면 검색은 여전히 **팝업에서 시작해 다른 화면에서 끝나는** 두 단계다. 그 사이에 뜬 몇 줄(빈 상태의 활용처 순위)이 "결과" 처럼 보여 도감까지 가지 않는다.

입구는 셋, 도착지는 하나로 만든다.

| 누르는 곳 | 전 | 후 |
| --- | --- | --- |
| 헤더 🔍 | `#search-dialog` 열기 | `#/dex` + `#dex-search` 포커스 |
| 넓은 화면 검색 칸 | 같은 팝업 | 같은 곳 |
| `/` 키 | 같은 팝업 | 같은 곳 |

`<section class="search">` 마크업, `#search-dialog` 조립, `toggleSearchPanel` · `closeSearchDialog` · `renderSearchResults` · `renderSearchTypeChips` · `searchSubmit` 을 전부 지웠다. 남긴 것은 `openSearch(query)` 하나다. 이미 도감에 있으면 다시 그리지 않고 입력칸만 잡는다 — 다시 그리면 치던 글자가 날아간다.

`aria` 도 따라 바뀐다: 🔍 는 팝업을 여는 버튼이 아니므로 `haspopup` · `expanded` · `controls` 를 뗐다.

### 변경 — 타입 칩을 도감으로

칩이 패널에 있었으니 패널과 함께 갈 곳이 없어졌다. 지우지 않고 옮긴다 — 무엇을 걸러 이 목록이 나왔는지는 **목록 옆에서** 읽혀야 한다.

`.dex__type-box` 는 기본으로 접혀 있다(열여덟 칸이라 펼쳐 두면 목록이 그만큼 밀린다). `?t=` 로 들어와 켜 둔 칩이 있으면 펼친 채로 연다. `[전체 도감 보기]` 는 검색어와 함께 칩도 푼다 — "전체" 라고 해 놓고 타입이 남아 있으면 거짓말이다.

### 변경 — 활용처 순위를 서비스 홈으로

패널의 빈 상태에 살고 있었다. `views/usage.js` 는 그대로 두고 부르는 곳만 바꿨다. "여러 순위표에서 두루 상위권" 은 어느 화면을 열지 정하기 전에 보는 값이라, 갈림길인 홈이 원래 자리에 가깝다.

### 변경 — 손가락이 닿는 넓이

전 화면 19개를 두 폭(390 · 1440)에서 훑어 32px 미만 과녁을 셌다.

| | 전 | 후 |
| --- | --- | --- |
| `[더보기]` · `[전체 보기]` · `[복사]` | 22~28px | **44px** |
| 칩류(`.uchip` · `.chips__item` · `.tool-btn`) | 28~30px | **36×36px** |
| 헤더 로고 (= 처음으로) | 22px | **44px** |
| 브레드크럼 🏠 | 14px | **32px** |

글자 크기는 그대로다 — 키우면 목록이 그만큼 아래로 밀린다. 문단 속 `<a>` 는 건드리지 않았다: 글줄 한가운데라 높이를 키우면 줄간이 벌어진다.

### 점검에서 나왔지만 고치지 않은 것

**육성 플래너 홈에 같은 곳으로 가는 버튼이 다섯이다** — `plan__hero-go` · `plan__summary-go` · `plan__step` 1 · `plan__step` 2 · `plan__more` 가 모두 `#/planner/collection` 으로 간다(그 밖에 최근 포켓몬 넉 줄은 내용이라 별개다). v2.18.0 이 "같은 화면으로 가는 버튼을 하나씩만" 으로 정리한 적이 있는데 그 뒤 다시 늘었다. 어느 것을 남길지는 이 화면의 설계 결정이라 보고만 한다.

</details>

<details>
<summary><b>v3.11.0</b> — 2026-09-12 · <code>추가·변경</code> 패치노트 영문판 · 화면 테마를 버튼과 설정 화면으로 가른다 · 가입 권유 팝업 외 2건</summary>

### 추가 — 패치노트 영문판

[EN] 으로 봐도 이 화면만 한국어였다. v2.29.0 이 그렇게 정한 이유는 "쌓인 기록을 번역하면 원문과 어긋난 채로 굳는다" 였는데, 그 걱정은 **문장 단위로 옮길 때** 생긴다.

날짜(묶음 키)로 통째 짝짓는다. 짝이 없는 묶음은 한국어가 그대로 나가고, 그런 묶음이 하나라도 있을 때만 "여기는 한국어" 안내가 뜬다. 지어낸 영어가 섞일 자리가 없다.

73개 묶음 313줄을 전부 옮겼다. 버전이 없이 날짜만 적힌 초기 묶음은 같은 날짜가 둘이라, 두 번째부터 `2026-09-08 (2)` 처럼 순번을 붙여 가른다(`releaseKey()`).

언어를 바꾸면 이 화면만 다시 그린다 — 사전 엔진(i18n.js)은 **그려진 글자**를 옮기는 방식이라 다른 배열로 갈아 끼우지는 못한다.

### 수정 — 패치노트의 `**굵게**` 가 별표로 찍혔다

`el('li', {}, item)` 이 문자열을 그대로 붙이고 있었다. 강조하려고 적어 둔 표시가 오히려 문장을 지저분하게 만들고 있던 셈이다. 문법이 하나뿐이라 파서도 한 줄이면 된다.

```js
String(text).split('**').map((part, i) => (i % 2 ? el('b', {}, part) : part))
```

### 변경 — 영문 사전의 구멍을 메웠다

영어로 켜고 열다섯 화면을 훑어 한글이 남은 텍스트 노드를 전부 긁었다. **291건에서 69건으로** 줄었고, 남은 69건은 전부 일정표 이벤트 제목과 기술 이름 — 일부러 한국어로 두는 데이터다.

가장 많이 샌 이유는 번역이 없어서가 아니라 **원문이 고쳐진 뒤 사전이 따라오지 못해서**였다. v2.60.0 에서 화면 말투를 "~합니다" → "~해요" 로 통일했는데, 사전 키는 옛 어미 그대로였다. 사전 키 560개를 원문과 대조해 그런 키 67개를 찾았다.

### 변경 — 화면 테마를 버튼과 설정 화면으로 갈랐다

상단 버튼이 세 상태(기기 설정 · 밝게 · 어둡게)를 돌고 있었다. 누를 때마다 무엇이 될지 예측이 안 됐다 — '기기 설정' 은 지금 화면이 밝은지 어두운지와 따로 놀아서, 같은 그림을 눌러도 어떤 날은 밝아지고 어떤 날은 어두워졌다.

| | 전 | 후 |
| --- | --- | --- |
| 상단 버튼 | 기기 설정 → 밝게 → 어둡게 → 기기 설정 | **밝게 ↔ 어둡게** (지금 보이는 것의 반대) |
| 기기 설정 따름 | 그 순환 안 | **설정 화면(`#/settings`)** |

"자주 뒤집는 것" 과 "한 번 정해 두는 것" 은 같은 자리에 있을 이유가 없다. 저장 값(`pogo_theme` 의 `system`/`light`/`dark`)은 그대로라 이미 고른 사람의 설정은 흔들리지 않는다.

설정 화면은 세 갈래를 라디오 줄로 놓고, 로그인 상태에 따라 "이 브라우저에만" 인지 "계정에 저장" 인지 밝힌다. 계정 저장은 v2.47.0 부터 `applyTheme()` 이 이미 하던 일이다 — 화면이 그 사실을 말하지 않고 있었을 뿐이다.

### 추가 — 가입 권유 팝업

**가입자를 늘리려고** 띄운다. 다른 목적이 없으므로 그렇게 적었다 — 안내인 척하지 않고, 로그인 전과 후를 나란히 놓은 표를 보여 준다.

잠긴 화면 목록은 라우터 표(`ROUTES.locked`)에서 그대로 읽는다. 화면이 늘거나 잠금이 풀리면 팝업도 저절로 따라간다.

비로그인 첫 방문 한 번만 뜬다. 매번 띄우면 광고가 되고, 광고가 되면 읽지 않는다. 동의 배너가 떠 있으면 그것이 먼저다 — 한 화면에 팝업 둘은 둘 다 안 읽힌다.

### 변경 — 이 문서를 접었다

112개 판이 쌓여 한눈에 훑을 수가 없었다. 각 버전을 `<details>` 로 접고, 요약 줄에 `버전 — 날짜 · 종류 · 그 판에서 한 일` 을 적는다. 되풀이되던 `변경 —` 접두사는 앞의 종류 표식 하나로 모았다.

</details>

<details>
<summary><b>v3.10.0</b> — 2026-09-12 · <code>변경</code> 도감 안 검색 칸이 돌아왔다 · 휴대폰 티어표도 두 장씩 · 카드 한 장을 85% 로 외 4건</summary>

### 변경 — 도감 안 검색 칸이 돌아왔다

v3.9.0 은 헤더 패널에 여덟 줄을 그리고 나머지를 도감으로 넘겼다. 그러면 같은 목록을 두 곳에서 그리는 셈이고, 정작 패널에 뜬 여덟 줄이 "결과" 처럼 보여 도감까지 가지 않는다.

패널은 조건을 받는 곳으로만 둔다. 결과는 도감 한 곳에서만 그린다.

그러면 v3.5.0 에 도감 안 검색 칸을 뺀 이유("헤더 검색과 생김새가 같은 입력칸 둘")가 사라진다. 헤더 패널이 더는 결과를 그리지 않으므로, 두 칸이 같은 일을 놓고 겹치지 않는다. 결과가 한 곳에 모였으니 **그 결과를 좁히는 칸**도 그 곁에 있어야 한다 — 한 글자 지우자고 헤더 팝업을 다시 열 수는 없다.

입력할 때마다 화면을 다시 그리지는 않는다.

```js
history.replaceState(history.state, '', `#/dex${query ? `?${query}` : ''}`);  // hashchange 가 안 뜬다
draw();                                                                       // 목록만 고쳐 그린다
```

`replaceState` 는 `hashchange` 를 쏘지 않으므로 `renderPage()` 가 돌지 않고, 따라서 입력칸이 다시 만들어지지 않는다 — 커서가 안 빠진다. 200ms 디바운스(한 글자마다 1,800줄을 훑지 않게), Enter 는 기다리지 않고 즉시.

주소는 계속 결과를 따라간다. 그대로 보내면 상대도 같은 화면을 연다.

### 변경 — 휴대폰 티어표도 두 장씩

`--list-cols` 가 700px 미만에서 1 이라 그리드를 켜도 한 줄에 한 장이었다. 한 화면에 두 마리도 안 들어와 티어표를 훑을 수가 없다. 도감·레이드 보스 그리드(`.dex__list.is-grid`)가 이미 쓰는 `1fr 1fr` 로 맞춘다.

### 변경 — 카드 한 장을 85% 로

| | 전 | 후 |
| --- | --- | --- |
| 카드 (1440px) | 282 × 420 | 268 × **362** |
| 그림 | 18rem | **14.2rem** |
| 위 여백 | 4.2rem | **3.6rem** |
| 이름 | 2.1rem | **1.8rem** |

한 장이 화면 높이의 절반을 먹어 스크롤 한 번에 한 줄밖에 못 봤다. 그림·여백·글자를 같은 비율로 줄여야 균형이 그대로 남는다.

점수 칸(`.row__stats`)은 세로로 쌓는다 — 좁은 화면의 한 칸(170px)에서 "66점" 과 "DPS 14.3 · TDO 149" 가 나란히 서면 앞 칸이 눌려 "66 / 점" 으로 쪼개졌다.

### 변경 — 동작 버튼은 좁은 화면에서도 가로 한 줄

v3.7.1 에 위아래로 쌓고 리그 넷을 2×2 로 만든 것을 되돌린다. 쌓아 놓으니 제목 오른쪽에 버튼 탑이 서서 화면 머리가 두 배로 높아졌다.

대신 좁은 화면에서 `.page-head` 를 한 칸으로 바꿔 버튼 줄이 **제목 아래 제 줄**을 쓰게 했다. 제목을 밀지 않으므로 옆으로 늘어놓아도 자리가 넉넉하다. 칸이 넷인 배틀 · PvP 의 리그도 390px 에 한 줄로 들어간다(글자 한 단 축소).

### 변경 — 리스트 아이콘 ☰ → ▤

오른쪽 위 ☰ 메뉴 버튼과 그림이 똑같았다. 나란히 놓이면 어느 쪽이 메뉴인지 헷갈린다. 줄마다 그림 자리가 있는 목록 아이콘으로 다시 그렸다(네모 점 + 글줄 석 줄).

### 추가 — 배틀 · PvP 에도 보기 전환

같은 티어표인데 여기만 줄로 되돌릴 방법이 없었다. 겸사겸사 v3.9.1 의 부작용도 여기서 사라진다: 카드 규칙이 `.is-grid` 를 요구하게 되면서, 클래스가 안 붙는 이 화면만 큰 카드(그림 153px)가 아니라 공용 작은 카드(84px)로 그려지고 있었다. 토글이 붙으면 `rowListLayout()` 이 늘 클래스를 달아 주므로 셋이 같은 카드가 된다.

저장 키는 `pogo_pvp_cols` — 화면마다 선택이 섞이지 않게 따로 둔다.

### 회귀

- `dex-search.js` 26 → 32: 패널에 결과 줄이 없는가 · 도감 안 검색 칸이 커서를 안 빼앗기고 목록·주소를 함께 고치는가
- `layout-toggle.js` 70 → 84: 휴대폰 그리드가 두 칸인가 · 동작 버튼이 한 줄에 서는가 · 리스트 아이콘이 ☰ 메뉴와 다른 그림인가

</details>

<details>
<summary><b>v3.9.1</b> — 2026-09-12 · <code>수정·회귀</code> 레이드 · PvE 와 D-MAX 의 보기 전환이 아무 일도 안 했다 · 클래스가 아니라 화면을 받는다</summary>

### 수정 — 레이드 · PvE 와 D-MAX 의 보기 전환이 아무 일도 안 했다

버튼은 멀쩡히 뒤집혔다. `data-view` 가 바뀌고, 목록에 `is-list` 가 붙었다 떨어졌다 했다. 그런데 화면은 처음부터 끝까지 카드였다.

CSS 특정도 싸움에서 지고 있었다.

| | 선택자 | 특정도 |
| --- | --- | --- |
| 카드로 그리는 쪽 (v2.53.0) | `body:is([data-route="dmax"], …) #content .row-list` | **(1,2,1)** |
| 줄로 되돌리는 쪽 (v3.3.0) | `.row-list.is-list` | (0,2,0) |

v3.3.0(레이드 · PvE)과 v3.5.0(D-MAX)에 넣은 토글이 **나간 적이 없는 셈**이다.

#### 되돌리는 대신 카드 쪽에 조건을 단다

되돌리기 규칙을 더 세게 적어서 이길 수도 있었다. 그러지 않은 이유는 그 방식이 이미 한 번 실패했기 때문이다 — 카드 규칙이 속성을 하나 더 줄 때마다 되돌릴 목록도 한 줄 늘려야 하고, 실제로 어긋나 있었다(v2.53.0 이 `row__name` 크기·`sprite` 그라데이션·`row__stats` 테두리를 더했지만 되돌리기 쪽은 v3.3.0 그대로였다).

카드 규칙 세 블록에 조건을 단다.

```css
/* list.css @1100 (공용) */      .row-list:not(.is-list) > .row { … }
/* pc-theme.css @1100 (티어표) */ .row-list:not(.is-list) > .row { … }
/* pc-theme.css (화면별 큰 카드) */ …#content .row-list.is-grid > .row { … }
```

되돌릴 속성을 셀 일이 없어졌다 — 카드 디자인이 또 바뀌어도 따라다닐 필요가 없다. v3.3.0 의 되돌리기 묶음은 삭제했다.

`:not(.is-list)` 와 `.is-grid` 를 섞어 쓴 이유: 배틀 · PvP 에는 아직 토글이 없어 클래스가 안 붙는다. 공용 블록은 `:not(.is-list)` 라야 PvP 가 지금까지처럼 카드로 남는다.

#### 휴대폰에서도 카드 보기가 켜진다

화면별 큰 카드 블록을 감싸고 있던 `@media (min-width: 1100px)` 를 뗐다. 카드냐 줄이냐는 이제 폭이 아니라 **사용자의 선택**이다.

전에는 좁은 화면에 카드 규칙 자체가 없어서, 버튼을 눌러도 바뀔 것이 없었다. 이제 `--list-cols` 가 1 이라 한 줄에 큰 카드 한 장 — 도감 그리드가 휴대폰에서 보이는 그 모양이다.

처음 여는 모습은 그대로다. `layoutInitial()` 이 `wideCards()` 를 보므로 휴대폰은 줄, PC 는 카드로 열린다. v2.53.0 이 막으려던 "휴대폰까지 2열 카드로 덮는" 일은 일어나지 않는다.

#### 클래스를 둘 다 명시한다

미디어 쿼리를 떼려면 CSS 가 "그리드"를 클래스로 알아볼 수 있어야 한다. 전에는 `is-list` 만 켜고 껐기 때문에 그리드가 **클래스 없는 상태**였고, 그것은 "기본" 과 구별되지 않았다.

```js
function rowListLayout(grid) {
  for (const node of document.querySelectorAll('#content .row-list')) {
    node.classList.toggle('is-grid', grid);
    node.classList.toggle('is-list', !grid);
  }
}
```

`components/ui.js` 한 곳에 두고 `applyPveLayout()` · `applyMaxLayout()` 이 부른다 — 두 함수는 같은 코드를 각자 들고 있었다. 두 전역 이름은 그대로 남겨 뒀다.

### 회귀 — 클래스가 아니라 화면을 받는다

이 버그는 `layout-toggle.js` 40개가 전부 통과하는 동안 살아 있었다. 검사가 `data-view` 와 `is-grid` 를 봤기 때문이다. 둘 다 맞게 바뀌고 있었다 — CSS 가 그걸 안 봤을 뿐이다.

클래스는 수단이지 결과가 아니다. 줄 하나의 **실제 높이**로 받는다.

- 도감 · 레이드 보스 · 알 부화 · D-MAX · 레이드 · PvE 다섯 화면 × 390px · 1440px 두 폭
- 버튼 얼굴이 뒤집히는가 · 줄 높이가 1.5배 이상 달라지는가 · 그리드가 리스트보다 높은가

30종 추가(40 → 70). 고치기 전 빌드에 걸어 보면 `grid 421px → list 419px` 로 잡힌다.

</details>

<details>
<summary><b>v3.9.0</b> — 2026-09-12 · <code>수정·추가</code> 검색 결과를 도감 화면에서 본다 · 타입 칩이 목록을 거르지 않았다 · 마리 수가 8에서 멈췄다 외 1건</summary>

### 추가 — 검색 결과를 도감 화면에서 본다

검색 패널은 결과를 여덟 줄까지만 그린다. 그 아래를 보려면 검색어를 더 좁히는 수밖에 없었고, 넓은 화면에서는 그 여덟 줄마저 오른쪽에 뜬 좁은 칸 안에 있었다. 찾으러 온 것을 제대로 펼쳐 놓을 화면이 이미 있는데(포켓몬 도감) 쓰지 않고 있었다.

조건을 주소에 실어 도감으로 넘긴다.

```
#/dex?q=리자몽          이름으로
#/dex?t=water,flying    타입 조합으로
#/dex?q=메가&t=water    둘 다
```

세 곳이 같은 데로 간다 — 입력칸의 **Enter**, 결과 머리의 **[도감에서 보기]**, 여덟 줄에서 잘렸을 때 목록 끝에 붙는 **[나머지 N마리는 도감에서 보기]**.

주소가 곧 결과라 링크로 보내거나 즐겨찾기에 넣어 두면 같은 화면이 다시 열린다.

#### 결과 줄은 종 목록이 아니라 검색 색인에서 만든다

도감 목록(`dexEntries()`)에는 도감번호를 가진 **종**만 있다. 그대로 거르면 '메가X 리자몽' · '섀도우 뮤츠' 로 찾았을 때 한 마리도 안 나온다. 그래서 검색 모드에서는 전역 검색 색인(`buildSearchIndex()`)에서 줄을 만든다.

| | 종 목록 | 검색 색인 |
| --- | --- | --- |
| 리자몽으로 찾으면 | 1마리 | 6마리 (원종 · 메가X · 메가Y · 섀도우 · 다이맥스 · 거다이맥스) |

누를 때 열리는 것도 바꿨다. `openDetailByDex(entry.dex)` 는 도감번호로 여니 폼을 눌러도 전부 원종이 떴다. `openDetail(entry)` 로 바꾼다 — 종은 `sprite === dex` 라 전과 똑같이 동작한다. `dexRowStats()` 도 CP 를 그 폼의 종족값으로 낸다(세대는 원종 도감번호 기준 — 폼 id 는 10000 번대라 세대 구간에 안 맞는다).

#### 머리 줄

무엇으로 걸러 몇 마리인지와 `[전체 도감 보기]`. 세대 칩은 그 결과 안에서 다시 거르고, 머리 줄의 수도 같이 줄어든다(232 → 62). 검색 모드에서는 `[더보기]` 를 감춘다 — 찾으러 온 것이 버튼 뒤에 숨으면 안 찾은 것과 같다.

화면 안에 입력칸을 다시 두지는 않았다. v3.5.0 에 뺀 이유가 그대로다 — 헤더 검색과 생김새가 같은 입력칸 둘이 한 화면에 있으면 어느 쪽에 친 글자가 진짜인지가 흐려진다.

### 수정 — 타입 칩이 목록을 거르지 않았다

후보를 고르는 한 줄이 이랬다.

```js
const candidates = types.length && typeof typeMonList === 'function' ? typeMonList(types) : buildSearchIndex();
```

`typeMonList()` 를 들고 있던 것은 상성 검색 화면(`components/typesearch.js`)인데, 그 화면을 v2.63.0 에 접으면서 파일도 같이 지웠다. `typeof … === 'function'` 가드가 있어 오류는 나지 않았고, 조용히 색인 전체로 되돌아갔다.

- `[물]` 을 눌러도 목록은 그대로
- 머리 줄은 `물 타입 2,614마리` — 색인 전체의 수였다

색인 항목이 이미 `types` 를 들고 있으므로 `searchTypePool()` 에서 직접 거른다. 고른 타입을 **모두** 가진 것만 남긴다(물+비행 = 물이면서 비행, 둘 중 하나가 아니다 — 상성 검색이 쓰던 규칙 그대로다).

### 수정 — 마리 수가 8에서 멈췄다

`monSearch(candidates, query, 8)` 로 여덟 개만 받아 놓고 `hits.length` 를 전체 수인 양 적었다. `"리자" 8마리` 는 232마리일 때도 8마리였다. 전부 찾고 나서 여덟 줄을 자른다 — 색인은 수천 줄이라 전부 훑어도 한 번의 입력에서 티가 나지 않는다.

### 회귀

`tests/e2e/dex-search.js` 26종 신설. 전체 21스위트 통과.

</details>

<details>
<summary><b>v3.8.3</b> — 2026-09-12 · <code>수정</code> 보기 전환 버튼이 iOS 에서 눌리지 않았다 · 휴대폰에서 세로 스크롤이 끊겼다 · 회귀</summary>

긴급. 사용자가 아이폰에서 겪은 두 가지다 — 세로 스크롤이 끊기고, 보기 전환 버튼이 안 눌렸다.

### 수정 — 보기 전환 버튼이 iOS 에서 눌리지 않았다

`document.elementsFromPoint()` 로 버튼 한가운데를 찍으면 이렇게 나왔다.

```
rect → rect → svg → button.icon-btn.view-toggle
```

히트 대상은 버튼이 아니라 도트 아이콘 속 네모 한 칸이고, 버튼은 그 위로 네 겹 떨어져 있다. 크로뮴은 거기서 click 이 버튼까지 올라오므로 개발 중에는 아무 문제가 없었다. iOS 사파리는 SVG 자식을 따로 히트 테스트해 탭을 흘린다.

`.pxi { pointer-events: none }`. 그림은 그림일 뿐이니 히트 테스트에서 통째로 빼면, 누르는 것은 언제나 버튼 자신이 된다. 아이콘을 감싼 모든 버튼(메뉴 · 검색 · 닫기 · 테마 · 위로)이 같은 구조였으므로 한 줄이 전부를 덮는다.

### 수정 — 휴대폰에서 세로 스크롤이 끊겼다

점 격자를 **화면을 덮는 고정 레이어**에 칠하고 있었다.

```css
#px-bg { position: fixed; inset: 0; background-image: radial-gradient(…); background-size: 0.8rem 0.8rem; }
```

화면 전체를 덮는 `fixed` 요소가 무늬를 들고 있으면, 본문이 움직일 때마다 그 무늬를 본문 위치에 맞춰 다시 칠해야 한다. 스크롤이 브라우저의 빠른 길(합성만으로 미는 경로)을 벗어나고, 그 대가는 휴대폰에서 제일 크게 나온다.

점은 8px 격자라 본문과 같이 흘러도 움직이는 티가 나지 않는다. `body` 에 그냥 깐다.

| | 전 | 후 |
| --- | --- | --- |
| 점 격자 | `#px-bg` (고정) | `body` |
| 몬스터볼 | `#px-bg`, `.px-ball` 만 `≤699px` 에서 숨김 | `#px-bg` 자체를 `≤1099px` 에서 `display: none` |

요소를 감추는 것으로는 고정 레이어 자체가 사라지지 않아 스크롤 비용이 그대로 남는다. 레이어를 아예 만들지 않는다. 경계를 1100px 로 잡은 것은 가로로 눕힌 휴대폰(932px)까지 덮기 위해서다.

### 회귀

`tests/e2e/nav.js` 에 셋을 더했다 (117개).

- 점 격자는 `body` 에 있다
- 좁은 화면에 화면을 덮는 고정 요소가 하나도 없다
- 도트 아이콘이 탭을 가로채지 않는다 (`elementFromPoint` 가 버튼을 돌려준다)

</details>

<details>
<summary><b>v3.8.2</b> — 2026-09-12 · <code>수정·변경</code> 검색식 만들기: 만들어진 식이 상단 바 뒤에 숨었다 · 묶음 간격을 다시 잡았다 · 머리 안내문에서 겹치는 문장을 뺐다 외 2건</summary>

### 수정 — 검색식 만들기: 만들어진 식이 상단 바 뒤에 숨었다

결과 칸은 `position: sticky; top: 0` 이었다. 그런데 상단 바도 sticky(`z-index: 30`)고 이 칸은 `z-index: 2` 라, 스크롤하면 칸의 위쪽 73px 이 바에 가려 **정작 만들어진 식이 안 보였다** — 남는 것은 `[복사] [비우기]` 줄뿐이었다. 붙여 둔 이유가 통째로 사라진 상태였다.

상단 바 높이만큼 내린다. 좁은 화면은 `calc(5.9rem + max(1.4rem, env(safe-area-inset-top)))`(아이콘 버튼 4.4 + 아래 여백 1.4 + 선 0.1 + 위 여백), 넓은 화면은 사이드바(`.app-nav`)가 쓰는 `8.1rem` 과 같은 값이다.

### 변경 — 묶음 간격을 다시 잡았다

| | 전 | 후 |
| --- | --- | --- |
| 칩 줄 사이(같은 묶음) | 6px | 6px |
| 제목 ↔ 그 묶음의 칩 | 10px | **8px** |
| 묶음 ↔ 다음 제목 | 18px | **28px** |

셋이 6 · 10 · 18 이라 거의 같았다. "어디부터 어디까지가 한 묶음" 이 안 읽힌 이유다. 붙을 것은 붙이고 뗄 것은 뗀다.

- 안내문 ↔ 결과 칸이 0px 으로 맞붙어 있던 것도 16px 로 띄웠다
- 각주(`.detail__foot`)는 24px

### 변경 — 머리 안내문에서 겹치는 문장을 뺐다

`게임 검색창에 붙여 넣을 검색식을 만들어요` 는 바로 위 화면 부제(`조건을 눌러 게임 검색창에 붙여 넣을 식을 만들어요`)가 이미 하는 말이다. 이 줄이 할 일은 세 상태를 도는 규칙(`＋포함` → `－제외` → 해제)을 알리는 것뿐이다.

### 변경 — 남은 이모지를 도트 아이콘으로

`🧹 박스 정리` · `🤝 교환용` · `📋 복사`. 라벨 앞 이모지를 가르는 일을 `pxIconLabelParts()` 로 뗐다 — `toolButton()`(v3.7.1)이 같은 코드를 따로 들고 있었다. 빗자루 · 교환(위아래 두 화살) · 클립보드를 더해 도트 아이콘 35종.

- `↺ 비우기` 의 `↺` 는 그대로 둔다. 컬러 이모지가 아니라 글자라 도트 화면에서 결이 튀지 않는다
- 칩(`.uchip`)은 `align-items: baseline` 이라 SVG 가 글자 밑으로 내려앉는다 — 그림에는 baseline 이 없으니 `align-self: center`

### 회귀

20개 전부 통과. `finder.js` 에 넷을 더했다 — 식 칸이 상단 바에 안 가리는지 · 스크롤해도 식이 보이는지 · 바 바로 아래에 붙는지 · 묶음 안 간격이 묶음 사이보다 좁은지.

</details>

<details>
<summary><b>v3.8.1</b> — 2026-09-12 · <code>수정</code> 상세 화면의 타입 배지가 그림을 덮었다</summary>

### 수정 — 상세 화면의 타입 배지가 그림을 덮었다

두 타입이면 배지가 세로로 쌓여 그림 왼쪽을 위에서 아래까지 덮었다. **가로 한 줄**로 놓는다 — 그림 위쪽 빈 자리만 스친다.

- 가장 긴 경우(`에스퍼` `페어리`, 102px)가 그림 상자(96px) 오른쪽 끝에 딱 맞는다. 글 칸으로 넘치지 않는다
- 배지 안에서 줄이 접히지 않게 `white-space: nowrap`

</details>

<details>
<summary><b>v3.8.0</b> — 2026-09-12 · <code>수정·변경</code> 보기 전환(리스트 ↔ 그리드)을 버튼 하나로 · 구역 제목 꼬리말을 내리는 범위를 좁혔다 · 회귀</summary>

### 변경 — 보기 전환(리스트 ↔ 그리드)을 버튼 하나로

**상태가 둘뿐인데 칸을 둘 둘 이유가 없다.** 화면 테마 버튼(`components/theme.js`)과 같은 문법이다 — 아이콘은 **지금 보기**를 보여 주고, 누르면 반대로 넘어간다.

모양이 두 번 바뀌었다. 왜 다시 버튼 하나인지 `components/ui.js` 머리말에 남겨 뒀다.

| 시기 | 모양 | 문제 |
| --- | --- | --- |
| ~v2.39 | 버튼 하나 | 라벨이 "지금 보기" 인지 "누르면 될 보기" 인지가 글자만으로 안 갈렸다 |
| v2.40.0 | 두 칸 세그먼트 | 지금 어느 쪽인지는 또렷해졌지만 자리를 두 배로 먹었다. v3.1.0 에서 화면 머리로 올린 뒤에는 제목을 밀어냈고, 좁은 화면에서는 글자를 떼고 아이콘만 남겨야 겨우 들어갔다 |
| **v3.8.0** | **버튼 하나** | 옛 문제는 화면 테마 버튼이 이미 푼 방식으로 푼다 — 아이콘은 지금 상태, 이름은 "지금 이것 · 누르면 저것" |

- 지금 보기는 색으로도 말한다 — 그리드면 강조색으로 칠한다 (`[data-view="grid"]`)
- 그림이 SVG 라 글자로 상태를 못 읽는다. `data-view` 에 남긴다 (화면 테마의 `data-icon` 과 같은 뜻)
- 이름(`aria-label`)은 v2.40.0 **이전** 문구를 그대로 되살렸다 — 사전(`i18n-en.js`)에 그때 키가 아직 있었다
- 저장 키(`pogo_dex_cols` 등)와 값 관례(`'1'` 리스트 / `'2'` 그리드)는 그대로다
- 클래스는 `.seg-view` → `.view-toggle` 로 옮겼다. 세그먼트가 아닌데 이름이 세그먼트면 다음 사람이 두 칸을 찾는다
- 격자 아이콘(`⊞`)을 도트 표에 더해 32종이 됐다

좁은 화면의 `.view-toggle` 축소 보정(v3.7.1)도 함께 지웠다 — 이제 헤더의 🌗 · ☰ 와 같은 44px 정사각 아이콘 버튼이라 따로 줄일 것이 없다.

### 수정 — 구역 제목 꼬리말을 내리는 범위를 좁혔다

v3.7.1 에서 좁은 화면의 제목 꼬리말을 아랫줄로 내렸는데, 실제로 접히던 것은 티어표 제목 줄(`.row-head`) 하나였다. 구역 제목(`.page__sec`)의 꼬리말은 `5성 레이드 2종` 처럼 짧아 한 줄에 드는데도 같이 내려가 있었다.

### 회귀

20개 전부 통과. 갱신: `layout-toggle`(칸 둘 → 버튼 하나. 원하는 보기가 아닐 때만 누르는 `setView()` 로 바꾸고, "한 번 더 누르면 되돌아온다" 를 더했다) · `shell`(그리드로 만들 때 칸이 아니라 버튼을 누른다) · `i18n`(번역이 닿는 곳이 칸 둘의 글자에서 버튼 이름 하나로 옮겨졌다).

</details>

<details>
<summary><b>v3.7.1</b> — 2026-09-12 · <code>변경</code> 굵기는 한 칸 옆으로 한 번 더 찍어서 낸다 · 좁은 화면의 화면 머리 배치 · 도구 버튼의 이모지를 도트 아이콘으로 외 2건</summary>

### 변경 — 굵기는 한 칸 옆으로 한 번 더 찍어서 낸다

v3.6.1 에서 가짜 굵기를 걷어내고 400 으로 내렸더니 이번에는 너무 얇았다. 그렇다고 700 을 되돌리면 다시 번진다 — 브라우저의 가짜 굵기(synthesized bold)는 획을 소수점 단위로 덧그려 도트 격자를 벗어나기 때문이다.

`text-shadow: 0.1rem 0 0 currentColor` — 1px 짜리 그림자는 격자에 딱 맞아 획이 **정확히 한 도트** 두꺼워진다. 도트 글꼴의 원래 방식이다.

- 거는 자리는 v3.6.1 이전에 굵기를 주던 곳 — 제목 · 이름 · 숫자 · 버튼 글자 · 배지
- 읽는 글(산문)에는 걸지 않는다. 거기는 Pretendard 라 진짜 굵은 획이 있다

### 변경 — 좁은 화면의 화면 머리 배치

| 화면 | 전 | 후 |
| --- | --- | --- |
| 레이드 · PvE | `[일반\|전체]` 와 `[리스트\|그리드]` 가 오른쪽에 나란히 | 위아래로 |
| 배틀 · PvP | 리그 넷이 한 줄 | 2 × 2 |

둘 다 오른쪽 덩이가 넓어 화면 제목이 남은 폭으로 밀려 두 줄이 됐다. 이제 제목이 한 줄에 들어온다.

2 × 2 는 `:has(> button:nth-child(4))` 로 칸이 넷 이상일 때만 건다 — D-MAX 의 `[전체·딜러·탱커]` 셋을 2 × 2 로 만들면 마지막 칸이 비어 한 칸이 빠진 것처럼 보인다.

구역 제목과 그 옆 꼬리말도 같은 처방으로 갈랐다 — `레이드 일반 티어표 (전 / 체)` 와 `30종 · 전설·환상·메가·섀도우 제 / 외` 가 양쪽에서 동시에 접히고 있었다.

### 변경 — 도구 버튼의 이모지를 도트 아이콘으로

`🧬 개체값 순위` · `🃏 덱 짜기` · `🧮 솔플 계산기`. 라벨이 한 문자열이라 `toolButton()` 에서 앞 이모지를 한 번에 가른다 — 글자는 이름만 남겨 사전(`i18n-en.js`)이 찾게 두고, 표에 없는 이모지면 라벨을 그대로 쓴다.

### 수정 — 넓은 화면에서 도트 규칙이 새어 나왔다

v3.6.1 의 곡선 제거는 `html:not(#_) body *` 로 우선순위를 id 한 칸 올려 걸었는데, 1440px 이상에서 쓰는 `body:is([data-route="dmax"], …) #content .row > .sprite` 가 **id 한 칸 + 클래스 셋**이라 그걸 이겼다. 그래서 티어표 카드의 그림 받침만 24px 둥근 모서리와 발광 그라데이션이 그대로 남아 있었다.

- 곡선 제거·그림 받침·카드 테두리 셋 다 `:not(#_):not(#__)` 로 두 칸 올렸다
- 그림 받침 규칙은 자리 이름을 늘어놓던 것을 `.sprite:not(.is-loading):not(.empty)` 한 줄로 합쳤다 — 화면마다 따로 적힌 발광 판을 이름으로 쫓아다니지 않는다
- 카드 테두리도 같은 자리에서 1px 로 따로 적혀 있었다. 한 서비스 안에서 카드 테두리 두께가 둘이면 같은 종류로 안 읽힌다

### 회귀

20개 전부 통과. 갱신: `nav`(도구 버튼 글자에서 이모지가 빠졌다 · 도트 아이콘 둘) · `breakpoints`·`shell`(도트 디자인에는 곡선이 없으니 "카드냐" 를 반경이 아니라 **세로 쌓임 + 2px 테두리**로 가른다).

</details>

<details>
<summary><b>v3.7.0</b> — 2026-09-12 · <code>변경</code> 키 컬러를 원작의 몬스터볼 빨강으로 · 앱 아이콘이 채운 몬스터볼로 (`backend/icon_gen.py`) · 공유 카드도 채운 몬스터볼 (`backend/og_gen.py`) 외 3건</summary>

### 변경 — 키 컬러를 원작의 몬스터볼 빨강으로

도트 디자인(v3.6.0)의 키 컬러를 원작에 맞춘다. 스플래시의 몬스터볼과 공유 카드 그림이 이미 이 빨강인데 브랜드만 인디고로 남아, 같은 화면에서 셋이 따로 놀고 있었다.

| 토큰 | 전 | 후 | 왜 |
| --- | --- | --- | --- |
| `--brand` · `--accent` | `#6366f1` 인디고 | **`#d63024`** | 몬스터볼 빨강 |
| `--brand-2` | `#a78bfa` 바이올렛 | `#b3231b` | 같은 계열 한 단 진한 빨강 — 한 서비스 안의 다른 갈래(PvP)라 계열까지 바꿀 이유가 없다 |
| `--warn` | `#dc2626` 빨강 | `#9f1239` | 빨강을 브랜드에 내준다. "나쁨" 은 여전히 붉지만 어둡기·색기가 갈려 "선택됨" 과 안 헷갈린다 |
| `--point` | `#ec4899` 핑크 | `#2f6fd0` | 브랜드 빨강 바로 옆 색이라 못 쓴다. 빨강의 정반대라 특별한 것(메가·전설·S티어)이 확실히 튀고, 바로 옆에 놓이는 A티어(`--caution` 앰버)와도 안 싸운다 |

다크는 어두운 바탕에서 `#e5372e` 가 무거워 보여 한 단 밝은 `#ff5f52` 를 쓴다. `--good`(teal) · `--caution`(앰버)은 그대로다.

**그림과 글자의 빨강을 반 단 가른다** — 그림(스플래시 공 · 앱 아이콘 · 공유 카드)은 원작 값 `#e5372e`, 글자로 쓰이는 토큰은 `#d63024`. `#e5372e` 는 밝은 바탕(`#fafafa`)에서 명암비가 4.1 이라 작은 글씨가 흐려 보인다 (`#d63024` 는 4.7).

### 변경 — 앱 아이콘이 채운 몬스터볼로 (`backend/icon_gen.py`)

선으로만 그린 흰 공은 어느 서비스인지 말해 주지 않는다. 홈 화면에 놓이는 그림이라 한눈에 "포켓몬" 으로 읽혀야 한다.

- 위 빨강 · 아래 흰색 · 가운데 검은 띠. 띠는 단추 자리를 비운다 — 가로지르면 단추가 반으로 잘려 몬스터볼로 안 읽힌다
- 바탕도 v3.0.0 잉크블랙(`#0a0a0f`)으로 맞췄다. `(18, 19, 21)` 로 멈춰 있었다

### 변경 — 공유 카드도 채운 몬스터볼 (`backend/og_gen.py`)

**팔레트가 두 번 바뀌는 동안(v3.0.0 인디고 · v3.7.0 빨강) 이 파일만 v2 시절 초록(`#4fb37c`)에 멈춰 있었다.** 공유 카드가 서비스와 다른 색이면 눌러 들어온 순간 결이 어긋난다.

- 토큰 다섯을 현재 값으로 (`BG` · `FG` · `MUTED` · `ACCENT` · `LINE`)
- 몬스터볼을 선에서 **채움**으로 — 카카오톡·디스코드 미리보기는 작게 뜨는데, 선으로만 그린 공은 그 크기에서 그냥 동그라미다

### 수정 — `theme-color` 메타 두 줄이 v2 값에 멈춰 있었다

브라우저 주소창 색이다. `#ffffff` · `#121315` → 현재 `--bg` 인 `#fafafa` · `#0a0a0f`.

### 변경 — 바탕을 한 단 더 죽였다

빨강은 인디고보다 눈에 잘 띈다. 바탕 몬스터볼 투명도 .10 → .08, 서비스 홈 히어로의 점 격자 24% → 16%.

### 회귀

20개 전부 통과(145초). `nav.js` 의 팔레트 검사를 새 값으로 옮기고 둘을 더했다 — `--warn` 이 브랜드와 다른 빨강인지, `--point` 가 빨강의 반대쪽인지.

</details>

<details>
<summary><b>v3.6.1</b> — 2026-09-12 · <code>수정·변경</code> 바탕이 글자 뒤에서 그대로 비쳤다 · 도트 글꼴에 가짜 굵기가 씌워져 있었다 · 글씨 크기·줄간 외 4건</summary>

도트 디자인을 실제 화면에서 보고 다듬었다. 바탕이 글자를 가렸고, 글꼴은 도트로 안 보였고, 둥근 자리가 남아 있었다.

### 수정 — 바탕이 글자 뒤에서 그대로 비쳤다

또렷한 점 격자 위에 글이 놓여 읽기가 힘들었다. 배경을 본문과 한 층에 두지 않고 `#px-bg` 한 겹으로 뺐다 — 흐리기를 걸 자리가 한 곳이어야 본문이 같이 흐려지지 않는다.

| | 전 | 후 |
| --- | --- | --- |
| 점 | 가장자리가 또렷한 원 | `transparent 65%` 로 흐린 원 |
| 농도 | `--fg` 7% | `--fg` 4.5% |
| 몬스터볼 | 그대로 | `filter: blur(0.6rem)` · 투명도 .12 → .10 |

### 수정 — 도트 글꼴에 가짜 굵기가 씌워져 있었다

**v3.6.0 의 제목과 포켓몬 이름이 도트로 안 보였던 진짜 이유다.** 갈무리에는 굵은 획(Bold)이 없다. `font-weight: 700` 을 주면 브라우저가 획을 옆으로 한 겹 덧그려 흉내를 내는데(synthesized bold), 그 순간 도트가 번져 그냥 흐린 고딕이 된다.

- 본문 전체를 400 으로 내리고, 굵기는 색(`--fg` ↔ `--muted`)과 크기로 낸다
- 긴 산문(Pretendard)은 진짜 굵은 획이 있으니 `.prose`·`.note`·`.legal`·`.release__sec` 안의 `b`·`strong` 에서만 700 을 되살린다

### 변경 — 글씨 크기·줄간

v3.6.0 은 글꼴만 갈아끼우고 크기·줄간은 옛 값을 그대로 뒀다. 도트 글꼴은 획이 얇고 글자가 작아, 같은 수치라도 훨씬 작고 빽빽해 보인다.

| 자리 | 전 | 후 | 왜 |
| --- | --- | --- | --- |
| 제목 줄간 | 22px (1.05배) | 1.35배 | 두 줄로 넘어가면 위아래가 맞붙었다 |
| 부제 · 안내문 | 11~12.5px | 13px / 줄간 20px | 한눈에 안 들어왔다 |
| 꼬리 글씨(`.meta` 등) | 줄간 22px (2배) | 17px | 줄끼리 흩어져 한 덩이로 안 읽혔다 |

### 변경 — 여백

- 구역 제목 위 40px → 20px. 아래(12px)와의 차이는 지켜야 제목이 아래 내용에 붙지만, 위가 과했다
- 필터 줄 아래 24px → 16px
- **빈 컨트롤 줄 제거** — 서비스 홈처럼 걸 것이 없는 화면에서도 위아래 여백(20 + 40px)만으로 60px 짜리 빈 줄이 제목 앞에 서 있었다
- 좁은 화면의 구역 제목 줄은 꼬리말을 아랫줄로 — 갈무리가 Inter 보다 넓어 `무엇을 해볼까 / 요?` 로 잘렸다
- 좁은 화면의 보기 전환(☰ ⊞) 높이 36px → 30px. 화면 제목보다 덩치가 커 보였다

### 수정 — 남아 있던 둥근 모서리

v3.6.0 은 둥근 자리의 **이름을 하나씩 적어** 눌렀다. 그래서 목록에 없던 자리가 그대로 둥글게 남았다: 상세 화면의 카드와 알약 배지, 그림 받침(넓은 화면에서는 아예 정원이었다), 일정표 날짜 칸, 검색식 입력칸, 티어 배지, 진화 칸, 맨 위로 버튼 …

이름을 더 적어 봐야 다음에 또 빠진다. 뿌리에서 한 번에 누르고(`html:not(#_) body *`), 진짜 동그란 것만 되돌린다 — 프로필 사진 · 알림 점 · 바탕 몬스터볼. 타입 색 점은 되돌리지 않는다: 도트 디자인에서 한 칸짜리 네모가 곧 점이다.

그림 받침의 무늬도 동심원(`modal.css`)에서 바탕과 같은 점 격자로 바꿨다 — 한 화면 안에서 바탕 무늬가 두 종류일 이유가 없다.

### 변경 — 문단 속 이모지를 도트 아이콘으로

`🧮 솔플 계산기` · `📅 이벤트 일정` · `🎯 포획 CP` · `🧮 내 개체 CP 계산기`. 문단 한가운데 기기가 그린 컬러 이모지가 끼면 그 줄만 결이 튄다. 과녁(🎯) 아이콘을 표에 더해 31종이 됐다.

- 글자는 이름만 남겨 사전(`i18n-en.js`)이 찾게 두고, 이모지를 뗀 키를 세 줄 더했다
- 접이줄 머리(`summary`)는 flex 라 아이콘 뒤 빈칸이 통째로 사라진다 — 사이는 CSS 로 벌린다

### 회귀

20개 전부 통과(146초). `nav.js` 에 다섯을 더했다 — 가짜 굵기 없음(제목 · 타일 이름) · 바탕 무늬가 `#px-bg` 한 겹에만 · 몬스터볼 흐리기 · 빈 컨트롤 줄 높이 0.

</details>

<details>
<summary><b>v3.6.0</b> — 2026-09-12 · <code>추가·변경</code> 도트(픽셀) 디자인 · 도트 아이콘 (`components/pxicon.js`) · 바탕의 몬스터볼과 점 격자 외 3건</summary>

### 변경 — 도트(픽셀) 디자인

**이 서비스가 보여 주는 그림이 전부 96px 도트 스프라이트인데 껍데기만 매끈했다.** 껍데기를 그림 쪽으로 맞춘다.

- 글꼴 — 갈무리(Galmuri11 본문 · Galmuri14 제목 · GalmuriMono11 숫자). 긴 산문(약관 · 안내문 · 패치노트)만 Pretendard 로 남긴다 — 도트 글꼴로 문단을 읽히게 하는 것은 다른 문제다
- 모양 — `--r-card` · `--r-chip` 0, 상자 테두리 2px(`--px`), 그림자는 흐림 0 의 오프셋(`--shadow-hard`), 전환은 `steps()`
- 규칙은 `frontend/styles/pixel.css` 한 파일에 모았다 — 되돌리려면 `build.py` 의 `STYLES` 에서 이 줄 하나만 빼면 된다

### 추가 — 도트 아이콘 (`components/pxicon.js`)

메뉴 · 홈 타일 · 버튼의 이모지를 12칸 격자에 직접 찍은 SVG 로 바꿨다. 이모지는 기기가 그리는 그림이라 아이폰 · 안드로이드 · PC 가 저마다 다르게, 그리고 하나같이 매끈하게 그린다.

- 표(`PXI_ART`)의 키가 이모지다 — 라우터 표(`ROUTES.icon`) · 홈 타일 · `iconBtn()` 이 이미 이모지를 들고 있어 호출부는 `pxIcon(icon) ?? icon` 한 줄씩만 고쳤다
- 표에 없는 이모지는 그대로 둔다. 패치노트 · 안내 문구 속 이모지는 글이라 건드리지 않는다
- `index.html` 에 글자로 박혀 있던 자리(헤더 버튼 · ☰ 메뉴 아래쪽 줄들)는 번들 끝에서 한 번 훑어 갈아 끼운다
- 30종: 몬스터볼 · 집 · 도감 기기 · 반짝임 · 엇갈린 검 · 카드 · 달력 · 알 · 돋보기 · 나선 · 계산기 · 가방 · 새싹 · 세 줄 · 가위표 · 화살표 셋 · 반달 · 해 · 달 · 사람 · 자물쇠 · 안내 · 두 사람 · 꽃가루 · 스패너 · 종이 · 쿠키 · 조각

### 추가 — 바탕의 몬스터볼과 점 격자

레퍼런스 포스터(짙은 남색 + 점 격자 + 큰 원)의 문법을 가져오되, 원 자리에 몬스터볼을 놓았다. 원 하나로는 어느 서비스인지 알 수 없지만 띠와 가운데 단추가 붙으면 무엇을 다루는 곳인지가 그림 하나로 읽힌다.

- 그림 파일이 아니다 — 요소 하나 + `::before`(띠) + `::after`(단추)
- `steps(8)` 로 20 · 26초에 걸쳐 칸칸이 떠다닌다. `prefers-reduced-motion` 이면 멈춘다
- 좁은 화면(< 700px)에서는 빼고, 서비스 홈 히어로의 부드러운 빛 번짐도 점 격자 + 또렷한 몬스터볼로 바꿨다

### 변경 — 불러오는 화면

몬스터볼이 좌우로 기우뚱한다 — 공을 던진 뒤 그 움직임. `steps(4)` 라 칸칸이 꺾인다.

### 변경 — 검색 색인용 표시 제거

아직 검색엔진에 올릴 단계가 아니다.

| | |
| --- | --- |
| 뺀 것 | 실서비스 `<meta name="robots" content="index, follow, …">` · 구조화 데이터(JSON-LD) |
| 남긴 것 | `og:*` · `twitter:*`(링크 미리보기 카드) · `canonical` · `description` · `sitemap.xml` |

dev 미리보기의 `noindex` 와 `robots.txt` 전면 차단은 그대로다. 되살릴 때는 `index.html` 의 주석과 `backend/build.py` 의 robots 분기, `tests/e2e/hardening.js` 의 검사 둘을 함께 뒤집으면 된다.

### 수정 — 넓은 화면에서 사이드바가 흘러내리던 것

`pixel.css` 가 바탕 몬스터볼 위로 본문을 올리려고 `.app-nav` 에 `position: relative` 를 덮어써, `position: fixed` 인 사이드바가 제자리를 잃고 본문이 그 아래로 1,600px 밀려 있었다. `position` 은 본문 두 덩이(`.layout` · `#page`)에만 준다 — 상단 바(sticky)와 사이드바(fixed)는 이미 자기 자리 잡는 방식이 있다.

</details>

<details>
<summary><b>v3.5.0</b> — 2026-09-12 · <code>추가·변경</code> 포켓몬 도감 안의 검색 칸을 뺐다 · D-MAX 보기 전환 · [이번 주 보스] ↔ 타입 필터 간격 32px → 8px</summary>

### 변경 — 포켓몬 도감 안의 검색 칸을 뺐다

v2.66.0 에 안내 문구로 성격을 갈라 놓았지만(헤더는 "어디로든 데려가는" 검색, 이 칸은 "이 목록을 거르는" 칸) **생김새가 같은 입력칸 둘이 한 화면에 있는 것 자체가 문제**였다. 헤더 검색은 어차피 포켓몬을 찾아 상세로 데려간다.

- 세대 칩은 그대로 남아 목록을 거른다
- 검색 칸을 제목 옆으로 끌어올리던 규칙(v2.65.0 의 음수 마진)도 함께 지웠다

### 추가 — D-MAX 보기 전환

넓은 화면에서 티어표가 카드 격자로 그려지는데(`pc-theme.css .row-list`) 줄로 되돌릴 방법이 여기만 없었다. 레이드 · PvE(v3.3.0)와 같은 규칙으로 축 세그먼트 옆에 나란히 둔다. 저장 키는 `pogo_max_cols` — 화면마다 선택이 섞이지 않게 따로다.

### 변경 — [이번 주 보스] ↔ 타입 필터 간격 32px → 8px

`.boss__acc` 의 아래 마진 12px 과 `.controls` 의 위 패딩 20px 이 겹쳐 32px 이었다. 둘은 "이번 주 무엇이 도나" → "그중 무엇을 볼까" 로 이어지는 한 덩이라 멀 이유가 없다.

- 아코디언이 뜨는 화면(D-MAX)에서만 좁힌다 — `renderBossAcc` 가 붙이는 `.is-on` 을 CSS 가 본다. 다른 화면의 컨트롤 줄 여백은 그대로다

</details>

<details>
<summary><b>v3.4.0</b> — 2026-09-12 · <code>수정·변경</code> ★ 즐겨찾기를 통째로 걷어냈다 · 메뉴의 `내 포켓몬` 들여쓰기</summary>

### 변경 — ★ 즐겨찾기를 통째로 걷어냈다

**담을 수는 있는데 담은 뒤에 할 수 있는 일이 없었다.**

★ 를 누르면 계정에 종이 쌓였고, 그 목록을 다시 보는 길이 세 갈래였다(v2.66.0 에 헤더 ★ 하나로 모았다). 그런데 모아 놓고도 그다음이 없었다 — "그래서 뭘 키우지" 로 이어지지 않았고, 그 일은 **육성 플래너**가 이미 한다.

반쯤 하는 기능을 남겨 두면 화면마다 ★ 자리를 지키고, 헤더 버튼 하나를 먹고, 메뉴에 한 줄을 더한다. 쓸모를 정한 뒤에 다시 만든다 (노션 백로그 등록).

**지운 것**

| 조각 | 자리 |
| --- | --- |
| `#/favs` 전용 화면 | `ROUTES` · `PAGES` 에서 뺐다. 옛 주소는 `legacy` 로 도감에 잇는다 |
| 헤더 ★ 팝업 | `components/favdigest.js` 삭제 · `#fav-toggle` 버튼 제거 |
| 목록·상세의 ★ 버튼 | 도감 · 레이드 보스 · 알 부화 · 상세 팝업 |
| 역할 보정 토글 | 상세 팝업의 `★ 즐겨찾기 분류` 섹션 |
| `initFavsMenu()` | 이미 죽은 코드였다 (`#menu-favs` 가 index.html 에 없었다) |

**남긴 것 — 중요**

- **`AUTH.favs` 의 저장·동기화는 그대로다** (`components/auth.js`). 계정에 담아 둔 목록은 Firestore 에 살아 있다. 다시 만들 때 그대로 되살아난다
- `roleWhereLabel()` — `components/favs.js` 에 남았다. 즐겨찾기와 무관하게 **헤더 검색 결과의 "레이드 전체 1위" 보조줄**(`components/search.js`)이 쓴다

### 수정 — 메뉴의 `내 포켓몬` 들여쓰기

v2.66.0 에서 부모(육성 플래너) 아래 한 칸 들여 썼는데, 좁은 화면 드로어는 줄마다 `›` 화살표가 붙는 목록이라 한 줄만 안쪽으로 밀리면 어긋나 보였다. 들여쓰기를 걷었다 — 소속은 순서(부모 바로 뒤)로 이미 읽힌다.

`drawer__item--sub` 클래스는 남긴다. 회귀가 "자식 줄인가" 를 이 클래스로 확인한다.

</details>

<details>
<summary><b>v3.3.0</b> — 2026-09-12 · <code>변경</code> 레이드 · PvE 재배치</summary>

### 변경 — 레이드 · PvE 재배치

읽는 차례대로 놓는다 — ① 어느 표를 볼까(머리) ② 타입으로 좁히기 ③ 혼자 잡을 수 있나.

- **`[일반 | 전체]` 를 화면 머리 오른쪽으로** — 목록을 거르는 값이 아니라 **이 화면이 어느 표를 보여 주는가** 다 (D-MAX 의 축 · PvP 의 리그와 같은 성격)
- **그 옆에 보기 전환을 새로 달았다** — 넓은 화면에서 랭킹이 카드 격자로 그려지는데(`pc-theme.css`) 지금까지 줄로 되돌릴 방법이 없었다. 도감 · 즐겨찾기에는 있던 선택지가 여기만 없었다. `.row-list.is-list` 가 격자를 줄로 되돌리고, 저장 키는 `pogo_pve_cols`
- **타입 필터는 기본 펼침** (`state.filtersOpen` 기본값 `true`) — 접힌 채로 두면 이 화면을 쓰는 주된 길이 한 번 더 눌러야 보인다. 접으면 그 상태가 세션 동안 유지된다
- **🧮 솔플 계산기는 타입 필터 아래 줄로** — 전에는 `[일반|전체]` 옆에 붙어 있었다

동작 슬롯이 컨트롤을 **여럿** 받는다 (`setPageHeadAction(...nodes)`). 컨트롤이 전부 머리로 옮겨 가 빈 껍데기만 남는 줄은 `:empty` 로 자리를 먹지 않는다.

</details>

<details>
<summary><b>v3.2.0</b> — 2026-09-12 · <code>수정·변경</code> D-MAX 축 · PvP 리그도 화면 머리로 · 앞 화면의 컨트롤이 머리에 남아 있었다 · D-MAX 로 바로 들어온 첫 화면</summary>

### 변경 — D-MAX 축 · PvP 리그도 화면 머리로

v3.1.0 에서 만든 동작 슬롯을 메인 셸까지 넓힌다. 뷰가 컨트롤에 `js-head-action` 표식만 달면 `render()` 가 슬롯으로 옮긴다 — 뷰는 지금까지처럼 `$controls` 에 붙이기만 하면 된다.

- **D-MAX `[전체 | 딜러 | 탱커]`** — 목록 하나를 거르는 값이 아니라 **이 화면이 무엇을 보여 주는가** 자체다. 셋은 서로 다른 순위표다
- **배틀 · PvP 리그** — 리그가 바뀌면 순위표도 개체값 순위도 덱 추천도 전부 다른 화면이 된다
- 슬롯을 다루는 함수는 `app-shell.js` 한 곳(`setPageHeadAction`). 전체 페이지는 `pages.js liftViewToggle`, 메인 셸은 `app.js liftShellHeadAction` 이 같은 함수를 부른다

### 수정 — 앞 화면의 컨트롤이 머리에 남아 있었다

알 부화에서 D-MAX 로 넘어가면 `[리스트 · 그리드]` 가 제목 옆에 그대로 붙어 있었다. v3.1.0 은 전체 페이지(`renderPage`)에서만 슬롯을 맞췄고, 메인 셸(`render`)은 슬롯을 건드리지 않았다.

- `render()` 가 화면을 그릴 때마다 슬롯을 맞춘다. 옮길 것이 없으면 **비운다** — 홈 · 육성 플래너 · 잠긴 화면으로 빠지는 이른 return 앞에서도 한 번 비운다

### 수정 — D-MAX 로 바로 들어온 첫 화면

축 세그먼트가 본문에 남아 있었다(화면을 한 번 옮기면 정상이 됐다). `components/app-shell.js` 는 `SCRIPTS` 의 **맨 끝**이라 `app.js` 가 바닥에서 부르는 첫 `render()` 때는 아직 `#page-head-actions` 가 없다.

- 셸을 다 만든 뒤 `app-shell.js` 가 한 번 더 맞춘다
- 슬롯은 `const` 로 잡지 않고 **id 로 찾는다** — 번들에서 함수 선언은 끌어올려지지만 `const` 는 그렇지 않아(TDZ), 앞줄의 `pages.js` 가 로드 직후 `renderPage()` 를 부르면 화면 전체가 죽었다 (`Cannot access 'pageHeadActions' before initialization`)

</details>

<details>
<summary><b>v3.1.0</b> — 2026-09-12 · <code>변경</code> 보기 전환을 화면 머리로</summary>

### 변경 — 보기 전환을 화면 머리로

리스트 · 그리드 전환이 목록 위 한 줄을 통째로 쓰고 있었다. **보기 방식은 목록 하나가 아니라 그 화면 전체에 걸리는 설정**이라, 본문 안이 아니라 제목과 같은 높이에서 화면의 손잡이처럼 놓이는 편이 맞다.

- 화면 머리에 동작 슬롯(`.page-head__actions`)을 만들었다. 머리를 두 칸 격자로 두고 왼쪽은 "여기가 어디인가"(브레드크럼 · 제목 · 부제), 오른쪽은 "어떻게 볼까"
- 슬롯이 비면 폭을 먹지 않는다 (`grid-template-columns: minmax(0,1fr) auto` + `:empty { display: none }`)
- 토글은 **본문에서 옮겨 담는다**(`renderPage` → `liftViewToggle`). 복제가 아니라 이동이라 `onclick` · `aria-pressed` · localStorage 키가 붙은 그 노드가 그대로 간다 — 화면마다 토글을 따로 만들 필요도, 각 화면 렌더러가 머리를 알 필요도 없다
- 네 화면(도감 · 레이드 보스 · 알 부화 · 즐겨찾기)이 한 번에 같은 자리가 된다. 스타일 가이드는 예외 — 거기 `.seg-view` 는 "이렇게 생겼다" 를 보여 주는 견본이라 옮기면 안 된다
- 덤으로 도감의 검색창과 토글이 더는 같은 줄을 다투지 않는다 (토글이 한 단 위로 갔다)

</details>

<details>
<summary><b>v3.0.0</b> — 2026-09-12 · <code>변경</code> 바탕과 카드를 뒤집는다 · 회색이 파란 기에서 보라 기로 · 브랜드가 초록에서 인디고로 외 2건</summary>

새 디자인 시스템. 스타일 레퍼런스([yceffort.kr](https://yceffort.kr/))의 색·글꼴 체계를 가져왔다. 화면 구조와 기능은 그대로고, **토큰과 글꼴만** 갈아끼웠다 — `styles/tokens.css` 한 곳이 색을 쥐고 있어 가능한 일이다.

### 변경 — 바탕과 카드를 뒤집는다

전에는 **흰 바탕 위에 회색 카드**였다. 이제 살짝 꺼진 바탕(`#fafafa`) 위에 **흰 카드**가 뜬다.

목록이 곧 카드 더미인 서비스라 카드가 바탕보다 밝아야 "떠 있다" 로 읽힌다. 회색 카드는 바탕에 눌려 보였다.

| | 전 | 후 |
| --- | --- | --- |
| `--bg` | `#ffffff` | `#fafafa` |
| `--surface` | `#f5f7fa` | `#ffffff` |
| `--hover` | `#eef1f6` | `#f2f2f5` |
| `--line` | `#e4e8ee` | `#e5e5eb` |

### 변경 — 회색이 파란 기에서 보라 기로

`--muted` 가 슬레이트(`#64748b`, 파란 기)에서 `#64647a`(보라 기)로 간다. 포켓몬 타입색 열여덟 개 중 파랑 계열이 넷(물·비행·에스퍼·강철)이라, 파란 기 도는 회색은 그 옆에서 계속 싸웠다.

`--line-2` · `--ink-2` · `--ink-4` 세 단을 더해 레퍼런스의 잉크 사다리를 그대로 가져왔다.

### 변경 — 브랜드가 초록에서 인디고로

`#16a34a` → `#6366f1` (다크 `#22c55e` → `#818cf8`).

**초록은 `--t-grass`(풀 타입)로만 남는다.** 브랜드색과 타입색이 같은 초록이라 "이게 선택 표시인가 풀 타입인가" 가 늘 한 박자 걸렸는데, 그 혼동도 같이 사라진다.

- "좋음(상향·내성)" 을 브랜드 초록이 겸하고 있었다 — `--good`(teal `#0d9488` / 다크 `#2dd4bf`)으로 뗐다. 그러지 않으면 ▲상향과 "지금 여기" 가 같은 인디고가 된다
- `--point` 는 보라(`#9333ea`)에서 핑크(`#ec4899`)로 — 브랜드가 보라 계열로 오면서 둘이 붙어 버렸다

### 변경 — 글꼴 세 벌

| 쓰임 | 전 | 후 | 왜 |
| --- | --- | --- | --- |
| 본문 · UI | Montserrat | **Inter** | Montserrat 은 제목용 기하 산세리프라 1.2rem 보조줄에서 글자폭이 들쭉날쭉했다 |
| 숫자 | (본문과 같음) | **JetBrains Mono** | CP · 순위 · 개체값을 세로로 줄줄이 세우는 서비스인데 비례 글꼴이라 자릿수가 어긋나 보였다 |
| 강조 한 마디 | — | **Fraunces** (이탤릭) | 레퍼런스의 성격을 만드는 글꼴. 통째로 쓰면 읽기 힘들어 구역 제목 옆 한 마디에만 |

한글 글리프는 셋 다 없어 자연스럽게 Pretendard 로 넘어간다 — 지금까지와 같은 구조다.

### 변경 — 카드 반경 1.4rem → 1.2rem

레퍼런스의 12px. 1.4rem 은 카드가 조금 물렁해 보였다.

</details>

<details>
<summary><b>v2.67.1</b> — 2026-09-12 · <code>수정·변경</code> 보기 전환이 왼쪽 아래에 주저앉았다 · 즐겨찾기도 갈래 칩과 보기 전환을 한 줄에</summary>

### 수정 — 보기 전환이 왼쪽 아래에 주저앉았다

v2.67.0 에서 `.seg-view` 를 `display: inline-grid` 로 바꾸면서 `margin-left: auto` 가 죽었다. **인라인 박스에는 auto 마진이 먹지 않는다.**

도감 · 레이드 보스 · 알 부화는 토글이 flex 컨테이너 안에 있어(플렉스 아이템의 auto 마진은 별개 규칙) 티가 나지 않았고, 줄을 혼자 쓰는 **즐겨찾기에서만** 왼쪽으로 내려앉았다.

- `display: grid` + `width: fit-content` 로 되돌린다 — 블록으로 두고 폭만 내용에 맞추면 홀로 있을 때도 flex 안에서도 똑같이 오른쪽 끝에 붙는다

### 변경 — 즐겨찾기도 갈래 칩과 보기 전환을 한 줄에

즐겨찾기만 토글이 제 줄을 혼자 쓰고 있었다. 다른 목록 화면과 같은 문법으로 맞춘다 — 왼쪽 "무엇을 보나"(갈래 칩), 오른쪽 "어떻게 보나"(리스트 · 그리드).

- 좁은 화면에서 칩이 길어지면 `flex-wrap` 이 토글을 아래로 내린다 (도감 도구줄과 같은 규칙)

</details>

<details>
<summary><b>v2.67.0</b> — 2026-09-12 · <code>추가·변경</code> 상세 화면의 포켓몬이 움직인다 · 컨트롤을 한 줄로 · 리스트 · 그리드 전환을 작게 외 1건</summary>

### 추가 — 상세 화면의 포켓몬이 움직인다

같은 PokeAPI 저장소의 B/W 애니메이션 GIF 를 정지 png 와 같은 스프라이트 id 로 받아 쓴다.

**목록은 일부러 그대로 둔다.** GIF 는 평균 54KB 로 정지 png(평균 4KB)의 13배다. 도감 첫 화면은 한 번에 100장을 그리므로 목록까지 바꾸면 화면 하나가 400KB → 5.4MB 가 된다. 상세는 한 번에 한 마리라 그 값이 그대로 1장이다.

- 1,172종 중 **949종(81%)** 에 있다. 없는 종(6세대 이후·폼 변형 다수)은 지금까지처럼 정지 png 를 그린다
- 정지본을 먼저 띄우고 다 받은 뒤 갈아 끼운다 — GIF 를 바로 `src` 에 넣으면 받는 동안 상세 화면이 빈 칸으로 열린다
- 없는 종은 `<id>.none` 빈 파일로 표시해 다음 빌드가 404 를 다시 묻지 않는다 (CI 캐시가 폴더째 되살린다)
- `prefers-reduced-motion: reduce` 면 갈아 끼우지 않는다 — GIF 는 재생을 멈출 방법이 없다
- 서비스워커 캐시 대상에 `sprites-anim/` 을 넣었다(`pogoplan-v5`). 상세를 열 때 한 장씩 받으므로 쌓이는 양은 열어 본 포켓몬 수만큼이다
- `robots.txt` 에서는 뺐다 — 검색에 의미가 없는 51MB 를 크롤러가 받을 이유가 없다

### 변경 — 컨트롤을 한 줄로

세 화면 모두 컨트롤이 제 줄을 혼자 쓰고 있었다. 한 줄의 문법을 정했다 — **왼쪽은 "무엇을 보나", 오른쪽은 "어떻게/다른 걸 해 볼까"**.

- 배틀 · PvP: `[🧬 개체값 순위] [리틀|슈퍼|하이퍼|마스터] ……… [🃏 덱 짜기]`. 켜 둔 도구가 줄의 제목 노릇을 하고 리그 탭이 바로 옆에서 범위를 좁힌다. 랭킹 화면은 켜 둔 도구가 없어 리그가 맨 앞에 서고 도구 둘이 오른쪽으로 간다
- 레이드 보스 · 알 부화: 안내문 옆에 보기 전환. 폭을 가리지 않고 한 줄이고, 자리가 모자라면 `flex-wrap` 이 내린다

### 변경 — 리스트 · 그리드 전환을 작게

v2.40.0 에 목록 폭을 다 쓰게 한 이유는 "눈에 들어와야 한다" 였는데, 실제로는 안내 문구보다 토글이 커져 먼저 눈에 띄었다. **이 줄의 주인공은 목록이지 보기 방식이 아니다.**

- 오른쪽 끝에 붙는 작은 덩이로 줄였다 (`width: auto`)
- 좁은 화면(~699px)에서는 글자를 떼고 `☰` `⊞` 아이콘만 남긴다. 이름은 `aria-label` 로 남아 스크린리더는 그대로 읽는다
- 글자를 `span` 으로 감쌌다 — 맨 텍스트 노드는 CSS 가 감출 수 없다

### 변경 — 검색 엔진 메타

이미 있던 것(canonical · description · OG · sitemap · robots.txt)은 그대로 두고, 빠진 것만 채웠다.

- 실서비스 빌드에 `robots` 를 명시한다 — `max-image-preview:large` 가 있어야 검색 결과에 공유 카드 그림이 크게 뜨고, `max-snippet:-1` 이 있어야 발췌가 잘리지 않는다. dev 의 `noindex` 와 같은 자리에 넣어 한 빌드에 `robots` 줄이 정확히 하나만 있게 했다
- `twitter:title` · `twitter:description` · `twitter:image` · `twitter:image:alt` — og 로 폴백하는 수집기가 대부분이지만 카드 필드를 따로 읽는 곳도 있다
- 구조화 데이터를 `@graph` 로 바꾸고 `WebApplication` 을 더했다 (`applicationCategory` · `operatingSystem` · 무료 `offers`). `WebSite` 하나로는 "이게 도구다" 를 말하지 못한다
- `SearchAction` 은 넣지 않았다 — 해시 라우팅이라 `?q=` 로 결과를 여는 주소가 실제로 없다. 없는 기능을 적으면 구글이 확인하고 무시한다

</details>

<details>
<summary><b>v2.66.0</b> — 2026-09-12 · <code>변경</code> 도구마다 주소를 준다 · 리그를 고르는 컨트롤이 둘이었다 · 구분선을 필터 줄 아래로 외 4건</summary>

한 화면이 여러 기능을 겸하면 그 화면의 이름이 무엇을 뜻하는지 흐려진다. dev 기준으로 화면과 기능을 대조해 겹치는 곳 여덟 군데를 찾고(노션 "한 페이지 한 기능 감사") 일곱을 고쳤다. 나머지 하나(D-MAX 칩 라벨)는 **이미 되어 있었다** — 백로그를 잘못 적은 것이라 해당 행에 근거를 적고 취소했다.

### 변경 — 도구마다 주소를 준다

**도구를 켜도 주소가 그대로였다.** 뒤로가기로 돌아올 수 없고, 링크를 보내면 상대는 랭킹 화면만 봤다.

- 배틀 · PvP 하나가 랭킹 · 덱 짜기 · 개체값 순위 셋을 겸했다(`state.pvpTool`) → `#/pvp` · `#/pvp/deck` · `#/pvp/ivrank`
- 레이드 · PvE 는 티어표와 솔플 계산기를 겸했다(`state.pveTool`) → `#/pve` · `#/pve/solo`
- 주소가 상태를 정한다 — `ROUTES` 의 `tool` 값을 `applyPlanRoute()` 가 `state.*Tool` 에 옮기므로, 화면을 그리는 쪽은 지금까지처럼 `state` 만 읽는다
- `parent` 를 단 화면은 브레드크럼에 한 칸이 는다 (`🏠 › 배틀 · PvP › 개체값 순위`)
- 공유된 `#/ivrank` 는 `legacy` 로 잇는다. `#/pvp/ivrank` 로 바뀌며 열린다

**계산기를 켜면 아무 일도 안 하는 컨트롤이 남아 있었다** — 티어표가 통째로 사라지는데 `[일반|전체]` 세그먼트는 그대로였다. 계산기 화면에는 그 줄을 그리지 않는다.

### 변경 — 리그를 고르는 컨트롤이 둘이었다

PvP 개체값 순위 블록이 **자기 리그 탭을 따로** 가지고 있었다. 화면 맨 위에서 '슈퍼'를 골라도 아래 상위 10 은 '리틀'일 수 있었다.

- 리그는 `state.league` 하나가 정한다. 블록의 탭과 넓은 화면의 3열(v2.65.0)을 걷어냈다
- 지금 고른 리그의 카드에 표시를 달아 위 세그먼트와 눈이 이어지게 했다
- 마스터리그는 CP 상한이 없어 순위가 없다 — 그 자리에 이유 한 줄을 놓는다
- 도구 버튼 차례도 바꿨다: 개체값 순위는 리그에 딸린 정보라 왼쪽, 덱 짜기는 리그와 상관없는 다른 일이라 오른쪽 끝

### 변경 — 구분선을 필터 줄 아래로

v2.65.0 에서 제목 바로 아래 그었더니 **필터 줄이 본문 쪽으로 넘어가** 무엇을 거르는 줄인지 흐려졌다. 제목 · 부제 · 필터가 한 덩이(화면 머리)고, 선 아래가 그 결과다.

- 메인 셸은 필터가 늘 `#controls` 한 곳이라 그 상자에 바로 긋는다
- 전체 페이지는 필터 줄의 자리가 화면마다 달라 공통 클래스 `.page__filters` 하나로 표시한다 (도감 도구줄 · 일정 분류 칩 · 레이드/알 안내줄)
- 필터가 없는 화면만 지금까지처럼 머리 자체에 긋는다. **한 화면에 선은 하나** — 회귀가 이것을 센다
- 도감만 두던 예외 규칙(검색창이 머리 위로 올라와 선을 가린다)은 지웠다

### 변경 — 입구를 하나로

- **★ 즐겨찾기** — 도감의 `★ 즐겨찾기 N` 칩을 뺐다. 같은 줄의 세대 칩은 그 자리에서 목록을 거르는데 이 칩만 다른 화면으로 보냈다. 모양이 같으면 하는 일도 같아야 한다. 입구는 헤더 ★ 하나
- **🔍 검색** — 넓은 화면에서 버튼을 감춘다. 바로 왼쪽 검색바가 같은 패널을 여는데 둘이 나란히 떠 있었다. 요소는 남긴다(`search.js` 가 `aria-expanded` 를 이 id 에 적는다). 도감 칸은 `이 목록에서 찾기` 로 성격을 밝혔다

### 변경 — 내 포켓몬을 메뉴로, 탭 줄은 걷어낸다

v2.47.0 이 지적한 "문이 둘" 은 **같은 층에 둘**이던 것이 문제였지 화면이 둘인 것이 아니었다. `육성 현황`(지금 뭘 키우는 중인가)과 `내 포켓몬`(내 상자에 뭐가 있나)은 다른 질문이다.

- `내 포켓몬` 을 메뉴로 되돌리되 육성 플래너 아래 **한 칸 들여쓴 자식**으로 둔다 (`parent`)
- `ROUTE_NAV` 가 자식을 부모 바로 뒤로 정렬한다 — 표에 적은 순서가 아니라 소속 순서다
- 화면 안 탭 줄(`PLAN_TABS`)을 걷어냈다. 다른 화면은 모두 왼쪽 메뉴가 이동을 맡는데 플래너만 남아 있었다
- 덩이 이름을 `내 포켓몬` → `뭘 키울까` 로 바꿨다 — 덩이 제목과 그 안 항목이 똑같은 글자가 됐다

### 변경 — 일정표와 레이드 보스의 역할을 가른다

같은 질문("이번 주에 무슨 5성이 도나")에 **두 화면이 서로 다른 원본**으로 답하고 있었다. 달력은 손으로 적은 공지(`SCHEDULE_ITEMS`), 레이드 보스 화면은 자동 수집(`GAMEDAY`) — 수집이 하루 늦거나 공지가 갱신되지 않으면 두 화면이 다른 보스를 가리킨다.

- 달력은 `언제`, 레이드 보스 화면은 `지금 무엇이` 로 역할을 적고 서로 잇는 길을 달았다

### 변경 — 화면 이름·설명을 한 벌로

홈 타일이 제목과 설명을 **따로** 들고 있었다. 아이콘과 순서는 이미 표에서 가져오면서 글만 두 벌이라, 말투를 정리한 v2.60.0 에서 화면 머리만 바뀌고 홈 타일은 옛 문장 그대로 남았다.

- 홈 타일이 `ROUTES` · `routeDesc` 를 그대로 읽는다. 타일 번호도 순서에서 계산한다
- 부모가 있는 화면은 홈에 올리지 않는다 — 홈은 "어디로 갈까" 의 첫 갈림길이다

</details>

<details>
<summary><b>v2.65.0</b> — 2026-09-12 · <code>변경</code> 메뉴를 "하려는 일" 로 다시 묶었다 · 화면 머리와 본문 사이에 구분선 · 넓은 화면에서 PvP 개체값 순위를 좌우로 외 2건</summary>

### 변경 — 메뉴를 "하려는 일" 로 다시 묶었다

**주요 기능 · 부가 기능은 우리 기준이지 쓰는 사람 기준이 아니다.** 무엇이 주요인지는 그날 하려는 일에 따라 달라진다.

`ROUTE_GROUPS` 를 세 덩이로 다시 적었다 — `지금 뭐 하지`(이벤트 일정 · 레이드 보스 · 알 부화) · `뭘 데려갈까`(포켓몬 도감 · D-MAX · 레이드 · PvE · 배틀 · PvP) · `내 포켓몬`(육성 플래너 · 검색식 만들기).

- 순서도 바꿨다 — 접속해서 먼저 보는 것(오늘 뭐가 도는지)이 맨 위, 내 상자를 여는 일이 맨 아래
- 홈 타일도 같은 차례로 놓았다. 메뉴와 홈이 다른 순서면 둘 중 하나는 매번 다시 읽어야 한다
- `ROUTES` 의 `group` 값만 바꾸면 메뉴·홈이 같이 따라온다 (표 하나가 원본)

### 변경 — 화면 머리와 본문 사이에 구분선

**여백만으로는 위계가 서지 않았다.** 제목 · 부제와 본문 첫 줄이 다 같은 바탕 위 글자라, 아래 여백을 아무리 넓혀도 한 덩이로 읽혔다.

위 여백은 좁히고(1.2rem) 아래는 넓힌 뒤(4.4rem) 그 사이에 선을 긋는다 — 상단 바와 제목이 한 묶음이 되고, 머리와 본문이 갈린다.

- 선은 **배경**으로 그린다. `border-bottom` 은 패딩까지 포함한 상자 폭 전체에 깔려 왼쪽 사이드바(투명 · 고정) 자리를 가로지른다
- `background-origin: content-box` 라야 화면마다 다른 좌우 패딩(사이드바 210px · 상세 패널 폭)을 따로 적지 않아도 저절로 따라온다. `background-clip: padding-box` 는 선을 글 아래 1.6rem 까지 내려 그리기 위한 것
- 도감만 예외 — 검색창이 음수 마진으로 머리 영역까지 올라와 있어 머리 안쪽 선이 가려진다. 도감은 도구줄(`.dex__toolbar`) 위에 선을 긋고, 검색창이 선에 닿지 않게 위치를 다시 잡았다

### 변경 — 넓은 화면에서 PvP 개체값 순위를 좌우로

**탭은 좁은 화면의 문법이다.** 1920px 에서 리그 네 개를 탭으로 갈아 가며 보는 것은 화면을 놀리는 일이다.

- 입력(종 · 개체값)을 왼쪽 34rem 에 고정(`position: sticky`)하고 결과를 오른쪽에 편다 — 값을 고치며 결과를 같이 본다
- 리그별 상위 10 은 세 리그를 나란히 그린다. 탭은 CSS 로 좁은 화면에서만 남긴다 (JS 에 폭 분기를 만들지 않는다)

### 변경 — 포켓몬 도감 검색창을 제목 옆으로

제목 아래 한 줄을 통째로 쓰고 있어, 정작 목록이 첫 화면에서 그만큼 밀려 있었다. `page-head` 는 화면마다 같은 조각이라 그 안에 넣을 수 없어, 음수 마진으로 같은 높이에 올린다.

### 변경 — 육성 플래너의 걸음 카드

다섯 중 넷이 같은 곳(`#/planner-collection`)으로 가고 있었다. 세 걸음으로 줄이고, 상자 정리 · PvP 개체값 순위로 이어지는 길을 더했다.

</details>

<details>
<summary><b>v2.64.0</b> — 2026-09-12 · <code>변경</code> ★ 즐겨찾기를 헤더 버튼 + 팝업으로 · ☰ 메뉴를 주요 기능 · 부가 기능 두 덩이로 · 배틀 · PvP 를 읽는 순서대로 외 1건</summary>

### 변경 — ★ 즐겨찾기를 헤더 버튼 + 팝업으로

**접어 둬도 한 뼘을 먹었다.** 배틀 · PvP 처럼 컨트롤이 많은 화면에서는 리그 세그먼트가 첫 화면 밖으로 밀렸다.

즐겨찾기는 **어느 화면에서나** 보고 싶은 것이라 화면 안 자리가 아니라 헤더에 두는 것이 맞다 — 검색(🔍)과 같은 문법이다.

- 버튼은 **로그인 + 즐겨찾기가 하나라도 있을 때만** 보인다. 빈 팝업을 여는 버튼은 두지 않는다
- 팝업에서 포켓몬을 누르면 팝업을 먼저 닫고 상세를 연다 — 팝업 위에 팝업이 쌓이면 뒤로가기가 두 번 필요해진다
- `#fav-digest` 아코디언과 펼침 기억(`pogo_fav_acc`)을 지웠다. 팝업은 열 때마다 처음부터 보여 준다

### 변경 — ☰ 메뉴를 주요 기능 · 부가 기능 두 덩이로

열 줄이 한 덩이로 붙어 있어 늘 쓰는 것을 찾으려면 매번 처음부터 훑어야 했다.

| 덩이 | 화면 |
| --- | --- |
| 주요 기능 | 육성 플래너 · 포켓몬 도감 · D-MAX · 레이드 · PvE · 배틀 · PvP |
| 부가 기능 | 이벤트 일정 · 레이드 보스 · 알 부화 · 검색식 만들기 (+ UI 목록) |

**서비스 홈은 어느 덩이도 아니다** — 목록의 출발점이라 맨 위에 혼자 둔다.

나눔은 `router.js ROUTES` 의 `group: 'main'` 하나로 정한다. 목록을 두 벌로 만들지 않고 한 `<nav>` 안에서 `<h3>` 로 가르므로 랜드마크는 지금처럼 하나다.

### 변경 — 배틀 · PvP 를 읽는 순서대로

이 화면에 온 사람은 **① 리그를 고르고 → ② 타입으로 좁히고 → ③ 덱을 짜거나 내 개체 순위를 본다.**

전에는 도구 버튼(`🃏 덱 짜기` · `🧬 개체값 순위`)이 리그 줄에 얹혀 있어, 리그를 고르기도 전에 "덱 짜기" 가 먼저 눈에 띄었다. 좁은 화면에서는 그 줄이 두 줄로 접혀 타입 필터를 아래로 밀어냈다.

이제 `리그 → 타입 → 도구` 순서다. 도구를 펼친 동안에는 타입 필터가 필요 없으므로 도구 줄만 리그 아래에 남는다.

### 변경 — 화면마다 제목과 부제

좁은 화면에서는 부제가 CSS 로 감춰져 있었다. 제목만 있으면 "여기가 어디인지" 는 알아도 **"여기서 무엇을 하는지"** 는 모른다. 한 줄이라 목록이 밀리는 양도 한 줄뿐이다.

부제가 없던 다섯 화면(검색식 만들기 · 패치노트 · 기술 변경 · 개인정보처리방침 · 이용약관)에 한 줄씩 달았다. 메뉴에 오르는 화면 14곳 전부 제목과 부제가 뜨는지 회귀로 붙잡았다.

`#/mon` 은 적지 않는다 — `kind: 'detail'` 이라 화면 머리가 아니라 팝업 안에 이름이 뜬다.

</details>

<details>
<summary><b>v2.63.0</b> — 2026-09-12 · <code>변경</code> 🧭 타입 & 상성 화면을 접었다 · 🧬 PvP 개체값 순위를 배틀 · PvP 안으로 · 정리 — 홈 타일 11개 → 9개</summary>

### 변경 — 🧭 타입 & 상성 화면을 접었다

**같은 표가 두 곳에 있었다.** 포켓몬 상세 팝업의 약점·내성 표가 이 화면이 보여 주던 것과 같은 계산(`typeMultAgainst`)을 쓴다. 화면을 따로 둘 만큼의 차이가 없었다.

- 라우트 · ☰ 메뉴 · 홈 타일에서 뺐다. `components/typesearch.js` 를 지웠다(178줄)
- **공유된 옛 주소는 죽지 않는다** — `#/types?t=water,dark` 는 `legacy` 로 도감에 잇는다
- 상세 팝업의 `🧭 상성 검색에서 딜러까지 보기 ▸` 버튼을 뗐다 — 바로 위 표가 그 화면이라 더 갈 곳이 없다
- 헤더 검색의 `… 타입 N마리 전부 보기 · 상성 검색 ▸` 줄은 "이름을 더 넣어 좁혀 보세요" 로 바꿨다

`typeChipEl` · `typeMultAgainst` 는 `components/detail.js` 에 있어 영향이 없다 — 지운 파일이 쓰던 쪽이지 정의한 쪽이 아니었다.

### 변경 — 🧬 PvP 개체값 순위를 배틀 · PvP 안으로

메뉴에 따로 둘 화면이 아니었다. **리그 순위를 보다가 "그럼 내 개체는 몇 위지" 가 떠오르는 자리**라, 화면을 옮기지 않고 그 자리에서 펼치는 것이 맞다.

`[🃏 덱 짜기]` 옆에 `[🧬 개체값 순위]` 를 붙였다 — 덱 짜기와 같은 문법(`state.pvpTool`)으로 누르면 펼쳐지고 다시 누르면 접힌다.

`#/ivrank` 주소는 남는다: 라우트 표에서 `nav` 만 빼면 메뉴에는 안 뜨고 주소는 산다. 북마크와 공유 링크가 그대로 열린다.

### 정리 — 홈 타일 11개 → 9개

두 화면이 빠지며 번호를 다시 매겼다. ☰ 메뉴도 같은 표(`router.js ROUTES`)를 보므로 함께 줄었다.

</details>

<details>
<summary><b>v2.62.0</b> — 2026-09-11 · <code>수정·변경</code> PvP 개체값 순위에 리그 탭 · 고른 포켓몬 카드가 한 줄에 뭉개지던 것</summary>

### 변경 — PvP 개체값 순위에 리그 탭

**"가장 쓸 만한" 한 리그의 상위 10 만 보여 줬다.** 그 종을 다른 리그에서 쓰려면 어떤 개체값을 노려야 하는지는 볼 길이 없었다.

리틀 · 슈퍼 · 하이퍼를 탭(`seg`)으로 고른다. **마스터는 뺀다** — CP 상한이 없어 15/15/15 가 늘 1위라 줄 세울 것이 없다. 처음 열었을 때 켜지는 탭은 지금처럼 "가장 쓸 만한" 리그이고, 한 번 고른 뒤에는 사람이 고른 것을 따른다(`pogo_ivrank.league`).

들어갈 수 없는 리그(Lv1 CP 가 이미 상한을 넘는 전설 등)를 고르면 표 대신 그 사실을 적는다.

### 수정 — 고른 포켓몬 카드가 한 줄에 뭉개지던 것

`레지스틸공격 143 · 방어 285 · 체력 190` 처럼 이름과 종족값이 붙어 나왔다. 플래너의 `.plan__picked` 를 빌려 썼는데 **그 클래스에는 스타일이 아예 없었다** — 플래너 쪽에서도 다른 규칙에 얹혀 보이고 있던 이름이다.

`.ivrank__picked` 를 따로 만든다: 그림 | 이름·종족값(세로) | ✕ 한 줄. ✕ 는 `--tap`(44px) 크기를 지킨다.

화면 위쪽 간격도 정리했다 — 안내 문단이 제목에서 멀리 떨어져 "이 화면이 무엇인가" 가 한 덩어리로 안 읽혔다.

</details>

<details>
<summary><b>v2.61.0</b> — 2026-09-11 · <code>추가·변경</code> 🧬 PvP 개체값 순위 (실험 기능) · 랭킹 탭 줄과 바로가기 제거 · 타입 & 상성을 열면 노말이 기본</summary>

### 추가 — 🧬 PvP 개체값 순위 (실험 기능)

**PvE 와 PvP 는 "좋은 개체" 의 기준이 정반대인데, 서비스는 PvE 기준만 보여 주고 있었다.**

- **PvE** — 공격 개체값이 높을수록 좋다. 15/15/15 가 항상 1위라 볼 것이 없다
- **PvP** — 리그마다 CP 상한이 있어 공격이 **낮을수록** 같은 CP 안에서 레벨을 더 올릴 수 있다. 그래서 0/15/15 같은 조합이 1위가 되는 일이 흔하다

지금까지는 만렙 CP 와 리그 도달 레벨까지만 보여 주고 "이 개체가 그 리그에서 몇 위냐" 는 답하지 않았다. 그 한 줄이 없어서 사람들이 바깥 사이트로 나가 다시 찾아봤다.

#### 순위를 매기는 법 (스탯 곱)

CP 상한 안에서 가장 높은 레벨까지 올렸을 때의 공격 × 방어 × 체력이 클수록 좋은 개체다.

```
공격  = (종족값.atk + IV.atk) × cpm
방어  = (종족값.def + IV.def) × cpm
체력  = floor((종족값.hp + IV.hp) × cpm)      ← 체력에만 내림
```

**체력에만 `floor` 를 쓰는 것이 핵심**이다 — 게임이 체력을 정수로 끊기 때문에 순위가 계단처럼 갈라진다. 이걸 빼면 순위가 미세하게 어긋난다.

#### 새 데이터 원본이 필요 없다

종족값은 `data/dex.json` 의 `forms`, 레벨별 배율은 같은 파일의 `cpms` 51개. 둘 다 화면이 이미 쓰던 값이라 지어낸 수치가 하나도 없다. `planner/collection.js` 의 `planLeagueReach()` 가 리그 도달 레벨을 이미 내고 있어, 거기에 순위만 붙였다.

#### 미리 계산하지 않고 그자리에서 센다

미리 계산해 번들에 넣으면 약 1,000종 × 4,096조합 × 4리그 = **1,600만 개** 값이 된다. 한 종·한 리그는 4,096회 계산이라 브라우저에서 **8ms** 에 끝난다 — 첫 화면을 늦출 이유가 없다.

레벨 찾기는 선형이 아니라 반씩 좁힌다(CP 는 레벨에 단조 증가). 선형 탐색과 **61,440건 대조해 불일치 0** 을 확인했고, 그 대조를 회귀에도 넣었다.

#### 상세에 붙는 PvP 블록

PvP 순위(상위 40)에 오른 종을 열면 **"🧬 PvP 라면 이 개체값"** 이 함께 나온다. 위의 `CP 100% 기준` 은 레이드 기준이라 PvP 에서는 쓸 데가 없었다. 순위 밖 종에는 붙이지 않는다 — 모든 종에 달면 "이 화면에 왜 이게 있지" 가 된다.

#### 짚어 둔 것

- **마스터리그는 순위를 매기지 않는다** — CP 상한이 없어 15/15/15 가 늘 1위다
- **획득 경로마다 개체값 하한이 다르다** — 알 · 레이드 · 리서치는 10 이상, 섀도우(교환 전)는 6 이상
- **베스트 버디(+1레벨)는 뺀다** — 모두가 가질 수 있는 조건이 아니다

### 변경 — 랭킹 탭 줄과 바로가기 제거

**같은 곳으로 가는 길이 화면에 두 벌이었다.**

v2.22.0 에 이 줄을 되살린 이유는 "D-MAX ↔ PvE ↔ PvP 를 오가려면 매번 메뉴를 열어야 한다" 였는데, v2.42.0 PC 셸에서 **왼쪽 메뉴가 늘 펼쳐져** 있게 되면서 그 이유가 사라졌다. 도감 · 상성 · 즐겨찾기 바로가기(📕 🧭 ★)도 메뉴에 같은 줄이 있다.

좁은 화면도 같다 — 화면마다 주소가 따로라(`#/dmax` · `#/pve` · `#/rank/pvp`) 뒤로가기로 오간다.

플래너의 `[육성 현황 | 내 포켓몬]` 은 남긴다: 그것은 메뉴 중복이 아니라 **한 화면 안의 갈래**다.

### 변경 — 타입 & 상성을 열면 노말이 기본

아무것도 고르지 않은 첫 화면은 칩 줄만 덩그러니 남아 휑했다. 노말을 켜 두면 이 화면이 무엇을 보여 주는 곳인지 한눈에 읽힌다 — 노말은 약점이 격투 하나뿐이라 표가 가장 짧아 본보기로 알맞다. 주소로 들어온 선택(`?t=`)이나 포켓몬(`&mon=`)이 있으면 그쪽이 먼저다.

</details>

<details>
<summary><b>v2.60.1</b> — 2026-09-11 · <code>수정</code> 화면을 옮겨도 상세 패널이 따라오던 것</summary>

### 수정 — 화면을 옮겨도 상세 패널이 따라오던 것

**넓은 화면에서 도감 포켓몬을 연 뒤 [서비스 홈] 으로 가면, 홈 오른쪽에 이전 포켓몬 카드가 그대로 붙어 본문이 눌렸다.**

화면을 옮길 때 패널을 닫는 리스너(`components/modal.js` 의 `hashchange`)가 **홈을 예외로 두고 있었다.**

```js
const onShell = routeIdOf() === 'home' || routeIdOf() === 'mon';
if (!onShell) closeDetailPanel();
```

`mon` 을 남긴 것은 맞다 — `#/mon/<id>` 는 상세를 가리키는 주소라 패널이 곧 그 화면이다(딥링크로 바로 들어왔거나 뒤로가기로 돌아온 경우). 그런데 `home` 까지 함께 넣어 두어 홈으로 가는 **모든 이동**이 패널을 살려 두었다.

남겨야 하는 것은 "홈" 이 아니라 **상세를 가리키는 주소 하나**다:

```js
if (!/^#\/mon\//.test(location.hash)) closeDetailPanel();
```

목록에서 다른 포켓몬을 누를 때는 `replaceState` 라 `hashchange` 가 나지 않아 이 리스너를 타지 않는다 — 같은 화면 안에서 내용만 바뀌는 동작은 그대로다.

`tests/e2e/detail-panel.js` 에 네 줄 추가: 패널이 열린다 · 홈으로 가면 닫힌다 · 다른 화면으로 가도 닫힌다 · 딥링크로 들어오면 열린다.

</details>

<details>
<summary><b>v2.60.0</b> — 2026-09-11 · <code>수정·변경</code> 화면 말투를 친근체 하나로 · 잠긴 화면 문구를 짧게 · 화면 이름 뒤 조사가 늘 "은" 이던 것</summary>

### 변경 — 화면 말투를 친근체 하나로

**같은 화면 안에서 "~합니다" 와 "~해요" 가 섞여 있었다.** 격식 306곳 · 친근 281곳으로 거의 반반이라, 어느 쪽도 이 서비스의 목소리가 아니었다.

- 안내 문구 **120여 곳**을 친근체로 통일했다. 어미 대응표를 만들어 **문자열 리터럴 안에서만** 바꿨다 — 주석과 코드는 건드리지 않는다
- `습니다` 글자가 안 들어가는 ㅂ불규칙(`씁니다` · `봅니다` · `열립니다` · `나옵니다` …)은 정규식에 안 걸려 2차로 따로 훑었다
- 공문서 낱말도 함께 뺐다: `해당` → `그` · `즉시` → `바로` · `확인하세요` → `봐요`
- 두괄식으로 다시 쓴 문장들 — 결론을 앞에 놓는다
  - `이 빌드는 로그인 기능이 꺼져 있어 저장이 안 됩니다. 계산·조회는 할 수 있어요.` → `계산·조회는 할 수 있어요. 이 빌드는 로그인 기능이 꺼져 있어 저장만 안 돼요.`
  - `⏳ 승인 대기 중 — 승인되면 개체를 저장할 수 있어요. 계산은 지금도 됩니다.` → `⏳ 승인 대기 중 — 계산은 지금도 돼요. 승인되면 개체를 계정에 저장할 수 있어요.`
- 겹치는 말을 걷어냈다 — `복사하면 … 복사돼요` · `계산돼 있어요 … 붙어 있어요`

**건드리지 않은 것** — 이용약관 · 개인정보처리방침 · 저작권/데이터 출처 고지는 격식체 그대로다. 법적 고지의 말투를 낮추면 구속력이 있는 문장으로 안 읽힌다. 패치노트(`RELEASE_NOTES`)도 지난 기록이라 그대로 둔다.

**영어판은 손실 0** — 한국어 원문이 곧 사전 키(`i18n-en.js`)라 원문을 바꾸면 키가 깨져 영어판이 한글로 샌다. 바꾼 (옛 → 새) 쌍을 그대로 키에 적용했고, 키 406개 · 영어 값 집합이 전후 동일함을 대조해 확인했다.

### 변경 — 잠긴 화면 문구를 짧게

**막힌 사람이 그 자리에서 할 수 있는 일은 로그인 하나뿐인데, 읽을 것이 세 줄이면 버튼까지 못 간다.**

전에는 화면마다 "왜 로그인이 필요한가" 를 한 문장으로 적고(`LOCK_WHY`) 거기에 "도감 · 타입 & 상성 · D-MAX 는 로그인 없이도 그대로 쓸 수 있어요" 까지 붙였다. 어느 화면인지는 **바로 위 화면 제목이 이미 말하고 있다**.

| 자리 | 전 | 후 |
| --- | --- | --- |
| 제목 | `로그인하면 열립니다` | `로그인하면 열려요` |
| 본문 | `레이드 · PvE은 추천 딜러를 내 보유 개체와 견줘 보여 주는 화면이라 로그인이 필요합니다. 도감 · 타입 & 상성 · D-MAX 는 로그인 없이도 그대로 쓸 수 있어요.` | `승인된 분만 쓸 수 있어서, 처음이라면 관리자 승인을 기다리게 돼요.` |
| 승인 대기 | `승인 대기 중이에요` / `관리자가 승인하면 레이드 · PvE을 쓸 수 있어요.` | `승인을 기다리는 중이에요` / `관리자가 승인하면 바로 열려요.` |
| 꼬리말 | `승인된 친구만 사용할 수 있어요. 첫 로그인 때 … 동의를 받습니다.` | `첫 로그인 때 … 동의를 받아요.` |

- 줄마다 다른 것을 말하게 나눴다 — 제목(결론) · 본문(승인제라는 조건) · 꼬리말(동의)
- `LOCK_WHY`(`components/pages.js`)와 `lockedCardNode` 의 `why` 인자를 지웠다 — 더 이상 화면에 나오지 않아 남겨 두면 죽은 코드가 된다
- **승인제 안내는 문구를 줄여도 남긴다** — 로그인하면 곧바로 열리는 게 아니라 `allowlist` 에 올라야 열린다(`components/auth.js`). "로그인만 하면 전부 보인다" 고 적으면 사실이 아니고, 로그인 뒤에 "왜 아직도 안 되지" 를 겪는다. 안내 팝업의 3단계(로그인 → 승인 대기 → 열림)도 그대로다

### 수정 — 화면 이름 뒤 조사가 늘 "은" 이던 것

`레이드 · PvE`**은** 처럼 붙었다. `koParticle`(`components/name.js`)은 한글 받침만 읽어서, 라틴 문자로 끝나면 받침이 있다고 보고 늘 `은`·`을` 을 골랐다. `PvE` 는 "피브이이" 라 `는` 이 맞다.

라틴 독음 받침표를 새로 들이는 대신, 이름 뒤에 조사가 필요 없는 문장으로 적는다(`레이드 · PvE 화면은 승인된 분만 볼 수 있어요.`). 표를 들이면 `PvP` · `D-MAX` · `CP` 마다 읽는 법을 정해 둬야 하고, 새 이름이 생길 때마다 늘어난다.

</details>

<details>
<summary><b>v2.59.0</b> — 2026-09-11 · <code>변경</code> 목록을 한 줄에 여러 개로 (격자 사다리) · dev 빌드의 탭 제목에 `[dev]`</summary>

### 변경 — 목록을 한 줄에 여러 개로 (격자 사다리)

**넓은 화면에서 한 줄에 하나씩만 놓이던 목록을 1920 기준 다섯 개로 흐르게 하고, 중단점마다 넷 · 셋 · 둘 · 하나로 줄인다.**

1920 에서 알 부화를 [리스트] 로 보면 한 줄에 한 마리였다. 오른쪽 절반이 비고, 27종을 보려면 스크롤을 스물일곱 번 내려야 했다. 줄 하나가 화면 폭을 다 쓸 이유가 없다 — 한 줄에 담긴 정보는 그림 · 이름 · CP 뿐이다.

- **열 개수를 한 곳에서 정한다** (`styles/tokens.css` `--list-cols`) — 1760↑ 다섯 · 1440↑ 넷 · 1100↑ 셋 · 700↑ 둘 · 그 아래 하나. 전에는 `pages.css` · `list.css` · `pc-theme.css` 세 곳이 각자 `auto-fill minmax(26rem)` 로 계산해서, 폭이 같아도 화면마다 열 개수가 달랐다
- 같은 값을 보는 화면: 포켓몬 도감 · 레이드 보스 · 알 부화 · 즐겨찾기(`.dex__list`), D-MAX · 레이드 PvE · 배틀 PvP(`.row-list`)
- **상세 패널이 열리면 두 칸 내린다** — 패널이 본문에서 `--panel-w` 만큼을 가져가므로, 같은 열 개수를 유지하면 카드가 눌린다. 1440 에서 넷 → 둘, 1280 에서 셋 → 하나
- **[리스트] 로 본 도감 줄은 두 줄 짜임으로** — `#0001 이상해씨 / CP 100% 1,260 ●●`. 줄 조각들이 "한 줄이 화면 폭을 다 쓴다" 는 전제로 짜여 있어(타입 칸 `min-width: 12.4rem` · 세대와 CP 두 칸 · 타입 이름 알약) 한 칸(1920 기준 298px)에 들어가지 못했고, 이름이 한 글자씩 세로로 쪼개졌다. 알 부화 줄(`.gameday__row`)과 같은 모양으로 맞춘다. 세대는 뺀다 — 목록 바로 위 세대 칩이 이미 그 값을 말한다
- 좁은 화면(<1100px)의 D-MAX · 레이드 PvE · 배틀 PvP 는 v2.53.0 결정대로 한 줄에 하나인 목록 그대로다

### 변경 — dev 빌드의 탭 제목에 `[dev]`

실서비스 탭과 미리보기 탭을 나란히 띄우면 제목이 똑같아 구분되지 않았다. 화면 안 버전 배지(`v2.59.0-dev`)는 탭 목록에서 보이지 않는다.

- 빌드가 정적 `<title>` 앞에 붙이고(`backend/build.py`), 화면을 옮길 때마다 `components/app-shell.js` 가 다시 붙인다
- 채널을 따로 주입하지 않고 `BUILD_VERSION` 끝의 `-dev` 로 판별한다 — 주입 경로를 하나 더 늘리지 않기 위해서다

</details>

<details>
<summary><b>v2.58.0</b> — 2026-09-11 · <code>추가</code> 🔎 검색식 만들기 (`#/finder`, 백로그 QA-57) · 넣지 않은 것이 이 화면의 경계다 · 잠금·문 외 2건</summary>

### 추가 — 🔎 검색식 만들기 (`#/finder`, 백로그 QA-57)

**조건을 눌러 고르면 게임 검색창에 그대로 붙여 넣을 검색식 한 줄이 만들어진다.**

박스가 수천 마리가 되면 "뭘 정리할지" 를 눈으로 고를 수 없다. 게임의 검색식이 그 일을 하는데, 문법이 낯설고(`4*` · `!` · `&` · `,`) 외워 쓰기 어려워 대부분 쓰지 않는다. 여기서는 **고르면 식이 되고**, 식을 보면서 문법을 배우게 된다.

- **한 칸이 세 상태를 돈다** — ＋포함 → －제외 → 해제. 버튼을 둘로 나누면(포함 줄 + 제외 줄) 같은 조건이 화면에 두 번 나와 어느 쪽이 켜졌는지 헷갈린다. 색으로도 가른다(포함은 브랜드색, 제외는 주의색 + 취소선) — ＋ 와 － 는 작아서 빠르게 훑을 때 안 읽힌다
- **타입은 `,` 로 잇는다** — `&` 로 이으면 "둘 다인 것" 이 되어 거의 안 걸린다. 게임은 쉼표를 "또는" 으로 읽는다
- **결과 칸이 맨 위에 붙어 있다**(sticky) — 조건을 고르는 내내 결과가 눈에 있어야 무엇이 달라지는지 보인다
- **자주 쓰는 묶음** 셋(🧹 박스 정리 · 🌱 육성 후보 · 🤝 교환용) — 처음 여는 사람이 "이런 걸 할 수 있구나" 를 한눈에 보고, 거기서 빼고 더한다
- 고른 것은 `pogo_finder` 에 남는다 — 검색식은 만들어 놓고 게임과 오가며 여러 번 쓴다

### 넣지 않은 것이 이 화면의 경계다

| 뺀 것 | 이유 |
| --- | --- |
| 게임 버전마다 갈리는 문법 (`countcandy` · `countcandyxl` · `count5-`) | 틀린 식을 주면 사용자가 게임에서 **빈 결과**를 보고 "이 서비스가 틀렸다" 고 알게 된다. 검증할 수단이 없으면 내보내지 않는다 |
| 교환 상대 닉네임 · 리모트 레이드 전용 | 게임이 아예 지원하지 않는다. 없는 것을 만들어 주는 것보다 **없다고 말하는** 편이 낫다 — 화면 아래에 적어 뒀다 |

### 잠금·문

- 다른 잠긴 화면과 같은 규칙으로 **로그인해야 열린다** — 만든 검색식은 계정에 이어서 쓰는 개인 설정이다. 잠긴 메뉴 줄이 여섯에서 일곱이 됐다
- **☰ 메뉴와 홈 타일 양쪽에** 올렸다 — 같은 화면인데 문이 한쪽에만 있으면 메뉴를 안 여는 사람은 그 화면이 있는 줄도 모른다. 홈 타일이 아홉에서 열이 됐다

### 회귀

`tests/e2e/finder.js` 16항목 — 세 상태 순환, `&`·`!`·`,` 문법, CP 범위, 새로고침 뒤 유지, 묶음이 포함·제외를 함께 넣는가, **버전 타는 문법을 노출하지 않는가**, 못 하는 것을 못 한다고 적는가, 로그인 안 하면 잠기는가.

### 백로그 판정 (이번에 함께 검토)

| 항목 | 판정 | 근거 |
| --- | --- | --- |
| QA-57 검색식 생성기 | **구현** | 데이터가 필요 없다 — 조건 선택 → 문자열 |
| QA-56 목표 자원 계산기 | **구현 불가** | 레벨업 사탕·별가루 비용표가 데이터에 없다. 만들려면 수치를 지어내야 하는데 그건 이 저장소의 금지 사항이다 |
| QA-55 육성 판단 카드 | 일부 이미 있음 | 활용처는 이미 상세 팝업에 있다. 남은 것은 "그래서 키울 가치가 있나" 라는 판단 부분 |
| QA-58 보유 개체 파티 | 선행 필요 | 저장 개체·기술 데이터 품질 고정이 먼저다 |
| QA-59 목표 × 일정 | 선행 필요 | `GAMEDAY.events` 52건은 있으나 목표 데이터 모델이 없다 |
| QA-74 Firebase 규칙 게시 | **사람 액션** | Firebase 콘솔에서 사람이 해야 한다 |
| QA-63 제품 계획 문서 | 확인 완료 | `labs/` · `docs/product_plan.md` 가 저장소에 **없다** — 백로그가 정한 대로 "원본 없음" 으로 기록 |

</details>

<details>
<summary><b>v2.57.0</b> — 2026-09-11 · 먼저 쟀다 · 변경 · 안전선 외 3건</summary>

화면에 보이는 변화는 없다. **나가는 것만 가볍게 했다.** 그래서 패치노트(`RELEASE_VER`)는 올리지 않는다 — 올려 봐야 새 소식 빨간 점만 뜨고 볼 것이 없다.

### 먼저 쟀다

"서비스가 커져 검증이 오래 걸린다" 와 "리소스가 방대하다" 는 지적을 짐작으로 고치지 않고 재어 봤다. **둘은 같은 뿌리였다** — 화면을 열 때마다 읽는 양이 크면, 사용자도 느리고 테스트도 느리다 (회귀의 남은 병목은 `page.goto` 였다).

| 항목 | 크기 | 그중 낭비 |
| --- | --- | --- |
| 인라인 JS | 434.5 KB | **주석 161.3 KB (37%)** |
| 인라인 CSS | 307.6 KB | **주석 65.4 KB (21%)** |
| `data.js` | 1,480 KB | **공백 13.1%** |

이 저장소의 주석은 "왜 이렇게 했는가" 를 길게 적는다 — 그게 이 코드의 값어치다. 다만 그건 **읽는 사람**에게 값어치가 있는 것이고, 브라우저는 매번 그만큼을 더 받아 더 읽는다.

### 변경

- **배포본에서 주석을 걷어낸다** (`backend/build.py` `strip_js_comments` · `strip_css_comments`) — **원본은 그대로 두고 나가는 것만** 걷어낸다. 주석을 지우는 게 아니라, 주석을 배포하지 않는 것이다
- **`data.js` 의 공백을 줄인다** (`JSON_TIGHT` · `optional_json`) — `json.dumps` 기본 구분자가 항목마다 공백 두 개를 붙이는데, 1,172종 × 필드 수만큼 쌓인다. 앞 단계가 만든 json 파일도 되감아 줄인다. **값은 한 글자도 바뀌지 않는다** — 사이의 공백만 없앤다

### 안전선

조금 덜 걷어내더라도 절대 깨뜨리지 않는 쪽을 골랐다.

| 규칙 | 이유 |
| --- | --- |
| JS 는 **줄 전체가 주석인 줄만** | 코드 뒤에 붙은 `// …` 는 문자열 안(`"https://…"`)일 수 있다. 실측 5.6KB 라 아낄 것도 별로 없다 |
| 여러 줄 백틱 안은 건너뛴다 | 그 안의 `//` 로 시작하는 줄은 주석이 아니라 **글자**다 |
| CSS 는 문자열을 피해 `/* */` 만 | `content: "/*"` 같은 것을 깨지 않는다 |
| `@license` · `@preserve` 는 남긴다 | 지우면 안 되는 주석이 있다 |
| 파일 머리말(`── 파일명 ──`)은 남긴다 | 걷어낸 **뒤에** 붙인다 — 합쳐진 배포본에서도 어느 파일에서 왔는지 알 수 있어야 한다 |
| 깨진 json 은 되감지 않고 원문 그대로 | 여기서 배포를 막을 이유가 없다 |

### 결과

| | 전 | 후 | 줄어듦 |
| --- | --- | --- | --- |
| `index.html` | 994 KB | 627 KB | −36% |
| `data.js` | 1,480 KB | 1,319 KB | −10% |
| **첫 방문 합계 (원본)** | 2,474 KB | **1,946 KB** | **−21%** |
| **첫 방문 합계 (gzip)** | 497 KB | **362 KB** | **−27%** |

gzip 보다 **원본**이 더 중요하다 — 브라우저가 풀어서 읽어야 하는 양이 그쪽이고, 회귀가 화면마다 다시 읽는 것도 그쪽이다.

### 증거

**22개 화면 × 2폭의 계산된 스타일 58,512줄을 전후 대조해 차이 0건.** 옛 빌드를 따로 띄워 두고 같은 지문을 떠서 견줬다. 주석을 걷어내도 화면은 한 픽셀도 바뀌지 않는다.

### 검증 시간은 여기서 안 줄었다 — 다시 쟀다

**틀린 예상이었다.** 배포본을 21% 줄이면 회귀도 빨라질 줄 알았는데 303초 그대로였다. 그래서 부팅을 프로파일했더니 **97%가 idle** — 일하는 게 아니라 **기다리고** 있었다.

| | 값 |
| --- | --- |
| HTML 응답 | 7 ms |
| `data.js` 내려받기 | 9 ms |
| `domInteractive` | **12,822 ms** |

원인 둘을 찾았다.

**(1) 바깥 요청을 안 끊은 스위트** — 이 방(샌드박스)에는 바깥이 없어 웹폰트·Firebase 요청이 타임아웃까지 매달리고, 그동안 첫 렌더가 끝나지 않는다. 한 번 여는 데 **13,700ms → 1,190ms (11.5배)**. `nav` · `bot-filter` · `legal` 과 `breakpoints` 의 최근 컨텍스트 둘에 그 차단이 빠져 있었다 (나머지 열넷은 이미 하고 있었다).

**(2) 오타 난 선택자가 30초씩 먹고 있었다** — `nav.js` 의 `settle()` 이 `#consent .consent-deny` 를 눌렀는데, 실제 클래스는 `.consent__deny` 다(BEM). 맞는 게 없으니 `click()` 이 **기본 30초** 를 꽉 기다렸고 `.catch(() => {})` 가 그걸 조용히 삼켰다. `settle()` 을 여덟 번 부르므로 **이 스위트 혼자 240초** 를 거기에 썼다 (실측 253초 중). 열여섯 스위트 중 오타는 `nav.js` 하나뿐이었다.

**없어도 되는 클릭에는 1.5초 한도를 뒀다** (22곳). 선택자가 또 어긋나도 30초가 아니라 1.5초만 잃고, 무엇보다 **그 손해가 눈에 띈다** — 조용히 삼켜지는 30초는 아무도 못 본다.

이건 **테스트 환경 문제**다. 실사용자는 인터넷이 되므로 폰트가 바로 오고 13초를 기다리지 않는다. 사용자에게 돌아가는 이득은 위의 리소스 최적화(gzip −27%) 쪽이다.

</details>

<details>
<summary><b>v2.56.0</b> — 2026-09-11 · 수정 · 회귀 · 확인</summary>

### 수정

- **공유 링크로 연 상세 팝업이 ✕ 를 눌러도 닫히지 않았다** (`components/modal.js` `openModal`)

  공유 링크로 **바로** 들어오면 히스토리에 항목이 하나뿐이고 그게 `#/mon/…` 이다. 거기서 팝업이 `pushOverlayEntry` 로 같은 주소를 하나 더 쌓으면 **위아래가 둘 다** `#/mon/…` 이 된다. ✕ 를 누르면 `closeModal` 의 `history.back()` 이 아래 항목으로 내려가고, 라우터가 그 해시를 읽어 팝업을 곧바로 다시 연다 — 사용자 눈에는 "✕ 를 눌러도 안 닫힌다" 로 보인다.

  **목록에서 열 때는 안 났다.** 그때는 `openModal` 이 도는 시점에 해시가 아직 `#/mon/…` 이 아니고(`detail.js` 가 `openModal` **뒤**에 넣는다), 아래 항목이 깨끗하기 때문이다.

  쌓기 전에 지금 항목의 해시를 지워 **깨끗한 바탕**을 만든다. 그 위에 오버레이 항목이 올라가고 주소의 `#/mon/…` 은 `detail.js` 가 바로 뒤에서 다시 넣으므로, 결과는 `[아래: 해시 없음][위: #/mon/…]` — 목록에서 열었을 때와 같은 모양이다.

- **로그인 안내 팝업의 두 버튼을 가운데로** (`styles/components/consent.css`) — 버튼은 내용 폭에 맞춰 줄어드는데 그대로 두면 왼쪽 모서리에 붙어, 위의 세 단계 설명과 같은 왼쪽 줄에 선다. 설명의 네 번째 줄처럼 읽혔다. 이 둘은 읽는 것이 아니라 **고르는 것**이라 줄에서 떨어져야 한다. `min-width` 와 가운데 정렬은 동의 모달의 `[동의하고 로그인]`(`.consent__go`)과 같은 문법이다

### 회귀

- **✕ 뿐 아니라 뒤로가기로도 되살아나지 않는지까지 본다** (`tests/e2e/router.js`) — 되살아나면 같은 버그가 다른 길로 난 것이다. 좁은 화면(390px)에서 찬 시작으로 연다: 버그가 난 폭이 거기다
- 로그인 팝업 두 버튼의 가로 중심이 팝업 중심과 1px 안에 드는지 (`tests/e2e/planner.js`)
- **경합 하나 제거** (`tests/e2e/theme.js`) — `commit` 직후 한 번 들여다보고 "번들 전인데 이미 붙었다" 를 확인했는데, 그 순간에는 head 스크립트조차 아직 안 돌았을 수 있어 부하에 따라 통과와 실패가 갈렸다(v2.54.0 병렬 실행에서 드러났다). **"언제 보느냐" 를 재면 재는 쪽이 흔들린다.** 흔들리지 않는 두 가지로 나눴다 — (1) 값이 실제로 붙는가 (2) 붙이는 코드가 번들보다 앞에 있는가. (2) 는 문서 순서라 부하와 무관하다

### 확인

- 실측 — 좁은 화면에서 공유 링크(`#/mon/143`)를 찬 시작으로 열면 팝업이 뜨고, ✕ 뒤 `modal: false` · 주소의 `#/mon` 사라짐 · 뒤로가기로도 되살아나지 않음
- 로그인 팝업 버튼 중심 720 = 팝업 중심 720 (두 버튼 모두)
- 넓은 화면(1100px~)은 원래 정상이었다 — 공유 링크가 오른쪽 상세 패널로 열린다(회귀가 이미 지키고 있었다)

</details>

<details>
<summary><b>v2.55.0</b> — 2026-09-11 · 변경 · 주의 — 두 표에서 고른 타입의 뜻이 다르다 · 구조 외 1건</summary>

### 변경

- **D-MAX 의 [딜러] 탭이 그 타입 티어표를 위에 함께 보여 준다** (`views/max.js`) — 타입을 고르고 [딜러] 로 들어오면 **"이 타입으로 뭘 키우나"** 와 **"이 타입 보스를 뭘로 때리나"** 둘 다 궁금하다. 전에는 뒤엣것만 보여 주고, 앞엣것을 보려면 [전체] 탭으로 되돌아가 타입을 다시 골라야 했다
- 위는 티어표, 아래는 딜러. [탱커] 탭과 PvE·PvP 는 손대지 않았다

### 주의 — 두 표에서 고른 타입의 뜻이 다르다

| | 타입의 뜻 | 예: '에스퍼' 를 골랐을 때 |
| --- | --- | --- |
| 위 (티어표) | 맥스무브 속성 | 에스퍼 **맥스무브를 쓰는** 개체 |
| 아래 (딜러) | 보스 속성 | 에스퍼 **보스를 상대할** 개체 |

**명단이 겹치지 않는 게 정상이다.** 그래서 두 머리글이 그 차이를 글자로 말한다 — "에스퍼 맥스무브 D-MAX 티어표" · "에스퍼 보스 상대 D-MAX 딜러". 머리글을 짧게 줄이면 두 표가 같은 것의 두 벌처럼 읽히므로 줄이지 않는다. 화면 아래 안내문도 이 차이를 먼저 적는다.

### 구조

- 티어표 렌더링을 `maxTierBlock(selectedType)` 으로 뗐다 — [전체] 탭과 [딜러] 탭이 함께 쓴다. 두 곳에 같은 코드를 두면 한쪽만 고쳐 놓고 다른 쪽이 옛 모양으로 남는다

### 확인

- 실측 (에스퍼) — 위 "에스퍼 맥스무브 D-MAX 티어표" 20종(S·A·B·C 티어), 아래 "에스퍼 보스 상대 D-MAX 딜러" 상위 30
- 타입을 고르지 않았을 때 — 위 "D-MAX 티어표 (전체)" 30종, 아래 "D-MAX 딜러 (중립 · 맥스 피해 × √내구)" 상위 30
- 회귀는 **순서 자체**를 못 박는다 (`tests/e2e/nav.js`) — 뒤집히면 화면의 뜻이 바뀐다. 두 머리글이 서로 다른지도 함께 본다

</details>

<details>
<summary><b>v2.54.0</b> — 2026-09-10 · 먼저 쟀다 · 추가 · 변경 외 2건</summary>

화면에 보이는 변화는 없다. 개발 속도만 손봤다. 그래서 패치노트(`RELEASE_VER`)는 올리지 않는다 — 올려 봐야 새 소식 빨간 점만 뜨고 볼 것이 없다 (v2.49.1 과 같은 판단).

### 먼저 쟀다

"배포 시간이 꾸준히 증가한다" 는 지적을 짐작으로 고치지 않고 재어 봤다.

- **CI 는 병목이 아니었다** — GitHub Actions 배포(`deploy.yml`) 최근 20회가 **29~52초**로 일정했고 늘어난 흔적이 없다
- **실제로 늘어난 것은 로컬 회귀였다** — 순차로 약 24분. 릴리스마다 여러 번 돌리니 그게 대기 시간의 대부분이었다

### 추가

- **`scripts/test.sh`** — 회귀를 일꾼 넷으로 병렬 실행. 스위트끼리는 서로를 건드리지 않는다: 각자 제 브라우저를 띄우고 같은 정적 서버를 **읽기만** 한다. 순서대로 기다릴 이유가 없었다
- **느린 것부터 넣는다** — 일꾼이 넷인데 느린 것이 맨 뒤에 남으면 셋이 놀면서 그 하나를 기다린다. 순서는 **병렬로 한 바퀴 돌려 정했다** — 혼자 돌 때와 순위가 다르다(혼자면 `fingerprint` 가 가장 느렸는데, 넷이 함께 돌면 `shell` 이 450초로 압도적이다)
- **서버를 빌려 쓸 때 같은 빌드인지 확인한다** — 다르면 멈춘다. 그러지 않으면 조용히 옛 화면을 검사하게 되고, 그건 통과가 아니라 헛것이다
- 출력은 파일에 모았다가 알파벳 순으로 낸다(여러 프로세스가 같은 화면에 쓰면 줄이 섞인다). 실패한 스위트는 그 안의 `FAIL` 줄까지 보여 주고, 하나라도 실패하면 종료 코드가 1이다
- 이름으로 걸러 돌릴 수 있다 (`bash scripts/test.sh nav shell`), 테스트만 고쳤을 때는 `--no-build`

### 변경

- **`tests/e2e/shell.js` 가 두 화면 폭을 동시에 돈다** — 전에는 PC 15화면을 다 돈 뒤에야 모바일을 시작해서, 이 스위트 하나가 회귀 전체를 붙잡았다(450초, 두 번째로 느린 것의 두 배). 두 폭은 각자 제 컨텍스트를 쓰므로 저장소도 쿠키도 섞이지 않는다
- **`tests/e2e/fingerprint.js` 도 같은 이유로 동시에** — 22화면 × 2폭을 차례로 돌아 183초였다
- 화면을 옮길 때마다 페이지를 통째로 다시 읽고(1.5MB `data.js`) 그게 비용의 대부분이다. 줄일 방법은 "덜 읽기" 아니면 "같이 읽기" 인데, **검사 자체는 그대로 둬야 하니** 후자를 골랐다 — 검사를 줄여 시간을 버는 건 시간을 번 게 아니다

### 결과 (실측)

| | 걸린 시간 |
| --- | --- |
| 전 (순차) | 약 **24분** |
| 병렬만 (일꾼 4) | **583초** (9.7분) |
| 병렬 + shell·fingerprint 도 폭 동시에 + 순서 교정 | **298초** (5.0분) |

**4.8배.** 17개 스위트 전부 통과 상태에서 잰 값이다.

두 번째 줄에서 세 번째 줄로 간 것이 이 작업의 절반이다 — 일꾼을 늘려도 `shell.js` 하나가 450초면 전체는 450초 밑으로 못 내려간다. 병렬화는 **가장 느린 하나**를 줄여야 더 빨라진다.

남은 병목은 `nav` 253초 · `router` 213초 · `shell` 229초로, 셋이 비슷해졌다. 더 줄이려면 화면을 옮길 때마다 페이지를 통째로 다시 읽는 것(`page.goto`)을 해시 이동으로 바꿔야 하는데, 그건 "첫 로딩부터 확인" 이라는 검사의 뜻을 바꾸므로 여기서 멈춘다.

### 문서

- `docs/DEVELOPMENT.md` 에 쓰는 법과 왜 그렇게 했는지를 적었다

</details>

<details>
<summary><b>v2.53.0</b> — 2026-09-10 · 수정 · 회귀 · 확인</summary>

### 수정

- **알 부화 목록에 남의 칸이 섞여 있던 것을 바로잡았다** (`components/gameday.js` `EGG_SOURCES`) — 거리로만 여섯 칸을 만들었는데, **같은 거리라도 알을 어디서 얻었는지에 따라 나오는 종이 아예 다르다.** 걸어서 깐 5km 알은 실제 3종인데 어드벤처 싱크 전용 5종이 같은 칸에 붙어 8종처럼 보였고, 10km 는 7종이 12종으로, 7km 는 친구 선물(7종)과 루트 선물(5종)이 한 칸이었다. 사용자가 겪은 "지금 나오는 애들과 다르다" 가 이것이다 — 목록이 틀린 게 아니라 **볼 필요 없는 칸이 섞여 있었다**
- 이제 아홉 칸이다: 1km · 2km · 5km · 5km 어드벤처 싱크 · 7km 친구 선물 · 7km 루트 선물 · 10km · 10km 어드벤처 싱크 · 12km
- 거리 정렬도 바로잡았다 — 원본이 준 순서를 그대로 쓰다 보니 1km 가 12km 뒤에 서 있었다. 숫자로 센다
- 줄마다 붙던 '어드벤처 싱크' · '선물 알' 꼬리표는 뺐다 — 이제 칸 이름이 그 말을 한다
- **좁은 화면에서 D-MAX · 레이드 PvE · 배틀 PvP 가 카드로 덮이던 것을 목록으로 되돌렸다** (`styles/components/pc-theme.css`) — 카드 규칙이 미디어 쿼리 밖에 있어 휴대폰까지 2열 카드가 됐다. 한 칸이 반쪽이면 그림은 작아지고 이름은 두 줄로 접혀 한 화면에 두 마리밖에 안 들어온다. 티어표는 위에서부터 순서대로 훑는 화면이라 좁은 화면에서는 한 줄에 하나가 맞다. 1100px 이상에서만 카드다
- 레이드 보스·알 부화의 그리드(`.dex__list.is-grid`)는 사용자가 직접 켜고 끄는 토글이라 손대지 않았다
- **휴대폰 상세 팝업 88vh → 75vh** (`styles/components/modal.css`) — 바텀시트가 화면을 거의 다 덮어 뒤에 무엇이 열려 있었는지 보이지 않았다. 그러면 팝업이 아니라 새 화면으로 읽힌다. 주소창이 접히고 펴질 때 카드 아래가 잘리던 것도 함께 줄었다

### 회귀

- **`tests/e2e/breakpoints.js` 가 이미 8건 실패한 채로 dev 에 있었다** — 이번 작업 전부터다. 화면은 멀쩡한데 **테스트가 재는 방법이 낡았다**
- 컨테이너를 단계마다 고정폭(1100 · 1440)으로 못 박아 뒀는데, 지금 설계(`pc-theme.css` 간격 시안)는 `--shell-half: min(90rem, calc(50vw - 3.2rem))` — 화면을 따라 늘고 1800px 에서 멈추며 양옆에 32px 만 남기는 **유동폭**이다. 같은 식으로 세도록 고쳤다 (1440 → 1376, 1680 → 1616)
- 목록과 패널 사이 간격을 `padding-right` 로 역산했는데, 지금은 padding 이 아니라 안쪽 목록 폭으로 자리를 낸다. 그래서 padding 이 0 이면 480px 겹친 것처럼 보였다 — 실제로는 32px 떨어져 있다(실측). 사용자가 겪는 것은 "줄이 패널에 가리는가" 하나뿐이라, **실제로 그려진 줄의 오른쪽 끝**을 재도록 바꿨다
- 이번 변경도 못 박았다 — 알 칸이 얻는 곳까지 나뉘는가·거리순인가·칸을 나눠도 종 수가 늘지 않는가(`gameday.js`), 폭별 목록/카드 갈림과 휴대폰 팝업 75%(`breakpoints.js`)

### 확인

- **매핑 근거** — LeekDuck 원본 페이지의 구획 9개를 긁어 종 단위로 전부 대조했다. `isAdventureSync` → 어드벤처 싱크, 7km 의 `isGiftExchange` → **루트 선물**(이름과 달리 친구 선물이 아니다), 나머지 7km → 친구 선물. 9칸 모두 일치
- **데이터는 이미 최신이었다** — 배포본 72종이 ScrapedDuck 최신본·LeekDuck 웹사이트와 종·거리·스프라이트까지 완전히 일치했다. 틀린 것은 데이터가 아니라 묶는 방식이었다
- 폭별 측정 — 390·800px 은 목록(1열, `display:block`, 모서리 0), 1440px 은 카드(4열 grid, flex column, 모서리 12px)
- 모달 — 390px 에서 633/844 = 정확히 75.0%, 뒤 화면 211px 남음

</details>

<details>
<summary><b>v2.52.0</b> — 2026-09-10 · 변경 · 확인</summary>

### 변경

- **길이 단위를 px 에서 rem 으로** (`styles/base.css` 에 `html { font-size: 62.5% }` — 1rem = 10px) — 브라우저 설정에서 글자를 키우면 전에는 **글자만** 커지고 여백·버튼·카드 크기는 px 에 못 박혀 그대로였다. 글자가 커진 만큼 담을 그릇은 그대로니 화면이 도리어 빽빽해진다. rem 은 그 기준 글자 크기의 배수라, 이제 여백도 버튼도 함께 자란다. CSS 18개 파일 약 1,760곳과 인라인 스타일 2곳
- 62.5% 를 쓰는 이유는 계산 때문이다 — 1rem 이 10px 이 되어 `1.6rem` 을 보면 16px 이 바로 읽힌다. 기본값 16px 기준이면 모든 값이 나누어떨어지지 않는 소수가 된다
- **px 로 남긴 두 가지** — `@media` 조건은 rem 이면 중단점이 사용자 글자 크기를 따라 움직여, 같은 기기가 사람마다 다른 레이아웃으로 갈린다. 1px 선(테두리·구분선·포커스 링)은 선이 굵기로 뜻을 전하지 않기 때문이다 — '있다/없다' 만 말한다
- **티어표 [선정 근거] 를 아코디언으로** (`views/max.js`) — 카드마다 따로 토글해서, 넷을 차례로 누르면 근거 넷이 한꺼번에 펼쳐진 채 쌓였다. 근거 판은 카드 **뒤에** 가로폭을 다 쓰고 붙으므로, 여러 개가 열리면 어느 카드의 것인지 짝지을 수 없다. 이제 하나를 열면 앞의 것이 닫히고, 같은 카드를 다시 누르면 닫힌다
- **동의 모달의 `[동의하고 로그인]` 을 가운데로** (`styles/components/consent.css`) — 체크 두 줄 바로 밑에 왼쪽으로 붙어 있어 마지막 체크 항목의 연장선처럼 보였다. 이 버튼은 두 체크를 받아 다음으로 넘기는 자리라 줄에서 떨어져 서야 한다. 위 20px · 아래 16px

### 확인

- **값이 그대로인가** — 22개 화면에서 계산된 스타일 25,322개를 전후 대조했다. 달라진 값은 `<html>` 자신의 font-size(16px → 10px) 22건뿐이다. 즉 지금 화면은 전과 똑같이 보인다
- **바뀐 값이 쓸모가 있는가** — 기준을 100% 로 올리면 본문 15 → 24px, 카드 여백 20 → 32px, 모서리 14 → 22.4px, 손가락 대상 42 → 67.2px 로 모두 1.6배가 되고, 1px 선만 1px 로 남는다
- 회귀에 위 두 가지와 아코디언 동작을 못 박아 뒀다 (`tests/e2e/nav.js`)

</details>

<details>
<summary><b>v2.51.0</b> — 2026-09-10 · 변경</summary>

### 변경

- **잠긴 화면을 누르면 로그인 유도 팝업** (`components/auth.js` `openLoginInvite`) — 잠긴 메뉴 줄·서비스 홈 타일·잠긴 화면의 로그인 버튼이 모두 이 팝업을 거친다. 전에는 ☰ 메뉴를 열어 계정 카드로 스크롤했는데, 메뉴에는 일정표·기준 안내·약관 같은 줄이 함께 있어 **정작 무엇을 하라는 건지가 묻혔다.** 팝업은 화면 하나에 할 일 하나다 — 왜 막혔고, 무엇을 누르면 되는가
- **승인제를 누르기 전에 알린다** — 로그인 → 관리자 승인 대기 → 사용, 세 단계를 팝업 안에 적었다. 이 서비스는 로그인하면 곧바로 열리는 게 아니라 승인 대기가 되는데, 그걸 말해 주지 않으면 로그인하고 나서 "왜 아직도 안 되지" 를 다시 겪는다
- 승인 대기 중인 사람에게는 로그인 버튼 대신 "관리자가 승인하면 바로 열립니다" 만 보여 준다 — 이미 한 일을 또 시키지 않는다
- 조사는 `koParticle` (`components/name.js`) 로 받침을 따른다 — "육성 플래너은(는)" 같은 말을 쓰지 않는다. `koParticle` 에 을/를(`eul`) 을 더했다
- `tests/e2e/planner.js` 에 7 단언 — 팝업이 뜨는지·메뉴는 안 열리는지·가려던 화면 이름을 말하는지·승인 단계를 미리 알리는지·조사가 맞는지, 잠긴 화면의 버튼도 같은 팝업을 여는지

</details>

<details>
<summary><b>v2.50.0</b> — 2026-09-10 · 수정</summary>

"알 부화 포켓몬이 바뀌었는데 반영이 안 된다" 는 제보를 확인한 결과, **배포된 데이터는 이미 최신이었다.** 대조 기록:

- upstream(ScrapedDuck) 72종, 프로덕션 72종 — 여섯 거리 모두 종 수와 내용이 일치, `fetched: 2026-09-10`
- 낡아 있던 것은 (1) 저장소의 로컬 작업본 `data/`(gitignore 대상이라 배포와 무관) 과 (2) **사용자 기기**

즉 파이프라인은 정상이고, 문제는 클라이언트가 새 데이터를 **받으러 가지 않는 것**이었다.

### 수정

- **설치형 앱(PWA)이 옛 데이터를 붙들고 있었다.** 홈 화면에 설치한 앱은 한 번 띄우면 그대로 살아 있다. 그 안의 `data.js` 는 앱을 처음 연 그날 것이라, 매일 00시에 새 빌드가 나가도 화면은 며칠 전 알 부화 풀을 계속 보여 준다. 탭을 닫지 않는 브라우저도 같다. 서비스워커가 `data.js` 를 네트워크 우선으로 받는 것과는 별개 문제다 — 그건 **다시 받을 때** 이야기고, 앱이 다시 받지 않으면 소용이 없다
- 신설 `components/freshness.js` — 앱이 다시 보이는 순간(`visibilitychange`·`focus`, 10분 간격 제한)에 작은 표식 파일 `build.json`(78바이트)을 `cache: 'no-store'` 로 받아 지금 띄운 것과 견준다. 버전이나 데이터 날짜가 다르면 화면 아래 줄 하나로 알린다
- **자동으로 새로고침하지 않는다.** 보던 화면과 스크롤이 통째로 날아가고, 무엇이 왜 바뀌었는지도 모른 채 화면만 튄다. 고를 기회를 주고 누르면 그때 넘긴다
- `data.js` 는 1.5MB 라 확인용으로 매번 받을 수 없다 — 그래서 `backend/build.py` 가 버전과 데이터 날짜만 담은 `build.json` 을 따로 낸다
- `BUILD_VERSION` 전역을 빌드가 심는다. 헤더에 글자로 박아 두던 `__VERSION__` 은 `app-shell.js` 가 헤더를 통째로 갈아 끼우며 지워 버려 읽을 수 없었다 (처음엔 그 자리를 읽으려다 DOM 에 없는 것을 확인하고 바꿨다)
- **dev 미리보기도 매일 다시 빌드한다** (`deploy-dev.yml`, 00:30 KST) — 푸시가 없으면 dev 는 마지막 푸시 날짜의 데이터에 머물러, 확인하러 들어온 사람이 같은 증상을 본다. 실서비스(00:00)와 겹치지 않게 30분 뒤로 뒀다
- `tests/e2e/hardening.js` 에 10 단언 — `build.json` 이 나오는지·작은지·띄운 버전과 맞는지, 같은 빌드면 조용한지, 새 빌드면 알리는지, 자동 새로고침을 안 하는지

</details>

<details>
<summary><b>v2.49.1</b> — 2026-09-10 · 변경</summary>

화면에 보이는 변화는 없다. 버그 제보 링크의 주소는 이미 요청받은 값(`https://www.notion.so/a0472984122d4f25b9b445b57465568f`)이었고, 이번에 한 일은 **그 값이 바뀌지 않도록 못 박은 것**이다.

노션 쪽을 정리하면서 관리용 WBS(내부 개발·버전 관리) 문서가 따로 생겼다. 두 주소는 생김새가 같아 눈으로는 구분되지 않지만 쓰는 사람이 다르다 — 제보자는 외부 사용자고 WBS 는 내부용이라 권한도 내용도 다르다. 바꿔 끼우면 제보가 엉뚱한 곳으로 가거나 아예 못 쓰게 되는데, 코드만 봐서는 티가 안 난다.

### 변경

- `frontend/index.html` 의 그 줄 위에 경고 주석 — 왜 바꾸면 안 되는지까지 적는다 ("바꾸지 마시오" 만 있으면 다음 사람이 근거를 몰라 지운다)
- README 링크 표에 같은 단서
- `tests/e2e/hardening.js` 에 단언 둘 — "노션 주소인가" 가 아니라 **정확히 이 주소인가**, 그리고 새 창으로 열리는가(`target=_blank` · `rel=noopener`)
- 패치노트(`RELEASE_VER`)는 올리지 않았다 — 사용자에게 보일 변화가 없는데 올리면 새 소식 빨간 점만 뜨고 열어 볼 것이 없다

</details>

<details>
<summary><b>v2.49.0</b> — 2026-09-10 · 수정 · 변경</summary>

### 수정

- **내 포켓몬 줄이 넓은 화면에서 세로로 늘어지던 문제** — `planner.css` 는 좁은 화면에서 CP·기술 묶음을 아래로 눕히려고 `grid-column: 1 / -1` 을 준다. 그 값이 넓은 화면까지 따라와, 격자를 다시 쓰는 자리에서 묶음마다 한 줄씩 차지했다. 카드 하나가 400px 넘게 늘어져 화면을 거의 다 먹었다. `pc-theme.css` 에서 `grid-column: auto` 로 되돌린다
- 함께 크기를 줄였다 — 그림 76→48px · CP 24→19px · 순번 30→26px · 기술 알약 여백 · 동작 버튼 폭 고정(78px). 카드 높이 112px
- 회귀 단언은 높이만 재지 않는다 — **번호 → CP → 기술 → 동작이 가로로 서는지**를 좌표로 본다. 높이만 보면 글자 크기를 줄여도 통과해 같은 버그를 놓친다 (`tests/e2e/planner.js`)

### 변경

- **로그인 잠금을 배틀 PvP · 이벤트 일정 · 레이드 보스 · 알 부화로 넓혔다.** 잠금 대상을 코드에 흩뿌리지 않고 `ROUTES` 의 `locked` 표로 옮겼다(`routeLocked`) — 대상이 하나에서 다섯으로 늘면서 "어디를 잠갔나" 가 표 한 곳에서 읽혀야 한다
  - 잠금 카드를 공용으로 뺐다 (`lockedCardNode`) — 화면 이름과 "왜 로그인이 필요한지" 한 줄만 갈아 끼운다. 이유를 못 적는 화면은 애초에 잠그지 않는다는 뜻이기도 하다 (`LOCK_WHY`)
  - 메뉴 줄·PC 사이드바·홈 타일은 `data-route` 를 달고 `syncLockedNav` 가 한 번에 갱신한다 (id 를 하나씩 박던 방식은 대상이 늘면 매번 코드를 고쳐야 했다)
  - 셸 탭(배틀 PvP)에는 자물쇠를 단다. 누르는 것 자체는 막지 않는다 — 눌러야 "왜 잠겼는지" 와 로그인 버튼이 있는 화면으로 갈 수 있다
  - 대상: 육성 플래너 · 레이드 PvE · 배틀 PvP · 이벤트 일정 · 레이드 보스 · 알 부화
  - **도감 · 타입 & 상성 · D-MAX 는 잠그지 않는다.** 회귀 검사가 잠긴 여섯과 안 잠긴 셋을 함께 확인한다 — 잠금은 늘리기 쉬워서, 잠그지 **않기로 한** 화면도 같이 못 박아 둬야 한다
- **(버그) 로그인이 끝난 뒤에도 잠금 화면이 남았다** — Firebase SDK 는 첫 화면이 그려진 뒤에 로드된다(`initAuth`). 그래서 `#/raids` 같은 주소로 바로 들어오면 그 순간에는 비로그인이라 잠금 카드가 그려지고, 잠시 뒤 로그인이 끝나도 화면을 다시 그리지 않아 **로그인한 사람이 자기 화면을 못 보는** 상태가 됐다. `onAuthChange` 가 현재 라우트의 `locked` 를 보고 다시 그린다. 회귀 검사(`layout-toggle.js`)가 이 버그를 먼저 잡았다

</details>

<details>
<summary><b>v2.48.0</b> — 2026-09-10 · 수정</summary>

**긴급.** v2.44.0 에서 CSP 를 고쳐 브라우저 로그인은 돌아왔지만, **홈 화면에 설치한 앱(PWA)에서는 여전히 로그인이 안 됐다.** 원인이 따로 있었다.

`signIn()` 은 `display-mode: standalone` 이면 `signInWithRedirect` 로 갈랐다. 그런데 우리 `authDomain` 은 `pogo-note.firebaseapp.com` 이고 앱은 `minsangkwak.github.io` 다 — 다른 출처다. 리다이렉트로 로그인하면 자격 증명이 authDomain 쪽에 저장되고, 앱은 그것을 authDomain 의 iframe 을 통해 읽어야 한다. 요즘 브라우저는 그 iframe 의 저장소를 최상위 사이트별로 갈라 두므로(서드파티 저장소 차단·파티셔닝) 앱이 값을 못 읽는다. 이때 `getRedirectResult()` 는 **오류를 던지지 않고 null 을 준다** — 화면은 조용히 비로그인으로 남는다. 설치형 앱은 브라우저와 저장소를 따로 쓰는 경우도 있어 더 자주 깨진다. "설치된 앱은 창을 못 띄운다" 는 그 분기의 근거는 옛 iOS 제약이고 지금 브라우저에는 해당하지 않는다.

### 수정

- **팝업을 먼저 쓴다 — 설치형 앱도 마찬가지** (`components/auth.js` `signIn`). 팝업은 창 사이 `postMessage` 로 자격을 건네므로 그 저장소를 타지 않는다. v2.44.0 에서 CSP 를 고쳐 `apis.google.com` 이 열린 뒤로는 팝업이 실제로 뜬다. 팝업이 막힐 때만(`popup-*` · `internal-error`) 리다이렉트로 넘긴다
- **빈손으로 돌아온 리다이렉트를 잡는다** — 나갈 때 `localStorage pogo_auth_redirect` 에 표시를 남기고, 돌아와서 결과가 비었으면 왜 안 됐는지 말한다. 오류가 아니라 `null` 로 끝나는 실패라 전에는 잡을 방법이 없었다
- 그 안내는 `AUTH.redirectMsg` 로 **상태로** 둔다 — 한 번짜리 문구로 두면 곧바로 뒤따르는 `onAuthStateChanged(null)` 의 다시 그리기에 지워진다(실제로 그랬다). 다시 로그인을 누르거나 로그인에 성공하면 지워진다
- `tests/e2e/auth-retry.js` 에 5 단언 추가 — 설치형 앱인 척했을 때 팝업을 쓰는지·리다이렉트로 새지 않는지, 빈손 리다이렉트를 말로 알려 주는지

</details>

<details>
<summary><b>v2.47.0</b> — 2026-09-10 · 변경 · 수정 · 추가</summary>

목업 4장(육성 현황 · 내 포켓몬 · 개체 비교 · 개체 추가)을 받아 플래너 쪽을 다시 짰다. 먼저 **메뉴를 합쳤다** — '내 포켓몬' 과 '육성 플래너' 는 같은 화면의 두 탭인데 ☰ 메뉴에도 서비스 홈 타일에도 문이 두 개씩 있어, 어느 쪽을 눌러야 하는지가 매번 질문이 됐다.

### 변경

- **메뉴·홈 타일 통합** — 🌱 육성 플래너 한 줄로. `planner-collection` 라우트와 옛 주소(`#/plan/collection`)는 그대로 살려 둔다: 저장해 둔 링크와 상세 팝업의 ➕(`planAddFromDetail`)가 그 주소를 쓴다 (`router.js` · `components/home.js`)
- **육성 현황** (`planner/home.js`) — 히어로 · 요약 카드(내 포켓몬 N마리 + 상태별 수치 세 칸 + 바로가기) · "지금 무엇을 하면 좋을까?" 다섯 걸음 · 최근 넷. 다섯 걸음은 **지금 실제로 되는 것만** 적는다. 목업의 걸음("필요 자원 계산하기" · "기술 세팅하기")은 아직 없는 기능이라, 그대로 적으면 눌러 보고 없다는 걸 알게 되는 헛걸음이 된다. 아직 없는 것은 아래 로드맵 한 줄에 그대로 남겼다
- **내 포켓몬 목록** (`planMonCard`) — 한 줄을 정보 묶음으로: 순번 배지 · 발광 그림 · 이름/상태/유사백 배지 · 타입 알약 · Lv + 개체값 막대 · 큰 CP · 주요 기술 두 줄 · 동작. 예전에는 Lv · 개체값 · CP · 기술이 한 문장으로 이어져 있어 찾는 값을 눈이 매번 훑어야 했다
- **좁은 화면은 묶음을 아래로 눕힌다** — 감추지 않는다. CP 와 기술은 v2.47.0 이전에도 좁은 화면에서 보이던 값이라, 지우면 휴대폰 사용자만 정보를 잃는다. 순번 배지만 감춘다(세로로 긴 목록에서 번호가 자리값을 못 한다)
- **같은 종 비교 창** — 표 위에 카드 두 장(왼쪽 브랜드 · 오른쪽 포인트, 표의 두 칸과 색으로 짝지운다), 표 아래에 결론 한 줄(`planCompareVerdict`). 표 머리는 이름만 남겼다 — 카드가 이미 그림·상태·개체값을 보여 준다
- **결론은 근거 순서를 지킨다** — ① 섀도우가 한쪽만이면 **승자를 정하지 않는다**(섀도우는 CP 에 안 잡히는 배틀 보정이 따로 있어 이 표만으로 "더 강하다" 라고 하면 틀린 말이 된다) ② 만렙 CP ③ 지금 CP ④ 둘 다 같으면 같다고 말한다. 목업은 승자를 단정하지만 우리 데이터로는 그렇게 말할 수 없는 경우가 있다
- **개체 추가 창의 검색 결과** — 헤더 검색 패널과 같은 줄을 쓴다. 줄 마크업을 `monSuggestRow`(`components/search.js`) 하나로 빼서 두 곳이 나눠 쓴다 — 같은 마크업을 두 군데 두면 한쪽만 고쳐지는 날이 온다
- **육성 플래너는 로그인해야 열린다** (`planner/shell.js` `planLocked` · `renderPlanLocked`) — 개체를 계정에 저장하는 화면인데 로그인 전에도 화면이 다 열려 있어, 적어도 저장되지 않고 끝나는 길이 있었다. 메뉴 줄과 홈 타일에 자물쇠가 붙고(`syncLockedNav`), 눌러도 그 화면 대신 계정 카드가 열린다. 주소로 직접 들어가도 잠긴 화면이 뜨고 탭 줄도 그리지 않는다 — 못 가는 곳으로 가는 버튼은 두지 않는다. 로그인 기능이 꺼진 빌드(`FIREBASE_CONFIG` 비어 있음)에서는 잠그지 않는다: 열 방법이 없는 잠금은 영구 차단이다
- `static/dev-mock.js` 에 `mons` 를 시드했다 — 플래너 화면이 개체 없이는 빈 상태만 보여 줘 배치를 확인할 수 없었다. 종·기술 이름은 전부 실제 데이터에서 가져온 값이다

### 수정

- **비교를 눌렀는데 아무 일도 안 일어나던 문제** — 다른 종을 고르면 조용히 새로 시작했고, 안내문은 목록 **아래**에 그려 문서 3,000px 지점(=화면 밖)에 있었다. 좌표로 재서 확인했다. 이제 안내를 목록 **위**에 띄우고, 왜 짝이 풀렸는지 말하고, 다시 그린 뒤 그 자리로 스크롤한다. 짝이 될 수 없는 줄의 [비교] 는 흐리게 둬 지금 짝인 줄이 어느 것인지 보이게 했다
- **한국어 조사** (`components/name.js` `koParticle`) — 안내 문구가 "거북왕 와" · "리자몽 로" 처럼 틀린 말을 내고 있었다. 앞 글자의 받침을 보고 와/과 · 로/으로 · 이/가 · 은/는 을 고른다
### 추가

- **화면 테마 전환** (신설 `components/theme.js`) — 세 상태를 돈다: 기기 설정 따름 → 밝게 → 어둡게. "기기 설정 따름" 을 없애면 기기를 밤에 어둡게 바꿔도 이 사이트만 밝게 남는다. 색은 `tokens.css` 가 이미 세 경우를 다 정의해 뒀다(`prefers-color-scheme` · `[data-theme]`)
  - 넓은 화면은 헤더 버튼(`#theme-toggle`), 좁은 화면은 ☰ 메뉴 줄(`#menu-theme`). 헤더에 버튼을 하나 더 넣었더니 좁은 화면에서 로고 "POGO PLAN" 이 잘려, 폭에 따라 하나만 켠다
  - 기억: `localStorage pogo_theme` + 승인된 로그인 사용자는 `users/{uid}.theme`. 둘이 어긋나면 계정 값이 이긴다(마지막으로 고른 값이 거기 있다)
  - **첫 화면 깜빡임 방지** — 저장한 값은 `index.html` `<head>` 인라인 스크립트가 붙인다. 번들은 `<body>` 끝의 인라인 `<script>` 라, 거기서 붙이면 어둡게 골라 둔 사람이 매번 흰 화면을 한 번 보고 지나간다
  - 브라우저 상단 바 색(`theme-color`)도 함께 맞춘다 — 고른 화면과 어긋나면 주소창만 다른 색으로 남는다
  - 개인정보처리방침의 수집 항목·로컬 저장소 목록에 이 값을 적었다 (`components/privacy.js`)
- `tests/e2e/theme.js` 신설 (23 단언) — 세 상태 순환 · 실제 배경색 · 새로고침 직후(번들 전) 적용 · 계정 동기화 · 폭에 따른 자리 바꿈
- `tests/e2e/planner.js` 신설 (41 단언) — 잠금(메뉴·타일·주소 직접 진입)·화면 구성·비교. 안내 바는 존재만이 아니라 **뷰포트 안에 있는지**를 좌표로 확인한다: 있기만 하면 이 버그를 다시 놓친다

</details>

<details>
<summary><b>v2.46.0</b> — 2026-09-10 · 변경</summary>

플래너·내 포켓몬 목업 4장을 받아 도감 쪽 화면을 같은 문법으로 맞췄다. 목업에 있고 도감에 없던 것 셋만 옮긴다 — 목업의 큰 히어로 배너는 붙이지 않았다. 도감 화면 머리에는 이미 제목과 한 줄 설명이 있고(`router.js` `ROUTE_DESC`), 그 위에 배너를 또 얹으면 "무엇을 찾을지" 보다 배너가 먼저 나온다. 공식 아트워크는 이 저장소에 없으므로 목업의 3D 그림 자리는 지금까지처럼 빛(그라데이션)으로 채운다.

### 변경

- **검색 결과 줄** (`components/search.js`) — 이름 한 줄에서 카드 한 장으로. 그림 판 + 이름 + 타입 알약 + 도감번호 + 화살표. 조각은 도감 목록에서 쓰던 것을 그대로 빌린다(`.dex__type` · `.dex__no`). 이름만 있던 줄은 물짱이 / 새도우 물짱이처럼 이름이 겹칠 때 "이게 내가 찾던 그건가"를 답해 주지 못했다
- **도감 줄·카드에 읽을 값** (`components/pages.js` `dexRowStats`) — 세대와 CP 100% 기준. 둘 다 이미 가진 데이터로 계산한다(도감번호 구간 `DEX_GENS` · 게임마스터 CPM `DEX_DATA.cpm.l50`, 상세 팝업이 맨 위에 보여 주는 그 값). 줄 모드는 두 칸, 카드 모드는 CP 만 — 카드에서는 번호 배지가 이미 그 자리를 말하고 세대 칩이 목록 바로 위에 있다. 값이 없으면 칸을 아예 만들지 않는다(빈 칸을 남기면 "없는 것"과 "0" 이 같아 보인다)
- **상세 팝업 머리** — 그림을 116px 발광 원판 위로, 이름 26px, 도감번호를 알약 배지로. 타입 배지는 판 왼쪽 위 모서리 그대로 둔다 (`tests/e2e/detail.js` 가 그 자리를 지킨다)
- 도감 화면 머리에 장식 띠 — D-MAX(폼 색)와 같은 문법, 도감은 브랜드 초록
- `components/styleguide.js` 에 `.sugg__item` 을 넣었다 — 검색 패널 안에서만 보이던 조각이라 목록에 없었다
- **거르기 칩 높이 34px → 22px** — 두 줄로 접히면 줄 사이가 벌어져 보였는데, 원인은 여백이 아니라 **속이 빈 알약**이었다. `.uchip` 은 `align-items: baseline`(`modal.css`)이라 글자가 아래에 붙고 위쪽 12px 이 그대로 빈다. 높이를 글자에 맞추고 가운데 정렬로 바꿨다 (손가락 대상 `--tap` 44px 은 좁은 화면 규칙이라 영향 없음)

</details>

<details>
<summary><b>v2.45.0</b> — 2026-09-10 · 변경 · 수정</summary>

도감 카드를 v2.43.0 티어표 카드와 같은 문법으로 맞췄다. `.dex__row` 는 도감만의 것이 아니다 — ★ 즐겨찾기 · ⚔️ 레이드 보스 · 🥚 알 부화가 같은 조각을 빌려 쓰므로(`components/favs.js` · `gameday.js`) 이 블록 하나로 네 화면이 같이 바뀐다. 티어표만 새 옷을 입고 도감이 옛 옷으로 남으면 같은 앱 안에 카드가 두 종류가 된다.

### 변경

- **카드 모드** — 번호는 왼쪽 위 알약 배지(티어표 순위 배지와 같은 자리·테두리, 자릿수 때문에 원 대신 알약), 그림은 빛나는 원형 판 위, 이름은 `var(--fs-lead)` 굵게, ★ 는 동그란 버튼(`.home__arrow` 와 같은 문법, 켜지면 `--caution`)
- **줄 모드** — 그림이 44px 뿐이라 발광 판을 그대로 줄이면 빛이 얼룩처럼 보인다. 모양만 잇는다 — 네모 상자를 동그란 판으로. 번호는 같은 알약 배지
- 모든 규칙은 `styles/components/pc-theme.css` 의 `@media (min-width: 1100px)` 안 — 휴대폰 배치는 손대지 않는다

### 수정

- **티어표 카드의 발광 판이 실제로는 한 번도 안 나오고 있었다** (v2.43.0 회귀) — `list.css` 의 `.row-list > .row > .sprite:not(.is-loading) { background: var(--bg) }` 가 **단축 속성**이라 `background-image` 까지 `none` 으로 되돌리는데, `:not()` 이 붙어 있어 특정도까지 더 높았다. 같은 선택자(`:not` 포함)로 받아 `background-image` 와 `background-color` 를 따로 적는다. 도감 카드도 `pages.css` 에 같은 모양의 규칙이 있어 함께 걸린다

</details>

<details>
<summary><b>v2.44.0</b> — 2026-09-10 · 수정 · 변경</summary>

로그인이 안 되던 원인은 기기도 브라우저 설정도 아니라 **우리 CSP** 였다. v2.27.0(보안 강화)에서 `script-src` 에 `https://apis.google.com` 을 빠뜨렸고, `firebase-auth-compat` 는 로그인할 때 그 주소의 `api.js` 를 먼저 받아 인증 이벤트용 iframe 을 띄운다. CSP 가 그 스크립트를 거부하면 SDK 는 `loadJS` 의 `onerror` 를 `auth/internal-error` 로 바꿔 던진다 — 사용자가 본 그 문구다. 리다이렉트로 넘겨도 같은 파일이 필요해 v2.39.0(재시도)·v2.39.1(리다이렉트)이 둘 다 듣지 않았다.

### 수정

- **CSP `script-src`·`frame-src` 에 `https://apis.google.com` 추가** — Google 로그인 복구 (`frontend/index.html`)
- **CSP `img-src` 에 `https://*.googleusercontent.com` 추가** — 계정 카드·가입 승인 패널의 Google 프로필 사진이 같은 이유로 막혀 있었다
- **리다이렉트 로그인 결과의 오류를 화면에 띄운다** — `getRedirectResult().catch(() => {})` 로 통째로 버리고 있어, 실패하면 아무 안내 없이 비로그인 화면으로 돌아왔다 (`components/auth.js`)
- **가입 요청(`requests` 문서) 저장 실패를 알린다** — 실패하면 관리자 패널에 줄이 아예 안 뜨는데 본인 화면은 "승인 대기 중"이라고만 해서, 기다리는 쪽만 계속 기다리게 됐다 (`AUTH.requestError`)

### 변경

- `components/auth.js` 의 v2.39.1 주석을 실제 원인으로 바로잡았다 — 팝업 대체(리다이렉트) 분기 자체는 남긴다. 팝업이 브라우저·확장 프로그램에 막히는 경우는 여전히 있고 그때는 그 길뿐이다
- **`tests/e2e/hardening.js` 가 "우리가 쓰는 출처"도 센다** — 지금까지는 CSP 가 빡빡한지만 물었고, 그 검사는 이번 회귀를 전부 통과시켰다. 지시어별 구간을 잘라 필요한 출처를 확인하고, `apis.google.com/js/api.js` 를 실제로 받아 본다(응답은 스텁, 보는 것은 CSP)

</details>

<details>
<summary><b>v2.43.0</b> — 2026-09-10 · 변경 · 불변</summary>

D-MAX 목업을 받아 티어표를 다시 짰다. **여기 있는 것은 전부 공용 조각**이다 — 티어 머리글·랭킹 카드·선정 근거는 D-MAX·레이드 PvE·배틀 PvP·일반 티어표가 같이 쓰므로 한 화면만 바꿀 수는 없고 그럴 이유도 없다.

### 변경
- **티어 머리글에 이름과 설명** (`views/tier.js` `TIER_DESC`) — 뱃지 한 글자(S·A·B·C)만으로는 처음 보는 사람이 위아래를 모른다. `[S] S 티어 · 4종` + "메타를 지배하는 최상위 포켓몬입니다." 한 줄. 뱃지 색도 의미 토큰으로: S 포인트(보라)·A 주의(호박)·B 브랜드(초록)·C 비활성(회색)
- **랭킹 카드** (`styles/components/pc-theme.css`) — 순위가 동그란 배지(그 티어 1위만 브랜드색), 그림 아래에 빛나는 판(공식 아트워크가 없으니 빛으로 자리를 만든다), 점수 26px, "활용 N곳"은 알약 하나로
- **선정 근거를 두 칸으로** (`views/max.js` `expandableRow`) — 왼쪽 "📊 티어 점수 산정 방식"(계산식 + 포켓몬 상세 보기 →), 오른쪽 "🛡 얘가 보스라면 데려갈 딜러"(칩). 성격이 다른 두 정보가 한 문단에 이어져 있어 어디까지가 계산식인지 눈이 못 잘랐다
  - 넓은 화면에서는 `order` 로 **그 티어의 카드가 다 놓인 뒤**에 붙인다. DOM 이 `[카드][근거][카드][근거]…` 순서라 그대로 두면 근거가 열릴 때마다 그 자리에서 줄이 끊겨 뒤 카드들이 아래로 밀렸다
- D-MAX 화면 머리에 폼 색(자홍) 장식 띠 — `.page-head[data-route="dmax"]` 로 그 화면에만

### 불변
- 좁은 화면 배치는 그대로 — 새 규칙은 전부 `pc-theme.css` 의 `@media (min-width:1100px)` 안에 있고, 바뀐 DOM(티어 이름·설명, 근거 두 칸)에는 좁은 화면용 기본 스타일을 `list.css` 에 따로 뒀다(설명은 제 줄로, 두 칸은 위아래로)

</details>

<details>
<summary><b>v2.42.0</b> — 2026-09-10 · 변경 · 수정 · 불변</summary>

목업 4장을 받아 **넓은 화면(1100px~)** 디자인을 통째로 다시 입혔다. 휴대폰 배치는 손대지 않았다 — 목업이 PC 전용이었고, 좁은 화면은 지금 배치가 이미 손가락에 맞다.

### 변경
- **디자인 토큰 교체** (`styles/tokens.css`) — 목업 팔레트로 갈아끼웠다. 브랜드 초록(`--brand`)이 "이 서비스"이자 "지금 여기"를 뜻하고, `--brand-2`·`--point`·`--warn`·`--caution`·`--off` 다섯이 뜻이 정해진 색이다. 다크는 목업의 딥네이비(`#0b0f15`), 라이트는 같은 팔레트를 흰 바탕 대비에 맞춰 한 단 진하게. 글자 크기(`--fs-title` 32 / `--fs-sec` 20 / `--fs-lead` 16 / `--fs-body` 14 / `--fs-sub` 12)와 반경(`--r-card`·`--r-chip`)도 토큰으로 뺐다
  - 화면 파일에 직접 적혀 있던 색(`#2f8f5b`·`#d84a5f`·`#2f6fd8`·`#e8a80c`·`#c0392b`·`#b8860b`)을 전부 뜻에 맞는 토큰으로 바꿨다 — 같은 초록이 세 파일에 세 값으로 적혀 있던 것을 한 곳으로 모은다
- **넓은 화면 전용 파일 신설** (`styles/components/pc-theme.css`) — 이 개편의 모든 규칙이 통째로 `@media (min-width: 1100px)` 안에 있고 밖에는 아무것도 없다. 한 파일에 섞으면 "이 규칙이 모바일에도 가나?"를 매번 되짚어야 한다
- **상단 바에 검색 칸** (`components/app-shell.js` `appSearch`) — 돋보기 아이콘 하나로는 "여기서 검색한다"가 안 읽혔다. 입력처럼 생겼지만 실제로는 버튼이라 누르면 늘 쓰던 검색 팝업이 열린다(입력을 두 벌 두면 어느 쪽 글자가 진짜인지 흐려지고 결과도 두 곳에 그려야 한다). `/` 키로도 열린다 — 글자를 치는 중이면 가로채지 않는다
- **화면 머리 = 브레드크럼 · 큰 제목(32px) · 한 줄 설명** — 설명은 `router.js` `ROUTE_DESC` 한 곳에 둔다. 브레드크럼·설명·검색 칸은 조각을 늘 만들어 두고 **보이고 감추는 일만 CSS 가** 가른다(JS 에 폭 분기를 더 만들지 않는다)
- **서비스 홈** — 초록 그라데이션 히어로 배너와 5열 타일(번호·아이콘·이름·설명·동그란 화살표), 푸터도 카드 한 장. 히어로의 그림은 CSS 로 그린다 — 목업의 공식 아트워크는 이 저장소에 없고, 없는 그림을 지어내지 않는다
- **도감** — 줄 모드가 카드 줄로(그림 44px·화살표), 카드 모드는 그림 76px. 거르기 칩과 보기 방식을 한 줄 좌우로 묶었다(`.dex__toolbar`). 타입이 점 대신 **이름이 적힌 알약**으로 (좁은 화면은 자리가 없어 점 그대로)
- **상세** — 약점·내성을 각각 카드 한 장으로 갈랐다(경고색/브랜드색). 칩만 두 줄로 늘어놓으면 훑을 때 어디까지가 약점인지 섞였다
- **UI 목록**(dev 전용) — 구역을 번호가 붙은 카드 격자로. 위에서 아래로 길게 늘어놓으면 "가진 조각이 이만큼"이라는 감이 안 온다. 사이드바 메뉴에도 올렸다(실서비스에는 파일이 없으니 항목도 없다)

### 수정
- UI 목록의 타입 배지 데모가 페이지 왼쪽 위로 튀어나가던 문제 — `.detail__types` 는 실제로 그림 위에 절대 배치되는 조각인데 데모에 기준점이 없었다. `.sg__demo` 에 `position: relative` 를 주고, 데모도 실제처럼 그림 상자와 함께 보여 준다

### 불변
- **휴대폰(1099px 이하) 배치는 그대로** — 색만 새 팔레트를 따른다. 회귀로 확인
- DOM id · localStorage 키 · 전역 이름 그대로. `tests/e2e/nav.js` 는 팔레트 값을 새 값으로 갱신(색을 화면에 직접 적는 습관이 되살아나면 바로 걸리도록 못 박아 두는 뜻은 그대로)

</details>

<details>
<summary><b>v2.41.0</b> — 2026-09-09 · 추가</summary>

"지금까지 만든 스타일을 한눈에 보고 싶다"는 요청. 스토리북 대신 **실제 조각을 그대로 늘어놓는 화면**을 만들었다.

### 추가
- **UI 목록 화면** (`components/styleguide.js`, `styles/components/styleguide.css`) — `#/styleguide` 에 색 토큰·글자·버튼·고르기(세그먼트·칩·탭)·태그/뱃지·목록·카드·메뉴·팝업을 구역별로 늘어놓는다(9구역 37칸). 화면이 마흔 판을 넘기며 같은 뜻의 조각이 여러 벌 생기기 쉬워졌는데, 늘어놓고 봐야 "이건 저것과 같은 것 아닌가"가 눈에 걸린다
  - **왜 스토리북이 아닌가** — npm·번들러가 통째로 들어와 "의존성 없는 바닐라 JS" 전제가 깨지고, 무엇보다 조각을 스토리용으로 한 벌 더 적게 되어 실제 화면과 조용히 어긋난다. 여기서는 실제 CSS 와 실제 함수(`uchip`·`seg`·`layoutToggle`·`sprite`…)를 그대로 부르므로 **보이는 모양이 곧 실제 화면의 모양**이다
  - **dev 미리보기에만** — `backend/build.py` 가 `BUILD_CHANNEL=dev` 일 때만 이 파일들을 `STYLES`/`SCRIPTS` 에 넣는다. 라우트(`ROUTES`)·페이지(`PAGES`) 등록을 그 파일이 스스로 하므로, 빼면 **주소 자체가 없는** 빌드가 된다(실서비스 번들에 코드가 0바이트 들어간다)
  - 자리가 정해져 있다: `components/pages.js` **뒤**(`PAGES` 가 만들어진 뒤), 첫 렌더 **앞**. 맨 끝에 붙였더니 이 주소로 바로 들어온 첫 화면이 등록 전에 그려져 홈으로 떨어졌다. `pages.js` 가 제 파일 끝에서 `renderPage()` 를 한 번 부르므로, 등록을 마친 뒤 한 번 더 부른다
  - 데모는 실제 DOM id 를 빌리지 않는다(`#drawer-extra` → `.sg__extra`) — 한 문서에 같은 id 가 둘이면 `getElementById` 가 어느 쪽을 집을지 문서 순서에 달린다(`app-shell.js` 가 그 id 를 쓴다)
  - `tests/e2e/styleguide.js`(신규) 는 화면에 적힌 버전으로 채널을 갈라 **양방향**을 검사한다 — dev 빌드(15항목)에서는 구역·조각·중복 id 없음·실제 메뉴 무결성, 실서비스 빌드(4항목)에서는 화면도 코드도 없음

</details>

<details>
<summary><b>v2.40.1</b> — 2026-09-09 · 수정 · 감사 (이번에 통과한 것)</summary>

배포 뒤 전 화면·전 폭을 훑는 감사에서 v2.40.0 이 심은 회귀를 하나 찾아 고쳤다.

### 수정
- **폭을 오갔다 돌아오면 ☰ 메뉴에서 기준 안내·트레이너 코드가 사라지던 문제** (`components/app-shell.js` `placeDrawerExtra`) — v2.40.0 이 정보 항목을 카드 한 장(`infoGroup`)으로 묶었는데, 넓은 화면으로 돌아갈 때 그 카드를 `remove()` 로 들어내면서 **안에 담아 둔 아코디언까지 함께 문서에서 빠졌다**(`#drawer-extra` 는 그 전에 사이드바로 옮겨져 살아남았지만 아코디언 둘은 갈 곳이 없었다). 카드를 들어내기 전에 담아 둔 것들을 드로어 제자리(버전 문구 앞)로 먼저 꺼내도록 고쳤다
  - 첫 로딩만으로는 안 드러난다 — 넓은 화면에서 시작하면 카드가 빈 채라 아무 일도 없고, **좁은 화면을 한 번 거쳐야** 나온다. 창 크기 조절·태블릿 회전·폴더블 펼침에서 재현
  - `tests/e2e/shell.js`(70항목) 에 폭 왕복(모바일 → PC → 모바일 → PC) 회귀 13항목 추가 — 항목 유지·목록 한 벌·`#drawer-extra` 복제 없음을 매 단계 확인한다

### 감사 (이번에 통과한 것)
- 15개 화면 × 7개 폭(360·390·768·1100·1280·1440·1680)에서 가로 넘침·콘솔 오류 없음 (112항목)
- 리사이즈 왕복 · 로그아웃 상태 · 다크 모드 강조색 · 화면별 보기 방식 저장 격리 (42항목)

</details>

<details>
<summary><b>v2.40.0</b> — 2026-09-09 · 변경 · 불변</summary>

☰ 메뉴와 보기 방식 버튼을 목업에 맞춰 다시 짰다. 둘 다 "지금 어디/어느 보기인지"가 안 읽히던 자리다.

### 변경
- **☰ 메뉴를 덩이(카드) 목록으로** (`components/app-shell.js`, `styles/components/drawer.css`, `index.html`) — 항목마다 테두리가 따로 있어 열 개가 넘는 메뉴가 "버튼 무더기"로 읽히던 것을, 성격이 같은 것끼리 카드 한 장에 담고 사이를 얇은 선으로만 나눴다. 구역 제목(👤 마이페이지 · 서비스 · 정보)은 좁은 화면에서만 단다(PC 사이드바는 이동 목록뿐이라 제목이 군더더기). 계정 카드를 이동 목록 위로 올렸다 — 메뉴를 열면 "내가 누구인지"가 먼저고 그다음이 갈 곳이다
  - 줄 한 개 = `[아이콘] 이름 [›]`. 아이콘은 서비스 홈 타일과 같은 그림을 쓴다 — 그래서 표를 **라우터 표 한 곳**(`router.js` ROUTES `icon`)으로 옮기고 `home.js`가 그 표를 읽게 했다(예전엔 home.js 안에만 있어 메뉴에는 못 붙였다)
  - 이모지를 라벨 글자에 섞지 않고 `.drawer__ico` span 으로 뗐다 — 아이콘이 한 칸에 가지런히 서고, 번역 사전(텍스트 노드 단위)은 이름만 있는 키로 맞아떨어진다(`i18n-en.js` 에 이름만 남은 키 추가)
  - 새 소식 점(`.dot-badge::after`)과 화살표가 같은 `::after` 자리를 다투므로, 카드 줄에서는 점을 `::before` 로 옮겨 둘 다 보이게 했다
  - 계정 카드: 즐겨찾기·내 포켓몬 개수를 가운뎃점으로 이어 붙인 한 줄에서 두 칸으로 나누고, 버튼(가입 승인·로그아웃)을 세로 쌓기에서 가로로 바꿨다
- **보기 방식을 두 칸 세그먼트 컨트롤로** (`components/ui.js` `layoutToggle`, `styles/components/seg.css` `.seg-view`) — 누를 때마다 뜻이 뒤집히는 버튼 하나였을 때는 라벨이 "지금 보기"인지 "누르면 될 보기"인지 글자만으로 안 갈렸고(v2.29.2 에서 한 번 고쳐 쓴 문제), 좁은 화면에서는 안내문 아래 홀로 떠 눈에도 안 띄었다. `[☰ 리스트 | ⊞ 그리드]` 를 나란히 놓고 고른 칸만 강조색으로 칠한다. 좁은 화면은 목록 폭을 다 쓰고 넓은 화면은 260px 로 오른쪽에 붙는다
  - 도감·즐겨찾기에서는 세대·갈래 필터 칩과 한 줄에 섞여 있던 것을 자기 줄로 뺐다 — 거르기(무엇을 보여줄지)와 보기 방식(어떻게 보여줄지)은 성격이 다르다
- 목업의 보라색 강조 대신 앱이 이미 쓰는 강조색(`--accent`)을 썼다 — "색은 의미에만"(지금 여기)이라는 규칙을 지키려고 새 색을 들이지 않았다

### 불변
- localStorage 키·값(`pogo_dex_cols` 등, `'1'` 리스트 / `'2'` 그리드) · `.dex__layout` 클래스 · DOM id 는 그대로. 저장해 둔 선택이 그대로 이어진다
- 회귀: `tests/e2e/layout-toggle.js`(40항목, 세그먼트 컨트롤 시나리오로 다시 씀) · `shell.js`(57항목, 메뉴 구역·카드·아이콘 칸 단언 추가) · `i18n.js` · `nav.js` 갱신

</details>

<details>
<summary><b>v2.39.1</b> — 2026-09-09 · 수정</summary>

v2.39.0 이 고쳤다고 적은 재로그인 실패가, dev 배포로 실제 확인해 보니 그대로였다.

### 수정
- **로그아웃 뒤 로그인이 매번 `auth/internal-error` 로 실패하던 문제, 다시 수정** (`components/auth.js`) — v2.39.0 은 "로그아웃 정리가 끝나기 전에 팝업이 뜨는 타이밍 문제"로 보고 잠깐 쉰 뒤 같은 팝업 방식으로 한 번 더 시도했는데, dev 배포 후 실제로 확인해 보니 재시도도 매번 똑같이 실패했다 — 타이밍이 아니라 팝업 창과의 통신 자체가 막힌 경우였다. GitHub Pages 응답 헤더를 직접 확인해 `Cross-Origin-Opener-Policy` 는 원인이 아님을 배제했고, 정확한 브라우저 쪽 원인(서드파티 저장소 차단·확장 프로그램 등)은 기기마다 달라 특정하지 않는 대신, 이미 있던 "팝업이 막힌 경우(`popup-blocked` 등) 리다이렉트로 전환" 처방을 `auth/internal-error` 에도 그대로 적용했다 — 창 사이 통신이 필요 없는 리다이렉트는 이 종류의 문제를 안 탄다. 리다이렉트까지 실패하면 그 오류를 그대로 안내한다(조용히 실패하지 않는다). `tests/e2e/auth-retry.js`(6항목, 시나리오를 재시도 확인 → 리다이렉트 전환 확인으로 다시 씀)

</details>

<details>
<summary><b>v2.39.0</b> — 2026-09-09 · 변경 · 수정 · 불변</summary>

카드 그리드 열 수를 화면 폭에 맞춰 자동으로 조절하고, 그리드·리스트 토글 버튼 위치를 다듬었다.
로그아웃 직후 재로그인이 실패하던 문제도 고쳤다.

### 변경
- **카드 그리드 열 수를 폭에 맞춰 자동 조절** (`styles/components/pages.css`, `styles/components/list.css`) — D-MAX·레이드·PvE·PvP·레이드 보스·알 부화의 카드 그리드가 고정 열 수(PC 4열, 일부는 3열 고정) 대신 `grid-template-columns: repeat(auto-fill, minmax(260px, 1fr))` 를 쓴다. 태블릿(본문 838px 안팎) 3열, PC(1178px 안팎) 4열이 나오고, 상세 패널이 열려 본문이 좁아지면(태블릿 374px·PC 646px 안팎) 1~2열로 자연히 줄어 겹치지 않는다. 미디어 쿼리를 늘리는 대신 그리드 자체가 폭에 맞춰 계산하게 둔다
- **그리드·리스트 토글 버튼을 안내 문구와 같은 줄, 오른쪽 끝으로** (`components/gameday.js`, `styles/components/pages.css` `.gameday__intro`) — 레이드 보스·알 부화 화면에서 안내문 아래 홀로 떠 있어 가독성이 떨어지던 토글 버튼을 재배치. 좁은 화면은 지금처럼 위아래로 쌓인다

### 수정
- **로그아웃 직후 곧바로 로그인하면 `auth/internal-error` 로 실패하던 문제** (`components/auth.js`) — Firebase Auth SDK 가 로그아웃 때 지우는 내부 저장소(IndexedDB) 정리가 끝나기 전에 로그인 팝업이 뜨는 드문 경쟁 상태였다. 이 오류를 받으면 잠깐 쉬고 한 번 더 시도하도록 고쳤다. 재시도까지 실패하면 새로고침을 안내한다(조용히 실패하지 않는다). `tests/e2e/auth-retry.js`(신규 6항목)가 재시도 성공·재시도까지 실패·기존 취소 처리(`auth/cancelled-popup-request`) 회귀를 모두 확인한다

### 불변
- 모바일(1099px 이하) 배치는 그대로

</details>

<details>
<summary><b>v2.38.0</b> — 2026-09-09 · 변경 · 수정 · 불변</summary>

"PC 는 1440 기준으로, 대신 태블릿 1100 을 넣어 달라"는 요청 — 한 단계였던 넓은 화면을 둘로 나눴다.
그 과정에서 상세 패널 여백 버그도 함께 잡혔다.

### 변경
- **태블릿(1100px~)·PC(1440px~) 2단계 레이아웃** (`styles/components/app-shell.css`, `dom.js`, `components/app-shell.js`, `components/modal.js`)
  - 컨테이너 반폭·패널 폭·패널 간격을 `--shell-half`·`--panel-w`·`--panel-gap` 세 CSS 변수로 뺐다. `@media (min-width:1100px)` 블록이 태블릿 값(550/420/24)을 정의하고 그 안의 사이드바·패널·패딩 계산식이 전부 이 변수를 참조한다. `@media (min-width:1440px)` 블록은 PC 값(720/480/32)으로 **변수만 다시 정의** — 계산식은 CSS 커스텀 프로퍼티라 값이 바뀌면 자동으로 다시 계산되므로, 규칙을 또 적을 필요가 없다
  - 사이드바 유무·목록의 줄/카드 갈래는 두 단계가 같다(태블릿이 곧 지금까지의 "PC" 배치) — 임계값 하나(`wideCards()`·`wideScreen`·`useDetailPanel()`, 전부 1024→1100)로 충분해 새 JS 분기는 만들지 않았다
  - 1024~1099px 는 이제 모바일 배치로 넘어간다(예전엔 "PC"였다) — 아주 좁은 태블릿·접이식 기기 화면 폭이 이 구간이라면 배치가 달라진다

### 수정
- **상세 패널이 목록에 거의 닿아 보이던 버그** — `body.has-detail-panel` 의 `padding-right` 를 "패널 폭 + 간격"으로만 계산했는데, 패널의 `right` 위치 공식(`max(20px, calc(50vw - 반폭 + 20px))`) 자체가 컨테이너 진짜 오른쪽 끝보다 20px 안쪽에서 시작한다 — 왼쪽 사이드바가 컨테이너 왼쪽 끝보다 20px 안쪽에서 시작하는 것과 거울 대칭이라 생기는 값인데, 이 20px 을 빼먹어 실제 간격이 의도한 24px 이 아니라 4px 였다. `padding-right: panel-w + gap + 20px` 로 고쳤다. `tests/e2e/breakpoints.js`(신규 27항목)가 태블릿·PC 각 폭에서 실측 간격·좌우 대칭까지 확인한다

### 불변
- 모바일(1099px 이하는 새로 이 폭까지 확장) 배치는 그대로. 태블릿 값(550/420/24)은 이전 "PC" 수치와 동일 — 1100~1439px 사용자는 크기 변화를 못 느낀다

</details>

<details>
<summary><b>v2.37.0</b> — 2026-09-09 · 추가 · 변경 · 불변</summary>

GA4 데이터에서 헤드리스 브라우저 신호를 확인해 집계에서 뺐고, PC 레이아웃을 두 차례 더 다듬었다.

### 추가
- **봇 트래픽 집계 제외** (`track.js`)
  - 최근 28일 GA4 활성 사용자 26명 중 9명이 화면 정확히 800×600(헤드리스 기본 뷰포트) · 구글 데이터센터 소재지(Council Bluffs·Boardman·Frankfurt)에서 잡혔다. 실사용자 17명은 한국 여러 도시·실기기 해상도(393×852 등)로 분산 — 뚜렷이 갈린다
  - `IS_BOT_LIKE` — `navigator.webdriver` · 화면 정확히 800×600 · UA 에 headless/puppeteer/playwright/bot/crawler/spider 포함(대소문자 무시), 셋 중 하나만 있어도 참(OR). GA4 초기화(gtag 로드)보다 먼저 확인해야 해 번들 맨 앞(track.js)에 둔다. 콘솔에 남기지 않는다 — 남기면 봇이 우회를 학습할 수 있다
  - `track()`·`setTrackingUser()` 가 이 플래그면 gtag 를 안 부른다 — 집계만 빠지고 사이트 기능은 그대로 동작한다(차단이 아니다)
  - **범위를 의도적으로 좁혔다**: 원 지침은 "Firestore 쓰기(로그인, favs 저장 등)도 no-op" 을 제안했지만, 이 저장소의 실제 Firestore 쓰기(즐겨찾기·내 포켓몬·가입 요청)는 전부 사용자가 저장한 실 데이터이지 "트래킹 로그"가 따로 있는 게 아니다. 그걸 no-op 하면 "사이트 기능은 봇에게도 정상 동작해야 한다"는 지침의 대전제와 정면으로 부딪히고, 이 저장소의 e2e 스위트 자체가 Playwright(늘 `navigator.webdriver=true`)로 돈다 — 즉 우리 자신의 회귀 검사가 즐겨찾기 저장을 검증할 때마다 "봇"으로 잡혀 죽은 코드를 검증하게 된다. 그래서 Firestore 쓰기는 건드리지 않고 GA4 신호(track/setTrackingUser)만 막았다
  - `tests/e2e/bot-filter.js`(9항목) 추가 — webdriver·화면·UA 각 신호 단독으로도 판정되는지, 판정이어도 기능은 살아있는지, 콘솔에 안 새는지 확인. 이 스위트 자체가 Playwright 라 "정상 브라우저"는 `navigator.webdriver` 를 일부러 덮어써 흉내 낸다
- **그리드 ↔ 리스트 토글 확장** (`components/ui.js` `layoutInitial`/`layoutToggle`, `components/pages.js`·`favs.js`·`gameday.js`)
  - 도감에만 있던 보기 전환 버튼을 즐겨찾기·레이드 보스·알 부화로 뗐다. 화면마다 localStorage 키를 따로 써 선택이 안 섞인다(`pogo_favs_cols`·`pogo_raids_cols`·`pogo_eggs_cols`, 도감의 `pogo_dex_cols` 는 값·이름 불변)
  - 레이드 보스·알 부화는 티어·거리별로 목록이 여러 묶음이라, `gamedaySection()` 이 각자 `wideCards()` 를 묻던 것을 그만두고 그리드 여부를 인자로 받아 토글 버튼 하나가 전부를 같이 바꾸게 했다
  - `tests/e2e/layout-toggle.js`(19항목) 추가
- **PC 메뉴 재구성** (`index.html`, `components/app-shell.js` `placeDrawerExtra`, `styles/components/app-shell.css`)
  - PC(1024px~)에서 ☰ 메뉴엔 마이페이지·기준 안내·트레이너 코드만 남는다. 패치노트·QA 제보·개인정보처리방침·이용약관·통계 설정(`index.html` `#drawer-extra`)은 왼쪽 사이드바로, 이동 목록(알 부화가 마지막) 아래에 통째로 옮긴다 — 복제가 아니라 이동이라 `onclick`·`id` 등 기존 연결이 그대로 간다(`destinations` 이동과 같은 방식)
  - 옮긴 자리는 구분선 하나로 위 이동 목록과 갈라내고 글자를 한 단 낮춰(뮤트 색·작은 글자) "보조 항목" 임을 보이게 했다(`.app-nav__extra`)
  - PC 는 왼쪽 사이드바가 늘 있어 상단 바의 ← 뒤로가기가 필요 없다 — `.app-bar__head .icon-btn` 를 PC 에서 숨겼다. 좁은 화면은 그대로
  - `tests/e2e/shell.js` 에 관련 검사 6항목 추가(PC 는 3항목만 보임·사이드바 보조 구역 존재·뒤로가기 숨김, 모바일은 회귀 없음)

### 변경
- **상세 패널 여백 확대** (`styles/components/app-shell.css`) — 타입 상성 칩 등이 테두리에 닿을 만큼 좁다는 지적으로 `.detail-panel__body` 패딩을 사방 10px 씩 늘렸다(위 4→14px, 좌우·아래 20→30px). 안쪽 실제 내용 폭이 좁아지지 않도록 패널 폭도 400→420px 로 같이 늘렸다

### 불변
- 즐겨찾기·내 포켓몬 저장, 로그인 등 실제 Firestore 쓰기는 봇 판정과 무관하게 그대로 동작한다
- 도감의 `.dex__layout` 클래스·`pogo_dex_cols` 키는 그대로. 좁은 화면의 ☰ 메뉴 구성·뒤로가기 버튼은 그대로

</details>

<details>
<summary><b>v2.36.0</b> — 2026-09-09 · 추가 · 수정 (구현 중 발견) · 불변</summary>

PC 는 오른쪽에 남는 자리가 많다는 지적 — 상세를 화면을 덮는 팝업 대신 목록 옆 패널로 열어 본다.

### 추가
- **PC 오른쪽 상세 패널** (`components/modal.js` `openDetailPanel`/`closeDetailPanel`/`useDetailPanel`, `components/detail.js`, `index.html`, `styles/components/app-shell.css`)
  - 넓은 화면(1024px~)에서 `openDetail()` 이 `openModal()` 대신 목록 오른쪽 고정 패널(`#detail-panel`)에 내용을 채운다. 모달과 달리 화면을 덮지 않아 목록을 계속 보고 누를 수 있다 — 그래서 스크롤 잠금·히스토리 오버레이 항목을 쓰지 않는다
  - 패널 자리는 왼쪽 고정 사이드바(`.app-nav`)와 같은 530px 반폭 상수로 거울 대칭(`right: max(20px, calc(50vw - 530px))`) — 사이드바·컨테이너 폭 공식은 손대지 않았다. 패널이 열려 있을 때만 `body.has-detail-panel` 클래스로 본문에 `padding-right`를 줘 자리를 비킨다. 카드 격자(`.row-list` 등)는 전부 `1fr` 기반이라 폭이 좁아지면 카드가 그만큼 좁아질 뿐 넘치지 않는다 — 그래서 컨테이너 자체를 넓히는 대신 이 방법을 썼다
  - 닫기(✕)는 스크롤 영역 밖의 자기 줄(`.detail-panel__bar`)에 둔다 — 안에 두면 v2.32~2.34.0 에서 겪은 "닫기가 내용과 자리를 다투는" 문제가 되풀이된다
  - 다른 화면으로 이동하면(hashchange) 패널을 자동으로 닫는다 — 다른 화면 옆에 이전 포켓몬이 남아 있으면 헷갈린다. 같은 화면에서 다른 포켓몬을 열 때는 내용만 바뀐다(패널이 여러 개로 쌓이지 않는다)
  - `#detail-panel` 은 스크립트가 만들지 않고 `index.html` 에 처음부터 있다(`#page`·`#drawer-backdrop` 과 같은 자리) — `#/mon/<id>` 딥링크 첫 렌더가 `app-shell.js` 보다 먼저 실행돼, 스크립트가 그때 막 조각을 만들면 자리가 없어 상세가 조용히 사라지는 문제가 있었다
  - 좁은 화면(1024px 미만)은 지금까지처럼 팝업(다이얼로그) — 패널 CSS 자체가 그 폭에서 안 먹는다

### 수정 (구현 중 발견)
- `useDetailPanel()` 을 처음엔 `app-shell.js` 의 `wideScreen`(같은 `matchMedia`, `const`)을 재사용하게 짰는데, `#/mon/id` 딥링크로 처음 들어오면 `pages.js` 의 첫 렌더가 `app-shell.js` 보다 먼저 이 함수를 불러 `wideScreen` 이 선언되기 전(TDZ)이라 `ReferenceError` 가 났다. `typeof` 로도 못 피한다 — TDZ 인 `let`/`const` 는 `typeof` 도 던진다. `matchMedia` 를 그때그때 직접 불러 외부 변수 의존을 아예 없앴다
- 위 딥링크 경로에서 `#detail-panel` 이 아직 없어 상세가 열리지 않고 사라지던 문제 — 정적 HTML 로 옮겨 해결(위 항목)

### 불변
- 모달(`openModal`)·기존 상세 내용·이벤트명은 그대로. 넓은 화면에서 "어디에 뜨는지"만 바뀌었다

</details>

<details>
<summary><b>v2.35.0</b> — 2026-09-09 · 변경 · 불변</summary>

사용자가 준 참고 배치대로 상세 팝업 헤더를 한 번 더 다듬었다 — 타입은 그림 위 배지로, ★는 아이콘 줄로, 폼 라벨은 이름 위 자기 줄로.

### 변경
- **타입을 이름 줄에서 그림 위 모서리 배지로** (`components/detail.js`, `styles/components/modal.css`)
  - `.detail__types` 를 `.detail__info` 가 아니라 `.sprite-box` 안으로 옮기고 `position: absolute; top: -6px; left: -6px` 로 그림 왼쪽 위 모서리에 살짝 겹치게 뗐다. 두 타입이면 세로로 쌓는다(`flex-direction: column`). 그림 색이 다양해 늘 또렷하도록 그림자(`box-shadow`)를 얹었고, 배지가 자리를 적게 차지하도록 패딩·글자 크기를 줄였다
- **★ 즐겨찾기를 공유·저장과 한 줄로** — 이름 옆(`.detail__name-row`, 이번에 없앴다)이 아니라 `.detail__top-actions` 에 `favBtn()` 을 그대로 이어 붙였다. `.detail__top-actions .fav` 스코프 선택자로 공유·저장과 같은 동그란 칩 모양을 덧입혔다 — `list.css` 의 기본 `.fav`(민무늬 별)보다 구체적인 선택자라 스타일시트 순서와 무관하게 이긴다
- **폼 라벨을 이름 줄에서 떼어 이름 위 자기 줄로** — `nameNode()`(폼 라벨+이름을 한 조각으로 합치는 공용 조립 함수)를 이 헤더에서는 쓰지 않고, `splitFormName()` 으로 라벨과 종 이름을 직접 나눠 `.detail__form-row`(라벨)와 `<h2>`(이름)를 별도 줄로 그린다. 영문명(`.detail__en-inline`)도 이름 옆 괄호 대신 그 아래 자기 줄(`display: block`)로

### 불변
- 타입·즐겨찾기·폼 라벨이 담는 정보와 동작은 그대로. 자리만 옮겼다
- 다른 화면(순위표·진화 단계 등)의 `nameNode()` 출력은 이 변경과 무관 — 상세 헤더만 따로 조립하도록 바꿨다

</details>

<details>
<summary><b>v2.34.0</b> — 2026-09-09 · 변경 · 불변</summary>

v2.33.0 은 ✕ 닫기 자리를 카드 안에서 예약해 겹침을 없앴다. 아예 카드 밖으로 빼면 더 근본적으로 해결된다.

### 변경
- **✕ 닫기를 카드 밖으로** (`components/modal.js`, `styles/components/modal.css`)
  - `.modal__close` 를 `.modal__box`(카드) 안이 아니라 그 부모인 새 `.modal__wrap` 에 절대 위치로 둬, 카드 오른쪽 위 모서리 바깥에 띄운다. 카드는 스크롤돼도 자신은 움직이지 않으므로 v2.33.0 의 sticky 자리 예약(`.modal__bar`)이 통째로 필요 없어졌다 — 카드 안에서 뭘 어떻게 배치하든 이제 겹칠 자리 자체가 없다
  - ✕ 가 카드 밖으로 나가며 카드 영역이 배경과 덜 또렷해질 수 있어, 뒤 배경 어둡기를 올렸다(`rgba(0,0,0,.45)` → `.64`)
- **CP 100% 기준 표를 아코디언으로** (`components/detail.js`, `styles/components/modal.css`)
  - `.detail__cp-card` 를 `<details>` 로 바꿨다 — 큰 숫자(`<summary>`)는 접혀도 항상 보이고, 레이드·부스트·야생·부스트 2×2 표만 접힌다. 다른 아코디언(포획 CP·CP 계산기)과 같은 꺾쇠 규칙, 기본은 닫힘

### 불변
- ✕ 닫기·CP 카드의 동작(닫기·숫자 계산)은 그대로. 자리와 기본 펼침 상태만 바뀌었다

</details>

<details>
<summary><b>v2.33.0</b> — 2026-09-09 · 수정 · 불변</summary>

v2.32.0 이 새로 만든 오른쪽 위 아이콘 두 개가, 원래 있던 ✕ 닫기와 자리를 다퉜다.

### 수정
- **상세 팝업 ✕ 닫기와 공유·저장 아이콘이 겹쳐 안 눌리던 문제** (`styles/components/modal.css`)
  - `.modal__close` 는 `height: 0` 인 sticky 바에서 `transform: translate(8px, -8px)` 로 떠 있었다 — v2.32.0 이전엔 그 자리에 아무것도 없어 문제가 없었지만, 새로 생긴 `.detail__top-actions`(공유·저장, 역시 우상단)와 자리가 겹치면서 두 요소가 서로를 가렸다
  - `.modal__bar` 가 padding 으로 실제 높이를 갖게 고쳤다 — 이제 뒤에 오는 내용이 그 높이만큼 자연스레 밀려나므로 겹칠 자리 자체가 없다. 헤더 위 여백도 더 넉넉히 뒀다
  - 스크롤해서 sticky 로 붙었을 때 아래 내용이 비쳐 보이지 않게 `.modal__bar` 에 배경색을 추가했다

### 불변
- ✕ 닫기·공유·저장의 동작은 그대로. 자리만 겹치지 않게 다시 잡았다

</details>

<details>
<summary><b>v2.32.0</b> — 2026-09-09 · 변경 · 추가 · 불변</summary>

상세 팝업 위쪽(그림 · 이름 · 타입 · CP)을 사용자가 준 참고 디자인에 맞춰 카드형으로 다시 짰다.

### 변경
- **헤더 구조 재배치** (`components/detail.js` `openDetail`, `styles/components/modal.css`)
  - `#도감번호` 배지 · 이름+★ 즐겨찾기 한 줄(`.detail__name-row`) · 타입 알약(`.detail__type-pill`)을 `.detail__info` 한 칸에 모았다. ★ 는 기존 오른쪽 세로 버튼 묶음에서 이름 옆으로 옮겼다 — "이 이름의 종을 즐겨찾기에 담는다"는 동작이 이름과 붙어 읽힌다
  - 공유(🔗)·내 개체로 저장(➕, 로그인 시)은 오른쪽 위 동그란 아이콘 두 개(`.detail__top-actions`)로. 공유를 누르면 "복사됨 ✓" 로 글자가 길어지는데, 원을 고정 너비 대신 `min-width` 알약으로 둬서 글자가 원 밖으로 새지 않는다
  - 타입은 상성표의 작은 점 칩(`.tchips__item`)과 다른 새 클래스(`.detail__type-pill`)로 — 타입 색을 배경으로 채운 굵은 알약이다. 상성표 칩을 그대로 키웠다면 이중약점 강조 같은 다른 화면의 뜻도 같이 흔들렸을 것이다
  - 그림 상자(`.sprite-box`)에 `var(--fg)` 를 옅게 섞은 동심원 배경을 추가했다 — 라이트·다크 각각 값을 안 적어도 두 테마 모두에서 자연스럽다
- **CP 를 문장에서 카드로** — `.detail__cpline`(한 줄 문장) 대신 `.detail__cp-card`: 만렙 CP 큰 숫자 하나 + 레이드·부스트·야생·부스트 2×2 표(`.detail__cp-tile`). 안쪽 칸은 카드보다 한 단 깊은 색(`var(--bg)`)을 써서 `party-card`·`boss__rec` 과 같은 "카드 위 카드" 규칙을 그대로 따른다
- **포획 CP·CP 계산기를 "누르면 열리는 행"으로** — 아이콘 + 굵은 이름 + 오른쪽 꺾쇠(`›`), 열리면 꺾쇠가 90도 돌아 아래를 가리킨다. 인터랙션은 그대로 `<details>/<summary>` 라 접근성·동작은 안 바뀌었다. 포획 CP 행은 가장 먼저 보는 표라 `.detail__acc--catch` 로 살짝 강조했다

### 추가
- `tests/e2e/detail.js` — 헤더 재배치 회귀 16항목 (배지·이름·타입 알약·아이콘 겹침 여부·CP 카드 숫자·아코디언 여닫기·복사됨 상태 오버플로·메가 폼 호환까지)

### 불변
- 즐겨찾기·공유·내 개체 저장의 동작·이벤트명은 그대로. 모양만 바뀌었다
- 메가·다이맥스/거다이맥스·섀도우 그림 테두리 색(`.sprite-box--*`)은 그대로 — 새 동심원 배경 위에 그대로 얹힌다

</details>

<details>
<summary><b>v2.31.0</b> — 2026-09-08 · <code>변경</code> 수정 · 포켓몬 그림 테두리 통일 · 불변</summary>

포켓몬 그림은 어디서든 같은 상자에 담기고, 레이드 보스 목록은 이름이 잘리지 않는다.

### 수정
- **레이드 보스 목록에서 이름이 중간에서 줄바꿈되던 문제.** `gamedayRow`(`components/gameday.js`)가 이름(`b`)과 조건 문구(`.gameday__note`)를 그림 옆 한 줄에 나란히 뒀는데, 문구가 `margin-left: auto` 로 오른쪽에서 자리를 밀어붙이자 이름 칸이 좁아져 "레지락" 같은 두세 글자 이름도 음절 사이에서 잘렸다(한글 음절 사이엔 브라우저가 기본으로 줄바꿈을 허용한다)
  - `favs.js` 의 `fav__main` 과 같은 모양으로 고쳤다 — 이름과 문구를 `.gameday__main` 안에 넣어 **세로로 쌓는다.** 각자 줄 전체를 쓰므로 다툴 일이 없다

### 변경 — 포켓몬 그림 테두리 통일
그리드(카드) 모드에만 있던 그림 상자를, 상자가 없던 나머지 화면에도 같이 적용한다.

- **목록 줄 모드** (`.dex__row .sprite`) — 도감·즐겨찾기·레이드 보스·알 부화가 전부 이 줄을 쓴다(gameday.js 상단 주석 "도감 목록과 같은 성격의 줄이라 같은 모양이어야 한다"). 줄 배경이 페이지 배경(`var(--bg)`)과 같아 그림이 배경에 그냥 떠 있었다 — `var(--surface)` 로 대비를 낸 상자를 준다
- **진화 단계** (`.evo__mon`, modal.css) — 이전 단계는 `border: 1px solid transparent`(보이지 않는 테두리)라 그림이 붕 떠 보였다. 모든 단계에 같은 상자를 주고, "지금 보는 이 포켓몬"(`is-now`)만 테두리 색(`var(--fg)`)과 글자 굵기로 구분한다 — `역할-chip.is-on` 등 이 서비스가 이미 쓰는 "선택됨" 관례(테두리색 + 글자 굵기)를 그대로 따랐다
- **추천 딜러 카드** (`.boss__rec`, list.css) — 카드 배경이 `var(--bg)`(페이지 배경과 같은 값)라 카드 자체가 안 보였다. `var(--surface)` 배경 + 테두리로 바꿔 카드꼴이 드러나게 했다

### 불변
- 카드/그리드 모드의 그림 상자는 그대로. 좁은 화면 목록 레이아웃도 한 줄도 바뀌지 않았다(그림 크기·행 높이만 상자 두께만큼 살짝 커짐)

</details>

<details>
<summary><b>v2.30.0</b> — 2026-09-08 · 추가 · 변경 · 수정 외 1건</summary>

주소가 곧 메뉴 구조가 되고, 그 주소를 읽는 곳이 한 곳이 된다.

### 추가
- **`frontend/scripts/router.js` — 라우트 표 한 곳** — 같은 주소를 네 곳이 각자 정규식으로 읽고 있었다(`pages.js` `/^#\/(\w+)/` · `planner/shell.js` `/^#\/plan…/` · `app.js` `'#/rank/'+id` · `app-shell.js` `APP_DESTINATIONS`). 주소 체계를 한 줄 바꾸면 네 곳을 같이 고쳐야 했고, 한 곳을 놓치면 "화면은 바뀌는데 제목만 안 맞는" 식으로 어긋났다. 이제 `ROUTES` 가 원본이고 나머지는 전부 이 표를 본다
  - 메뉴(`ROUTE_NAV`)·페이지 판정(`currentPageId`)·플래너 판정(`planRouteFromHash`)·탭 이동(`routeHash`)·상세 딥링크가 모두 같은 표에서 나온다
- **`frontend/scripts/components/ui.js` — 재사용 조각** — `uchip` · `iconBtn` · `pageBody` · `sectionTitle` · `footNote` · `hintNote` · `metaText`. 두 곳 이상에서 똑같이 쓰이고 모양이 클래스 하나로 끝나는 것만 올린다
- `tests/e2e/router.js` — 주소·옛 주소·식별자 회귀 44항목

### 변경
- **주소가 메뉴 이름과 1:1** — `#/rank/max`→`#/dmax` · `#/rank/pve`→`#/pve` · `#/rank/pvp`→`#/pvp` · `#/plan`→`#/planner` · `#/plan/collection`→`#/planner/collection`
  - **옛 주소는 버리지 않는다.** 이미 공유·북마크된 링크라 `legacy` 로 남겨 두고 새 주소로 돌린다. 돌리는 것은 `location.replace` 라 **뒤로가기 기록에 옛 주소가 쌓이지 않는다** — 한 번 눌러 원래 화면으로 돌아간다
  - `navigateHash` 도 옛 주소를 정규화한다. 내부 호출을 하나 놓쳐도 새 주소로 간다
- **좁은 화면도 넓은 화면과 같은 헤더 규칙** — 상단 바 = 로고 + 뒤로가기, 화면 이름 = 본문 맨 위 헤더(`.page-head`). 상단 바가 화면 이름을 겸하면 로고가 사라져 "지금 어느 서비스인지"와 "처음으로 가는 길"이 함께 없어졌다
- **로고를 누르면 서비스 홈** — 웹의 오랜 약속을 그대로 쓴다. `h1#app-title` 은 화면 이름을 읽어 주는 포커스 자리라 그대로 두고 안쪽에 버튼(`#app-logo`)을 넣었다
- **화면·상세에 측정용 식별자** — `body[data-route]` · `#page[data-route]` · `#page-<id>` · 상세는 `#detail-<스프라이트>` + `data-mon`·`data-form`·`data-view`(도감에서 열었나 목록에서 열었나). 상세는 주소가 `#/mon/<id>` 하나뿐이라 GA·히트맵에서 전부 한 덩어리로 뭉쳐 있었다. 라우트 전환마다 `route_view` 이벤트도 남긴다(기존 이벤트명은 그대로)

### 수정
- 재사용 조각의 이름을 `foot`·`hint` → `footNote`·`hintNote` 로. 짧게 뒀더니 `detail.js` 의 지역 변수 `const foot = [...]` 이 그대로 가려 상세 팝업이 열리지 않았다
  - 교훈: **번들이 한 `<script>` 라 전역 함수는 어디서든 보이지만, 같은 이름의 지역 변수가 있으면 그 함수 안에서는 진다.** 전역에 흔한 낱말을 두지 않는다

### 불변
- `state` 키 · DOM id · GA 이벤트명 · Firestore 필드명 · localStorage 키 그대로 (`route_view` 만 신규)
- 옛 주소로 들어오는 링크는 전부 살아 있다

</details>

<details>
<summary><b>v2.29.2</b> — 2026-09-08 · 변경 · 불변</summary>

### 변경
- **도감 보기 전환 버튼이 "열 개수" 대신 "보기 방식" 을 말한다** — `☰ 1열` · `⊞ 2열` → `☰ 리스트` · `⊞ 그리드`
  - 열 개수는 화면 폭이 정한다(2 → 3 → 4열). 라벨에 `2열` 이라고 박아 두면 넓은 화면에서 **그냥 틀린 말**이 된다. 버튼은 화면이 실제로 하는 일만 말해야 한다
  - 라벨은 **지금 어떤 보기인지**를 보여 준다. 누르면 무엇이 되는지는 `aria-label`(`보기 방식: 그리드 · 누르면 리스트`)이 맡는다 — 글자 자리가 좁은 칩에서 상태와 행동을 둘 다 적으면 어느 쪽도 안 읽힌다

### 불변
- `localStorage.pogo_dex_cols` 의 키 이름과 값(`'1'` · `'2'`)은 그대로다. 이미 나간 키라 뜻만 바뀌고 저장된 선택은 계속 살아 있다

</details>

<details>
<summary><b>v2.29.1</b> — 2026-09-08 · 수정 · 변경</summary>

### 수정
- **플래너 「내 포켓몬」 목록이 깨져 보이던 문제.** v2.28.0 의 PC 카드 규칙을 `.row__name` · `.row__moves` · `.row__stats` 처럼 **맨 클래스**로 적었는데, 이 조각들은 랭킹 줄만 쓰는 게 아니다 — `.plan__mon-main`(플래너)과 `.boss__rec`(보스 추천 카드)이 같은 클래스를 빌려 쓴다. "이름 가운데 정렬" 같은 카드 전용 규칙이 그쪽까지 끌려가 이름이 행 가운데로 밀렸다. 카드 규칙을 전부 `.row-list > .row` 아래로 스코프했다
  - 교훈: **여러 블록이 빌려 쓰는 클래스에 맨 이름으로 규칙을 걸면, 그 규칙은 반드시 남의 화면으로 샌다.** 조건부(미디어 쿼리) 규칙일수록 새는 것을 늦게 발견한다
- 헤더 👤 를 없앤 뒤에도 남아 있던 로그인 안내 문구 3곳(`planner/collection.js` · `planner/home.js` · `components/privacy.js`)을 "☰ 메뉴 맨 위 마이페이지" 로 고쳤다
- `.i18n-note` 의 표시/숨김 규칙에 `:root` 를 붙여 특정도를 올렸다 — 뒤 파일의 `.note`(단일 클래스)와 같은 값이면 순서로 진다

### 변경
- **영어 화면의 일정표에 "한국 서버(KST, UTC+9) 기준" 을 먼저 밝힌다.** 번역 여부보다 **어느 지역 일정인지**가 먼저다 — 포켓몬 GO 이벤트는 지역마다 날짜·시간이 다르고, 영어로 보는 사람이 자기 지역 일정으로 오해하면 실제로 이벤트를 놓친다. 전체 화면과 드로어 미리보기 양쪽에 단다(`i18nKoOnlyNote('kst')`)
  - 레이드 보스·알 부화의 지역 표기는 옮기지 않는다 — 각주가 이미 "지역과 이벤트에 따라 실제와 다를 수 있습니다"를 영어로 밝힌다

</details>

<details>
<summary><b>v2.29.0</b> — 2026-09-08 · 추가 · 변경 · 번역하지 않는 것 (일부러) 외 1건</summary>

영어로도 읽히게 한다. **화면 문구는 사전이, 이름은 데이터가 맡는다** — 이 경계가 이 버전의 전부다.

### 추가
- **KR/EN 전환** — 헤더 오른쪽 위 토글. 고른 언어는 `localStorage.pogo_lang` 에 남고, `<html lang>` 도 함께 바뀐다
- **`frontend/scripts/i18n.js` (엔진)** — 화면 문구를 코드 안에 한국어로 적는 구조(35개 파일·문자열 1,000개 남짓)를 그대로 두고, **원문을 키로 써서 그려진 뒤에 훑는다**. 전부를 `t('key')` 로 바꾸면 파일마다 손이 가고 한 곳만 빠져도 그 줄이 한국어로 남는다. `MutationObserver` 가 새로 붙는 가지만 훑으므로 도감 청크·상성 결과·검색 결과처럼 부분 렌더되는 곳도 따로 부르지 않아도 된다
  - 찾는 순서: 사전 → 이름표 → 숫자 뼈대(`더보기 (#/#)`) → 패턴(`(.+) 상세 보기`) → 구분자 조각(`·` `/` `—`) → 낱말. 한 조각이라도 모르면 그 줄은 한국어로 남긴다 — **반쯤 영어인 줄이 제일 읽기 나쁘다**
- **`frontend/scripts/i18n-en.js` (사전)** — 화면 문구 영문. 번역이 느는 곳은 이 파일 하나다
- `tests/e2e/i18n.js` — 전환·되돌리기·부분 렌더·헤더 개편 회귀 22항목

### 변경
- **이름은 빌드가 굽는다** — 포켓몬·기술·폼·타입 이름을 사전에 손으로 적으면 1,000개가 넘고 새 종이 나올 때마다 사람이 따라 적어야 한다. 원본 표(PokeAPI `species_names.csv` 언어 9 · `move_names.csv` 언어 9)에 이미 영문이 있으므로 그대로 굽는다
  - `backend/names.py`: `en_species` · `FORM_EN`(FORM_KO 와 같은 토큰 키)
  - `backend/dex_build.py`: `dex.json` 에 `en`(도감번호→영문) · `moveKo`(한글 기술명→영문) · `formKo`(한글 폼 라벨→영문)
  - `backend/build.py`: `TYPE_EN`
  - 짝이 없으면 넣지 않는다 — **없는 영문을 지어내지 않는다** (한글 이름 규칙과 같은 원칙)
- **헤더에서 계정 버튼(👤)을 뺐다** — 로그인·마이페이지가 헤더와 ☰ 메뉴 두 곳에 있던 중복을 지우고 메뉴 맨 위 "👤 마이페이지" 한 곳으로 모았다(v2.15.1 "화면 하나에 진입 버튼 하나"). 비운 자리가 KR/EN 토글이 됐다
  - 토글 글자는 **상태가 아니라 행동**이다 — 한국어로 보고 있으면 `EN`, 영어면 `KR`. 눌러야 할 것이 분명해진다

### 번역하지 않는 것 (일부러)
- **패치노트 · 일정표** — 한국 서버 일정과 한국어 공지를 그대로 옮긴 콘텐츠다
- **개인정보처리방침 · 이용약관** — **한국어 원문이 효력을 갖는 문서**다. 옮긴 글이 원문과 어긋나면 그게 더 위험하다
- 네 화면 모두 영어로 볼 때 맨 위에 안내 한 줄이 뜬다(`i18nKoOnlyNote()`)

### 불변
- 전역 이름 · `state` 키 · DOM id · GA 이벤트명 · Firestore 필드명 그대로. 새 localStorage 키는 규칙대로 `pogo_lang`

</details>

<details>
<summary><b>v2.28.0</b> — 2026-09-08 · 변경 · 수정 · 불변</summary>

넓은 화면에서 목록을 **줄이 아니라 카드로** 훑는다. 도감 2열에서 쓰던 카드(v2.26.0)를 서비스 전체의 문법으로 올린다.

### 변경
- **랭킹 목록이 PC 에서 카드 격자** — D-MAX · 레이드 · PvE · 배틀 · PvP · 솔플 스쿼드 · 추천 덱. 모두 `components/list.js` 의 `<ul class="row-list">` 를 거치므로 `styles/components/list.css` 의 `@media (min-width: 1024px)` **한 곳**만 고쳤다. 순위는 카드 모서리로 빼고, 점수는 `margin-top: auto` 로 카드 바닥에 붙여 기술 줄 길이가 달라도 가지런하다
- **도감·즐겨찾기·레이드 보스·알 부화도 PC 에서 카드** — 이쪽은 `.dex__list.is-grid` 라는 기존 클래스가 이미 있어 CSS 를 복제하는 대신 `wideCards()`(`scripts/dom.js`) 한 함수가 붙일지 말지를 정한다. 카드 모양의 정의는 계속 `pages.css` 한 곳이다
- **도감은 PC 에서 카드가 기본** — 다만 `pogo_dex_cols` 에 저장된 선택이 있으면 그쪽이 이긴다. 넓은 화면에서 굳이 1열을 골라 둔 사람의 뜻을 기본값이 덮지 않게
- 창 폭이 카드 기준선(1024px)을 넘나들면 화면을 다시 그린다(`app-shell.js`) — 목록 모양이 폭에 달렸는데 재렌더가 없으면 늘렸다 줄일 때 이전 모양이 남는다

- **카드를 그림 칸과 글 칸으로 나눴다** — 그림에 회색 판(상세 팝업 `.sprite-box` 와 같은 뜻)을 깔고 아래에 hairline 하나. 그림과 이름이 그냥 붙어 있으면 어디까지가 그림 자리인지 눈이 잡지 못해 카드가 한 덩어리로 뭉갠다

### 수정
- **타입 & 상성에서 타입 칩을 눌러도 결과가 나오지 않던 버그** — `matchupBucket()` 이 묶음 키(`x2`·`r2`)가 아니라 CSS 클래스(`is-weak2`·`is-resist2`)를 돌려주고 있었다. v2.24.0 BEM 리네이밍이 "클래스처럼 생긴 문자열"을 한꺼번에 바꾸면서 딸려 들어갔다. `buckets['is-weak2']` 가 `undefined` → `.push` 에서 예외 → **결과 영역이 통째로 안 그려짐**. 예외는 콘솔에만 남아 화면은 조용히 아무것도 하지 않았다. 회귀 검사를 `tests/e2e/nav.js` 에 3항목 추가
  - 교훈: **한 값이 데이터 키이면서 클래스 이름이면 리네이밍이 반드시 한쪽을 깬다.** 클래스는 `bucketRow` 의 인자로 따로 넘긴다

### 불변
- **좁은 화면(1024px 미만)은 한 줄도 바뀌지 않는다.** 손가락으로 훑는 화면에서는 세로 줄이 카드보다 빠르고, 카드로 바꾸면 한 화면에 들어오는 종 수가 줄어든다
- 전역 이름 · `state` 키 · DOM id · localStorage 키 · GA 이벤트명 · Firestore 필드명 그대로

</details>

<details>
<summary><b>v2.27.0</b> — 2026-09-08 · 추가 · 변경 (보안) · 변경 (대역폭) 외 1건</summary>

공개 서비스로서의 최소 방비. 트래픽이 늘기 시작한 시점에 맞춰 **막을 수 있는 것과 막을 수 없는 것을 가른다.**

### 추가
- **공유 미리보기(OG)** — 카카오톡·디스코드·X 에 링크를 붙이면 카드가 뜬다. 그림은 `backend/og_gen.py` 가 **도트로 직접 그린다**(1200×630). 도감 스프라이트가 전부 픽셀인데 미리보기만 매끈하면 눌러 들어온 화면과 결이 어긋난다 — 글자까지 5×7 픽셀 글꼴로 찍었다. 외부 라이브러리 없이 PNG 청크를 직접 쓴다(`icon_gen.py` 와 같은 원칙)
- **검색 최적화** — `canonical` · `meta description` · OG · Twitter 카드 · JSON-LD(`WebSite`) · `sitemap.xml`. 해시 라우팅이라 색인되는 주소가 첫 화면 하나뿐이므로 **없는 하위 페이지를 지어내지 않는다**
- **`security.txt`**(RFC 9116) — 공개 저장소라 누구나 코드를 읽는다. 취약점을 발견한 사람이 찾는 표준 위치에 제보 창구를 둔다
- **`404.html`** — GitHub Pages 기본 404 대신 서비스로 돌려보내는 문. 해시 라우팅이라 오타 하나로도 여기 온다
- `tests/e2e/hardening.js` — 보안·공유·검색 회귀 32항목

### 변경 (보안)
- **CSP** — GitHub Pages 는 응답 헤더를 못 바꿔 `<meta>` 로 건다. 번들이 인라인 `<script>` 라 `'unsafe-inline'` 이 필요하지만 **외부 도메인 주입은 막힌다** — 공격자가 `<script src="악성.com">` 을 심어도 브라우저가 거부한다. `object-src 'none'`(플러그인) · `base-uri 'none'`(`<base>` 로 상대경로를 통째로 돌리는 수법) · `form-action 'none'`(폼이 없는 사이트)
- **`referrer` 정책** — 다른 사이트로 나갈 때 경로를 흘리지 않는다. 해시에 어느 화면을 보고 있었는지가 담긴다
- **외부 스크립트 출처 검사** — `loadScript()` 가 허용 목록(gstatic firebasejs)에 없는 주소를 거부한다. CSP 가 이미 막지만, 나중에 임의 주소를 넘기는 실수를 코드에서도 잡는다. 쿠키를 실어 보내지 않고(`crossOrigin='anonymous'`) 참조 주소도 넘기지 않는다
- **Firestore 쓰기 한도** — 지금까지는 본인이면 무엇이든 얼마든지 쓸 수 있었다. 승인된 사용자가 실수하거나 계정이 털리면 문서 하나로 무료 한도를 태울 수 있다. `users` 는 필드 12개·개체 300마리(클라이언트 `PLAN_MAX_MONS` 와 같은 값)·즐겨찾기 1000개, `requests` 는 필드 12개로 제한. **⚠️ 콘솔에 규칙 재게시 필요**
  - 필드 이름을 `hasOnly` 로 못 박지 않았다. `set({merge:true})` 는 `request.resource.data` 가 **합쳐진 결과**라, 예전 버전이 남긴 필드가 목록에 없으면 그 사용자는 그때부터 아무것도 저장하지 못한다. 이름 대신 **개수와 크기**로 막는다

### 변경 (대역폭)
- **`robots.txt` 에서 무거운 산출물 제외** — `data.js`(약 1.4MB)와 `sprites/`(1,100여 장)는 검색에 의미가 없는데 크롤러가 통째로 받으면 GitHub Pages 대역폭만 먹는다. 첫 화면만 색인하면 충분하다
- **dev 미리보기의 공유 카드가 실서비스를 가리키지 않게** — 빌드가 `canonical` · `og:url` · `og:image` · `sitemap` 을 dev 주소로 바꾼다. 색인은 어차피 막지만, 링크를 붙였을 때 엉뚱한 곳으로 가는 사고를 막는다

### 참고 — 트래픽 급증의 정체
GA4 에서 이벤트 1,363건 · 사용자 25명(28일)이 잡혔다. **봇이 아니다.** 크롤러는 `tab_start`(217) · `detail_open`(118) · `login`(49) 같은 상호작용 이벤트를 만들지 못한다. 활성 사용자당 평균 참여 시간 3분 12초, DAU/MAU 72% 도 사람의 패턴이다. 서비스를 친구들에게 알린 시점(9/1 전후)과 그래프가 시작되는 지점이 일치한다.
다만 GA 속성을 다른 프로젝트와 함께 쓰고 있어 `CODE DIARY`(4) · `POGO NOTE`(4) 조회수가 섞여 들어온다 — 이 사이트의 수치가 아니다.

</details>

<details>
<summary><b>v2.26.0</b> — 2026-09-08 · 수정 · 변경 · 추가</summary>

### 수정
- **PC 에서 스크롤이 막히던 버그** — 커서가 탭 줄(D-MAX · PvE · PvP) 위에 있으면 휠이 페이지로 가지 않았다. 원인은 `.tabs { overflow-x: auto }` 다. CSS 규칙상 **한 축이 `visible` 이 아니면 나머지 축의 `visible` 은 `auto` 로 계산**되므로 세로도 스크롤 컨테이너가 된다. 거기에 선택된 탭의 밑줄이 컨테이너 테두리를 덮으려고 `margin-bottom: -1px` 로 1px 삐져나와 있어, 탭 줄이 "세로로 스크롤할 게 1px 있는 영역"이 됐다. 브라우저는 그 1px 을 먼저 소비하고 페이지로 넘기지 않는다. PC 는 즐겨찾기 카드가 커 탭 줄이 화면 한가운데 와서 커서가 자연히 그 위에 놓였고, 모바일은 손가락으로 굴려 드러나지 않았다.
  고치는 법은 세로 넘침을 없애는 것 — 컨테이너 테두리를 `inset` 그림자로 바꿔 안쪽에 그리고 탭의 음수 여백을 뺐다. 겉모습은 그대로다. 같은 덫이 있던 `.chips` · `.boss__recs` · `.priv__table-wrap` 에도 `overflow-y: hidden` 을 명시했다
- **스크롤 잠금이 남을 수 있던 구조** — 팝업·드로어·검색을 열고 닫는 자리마다 `document.body.style.overflow` 를 직접 켜고 껐다. 닫는 경로를 하나라도 놓치면 잠금이 남아 페이지가 영영 스크롤되지 않고, 사용자가 되돌릴 방법도 없다. 이제 `syncScrollLock()` 이 **열린 `<dialog>` 이 있는가** 하나에서만 잠금을 끌어내고, `hashchange` · `popstate` · `pageshow` 마다 다시 맞춘다 — 놓쳐도 다음 이동에서 저절로 풀린다
- **`[hidden]` 이 무시되던 문제** — `drawer.css` 가 `header { display: flex }` 로 모든 `<header>` 를 잡고 있었다. 작성자 규칙은 브라우저 기본값 `[hidden] { display: none }` 을 이기므로 `hidden` 을 붙여도 감춰지지 않는다. 선택자를 `.app-bar` 로 좁히고, 안전망으로 `[hidden] { display: none !important }` 를 뒀다
- 화면 맨 아래 120px 빈 칸 제거 — v2.21.0 앱 셸 개편에서 하단 고정 '기준 안내' 막대가 사라졌는데 그 자리를 비워 두던 여백과 CSS 만 남아 있었다

### 변경
- **PC 헤더 분리** — 1024px 이상에서 상단 바는 서비스 이름(POGO PLAN) 자리를 지키고, 지금 보고 있는 화면 이름은 본문 맨 위 헤더(`.page-head`)가 맡는다. 뒤로가기 버튼은 그대로. 좁은 화면은 지금까지처럼 상단 바가 제목을 겸한다
- **도감 2열은 카드형** — 한 줄에 둘을 욱여넣으니 이름이 잘리고 밑줄만 촘촘해 무엇을 고르는 화면인지 읽히지 않았다. 2열로 바꾸는 이유가 "한눈에 훑기"라면 줄이 아니라 카드가 맞다. 그림 64px, 번호는 왼쪽 위·★ 는 오른쪽 위 모서리. 560px~ 3열 · 1024px~ 4열
- **버튼 반응** — hover·active 에 전환 효과. 원칙 셋: 상시로 움직이는 것을 두지 않는다(새 소식 점만 예외), 레이아웃을 흔들지 않는다(`transform`·색만), `prefers-reduced-motion` 을 존중한다. **위치는 옮기지 않는다** — hover 로 버튼이 이동하면 가리키는 순간 목표가 달아나 조준이 어긋난다

### 추가
- `tests/e2e/shell.js` — 앱 셸 회귀 27항목. 화면 15개 × 두 폭에서 실제로 휠을 굴려 보고, **"세로로 조금 넘치는 가로 스크롤러"를 화면 전체에서 찾아** 이번 원인을 직접 잡는다. 오버레이 여닫기 9가지 조합 뒤 잠금이 남는지, 잠금이 남아도 이동하면 풀리는지도 본다

</details>

<details>
<summary><b>v2.25.0</b> — 2026-09-08 · 추가 · 참고</summary>

PoGoMate 대조에서 나온 결손 중 **자동 수집으로 채울 수 있는 것**부터 채웠다. 손이 닿아야만 갱신되던 데이터를 파이프라인으로 옮기고, 그 위에 화면 둘을 얹었다.

### 추가
- **⚔️ 레이드 보스**(`#/raids`) — 지금 도는 보스를 티어별(5성·메가·3성·1성 …)로. 줄마다 타입 · CP 범위 · 부스트 날씨 · 색이 다른 모습 가능 여부. 누르면 상세가 열려 약점·추천 딜러로 이어진다
- **🥚 알 부화**(`#/eggs`) — 2·5·7·10·12km 알의 부화 풀을 거리별로. 지역한정 · 어드벤처 싱크 · 선물 알을 구분해 표시하고, ★ 로 즐겨찾기에 담을 수 있다
- **유사백 판정** — 🎒 내 포켓몬 카드에 `백개체` · `유사백` · `준수` 뱃지. 개체값 퍼센트(합/45)로는 실전 차이가 안 보인다 — 공격 15 인 14/15/15 와 방어 15 인 15/14/15 는 같은 98% 지만 쓰임새가 다르다. 그래서 **같은 레벨의 백개체 CP 와 견줘** 몇 %인지·몇 CP 모자란지를 낸다(만렙 50 기준, 툴팁에 수치). 97% 미만은 뱃지를 붙이지 않는다
- **`backend/gameday_build.py`** — ScrapedDuck(LeekDuck 스크랩) 원본을 화면이 쓸 모양으로 굽는다. `scripts/fetch_data.sh` 가 `raids` · `eggs` · `events` 를 받고, 실패해도 배포를 막지 않는다(그 화면만 빈다)
- **`tests/e2e/`** — 회귀 스위트를 저장소로 옮겼다. `nav`(탐색·디자인 40항목) · `legal`(약관·동의·계정 삭제 35항목) · `gameday`(신규 화면 20항목) · `fingerprint`(이름 리팩토링 검증). 그동안 세션 스크래치패드에서만 돌아 저장소에 남지 않았다

### 참고
- **한글 이름을 짓지 않는다** — 원본은 영문 이름만 준다. 그림 주소(`pm<번호>.f<폼>.icon.png`)에서 번호와 폼 토큰을 뽑아 `dex.json` 이름표와 `names.py FORM_KO` 로 조립하고, 이름표에 없는 번호는 그 항목을 통째로 버린다. 날씨도 같은 원칙이라 매핑에 없는 값은 뺀다 — 한 단어라도 영문이 섞이면 화면이 반쪽이 된다
- **일정표(`#/schedule`) 는 그대로 손으로 적는다.** ScrapedDuck 이벤트는 영문 원제뿐이라 한국 공식 발표로 쓴 지금 일정표를 대신할 수 없다. 데이터는 `GAMEDAY.events` 로 실어 두었고, 화면에 붙일지는 별도 결정 사항
- 필드 리서치(`research`)와 GO로켓단은 이번에 넣지 않았다 — 리서치는 과제 문구가 영문이라 번역 없이는 반쪽이고, 로켓단은 자동 수집원이 없어 수동 관리가 된다

</details>

<details>
<summary><b>v2.24.0</b> — 2026-09-08 · 변경 · 참고</summary>

클래스 이름 리팩토링. 화면·기능은 바뀌지 않는다 (순수 이름 변경).

### 변경
- **CSS 클래스 이름을 BEM 으로 통일** — `block-name__element--modifier` 꼴, 단어 구분은 `-`, 이름은 최대 3단어. 332개 클래스를 한 번에 바꿨다. 예: `service-tile` → `home__tile` · `psearch-head` → `search__head` · `modal-close` → `modal__close` · `acct-danger` → `account__danger` · `d-sprite` → `sprite-box`
- **겹치던 이름을 블록으로 갈랐다** — `overlay`/`modal` 이 각각 바깥 `<dialog>` 과 안쪽 카드였던 것을 `modal`/`modal__box` 로, `drawer-backdrop`/`drawer` 를 `drawer`/`drawer__panel` 로. 블록 이름만 보면 어디에 붙는지 알 수 있다
- **여러 블록이 함께 쓰는 상태는 `is-*` 유틸리티로** — `on` → `is-on` · `unrel` → `is-unreleased` · `x2`/`r2` → `is-weak2`/`is-resist2` · `hi` → `is-high`. 한 블록에만 붙는 상태는 `--modifier` 로 남겼다
- **코드에서 만들던 동적 클래스도 정리** — 티어 등급 글자는 `g-S` → `grade-mark--s`, 플래너 상태는 `s-0`/`s-1`/`s-2` → `plan__status--a`/`--b`/`--c`. 대소문자·숫자가 CSS 와 어긋날 여지를 없앴다

### 참고
- 불변 규칙은 그대로 지켰다 — 전역 이름 · `state` 키 · DOM id · localStorage 키 · GA 이벤트명 · Firestore 필드명은 하나도 건드리지 않았다. 바뀐 것은 `class` 속성과 CSS 선택자뿐
- 계산된 스타일 지문(22개 화면 × 모바일·PC 두 폭, 클래스 이름을 뺀 태그·구조·44개 계산 속성)을 리팩토링 전후로 떠서 렌더 결과가 같은지 확인했다

</details>

<details>
<summary><b>v2.23.0</b> — 2026-09-08 · 추가</summary>

### 추가
- **PC 레이아웃** — 1024px 이상에서 왼쪽 고정 사이드바(서비스 이동 9항목) + 넓은 본문. 컨테이너 760px → 1100px, 사이드바 210px + 32px 간격. 서비스 홈 타일 4열(560px~ 3열 · 그 아래 2열), 팝업·검색 시트 520px. 하단 기준 안내도 같은 열에 정렬. **좁은 화면(~1023px)은 기존 배치 그대로** — 서비스 이동은 ☰ 드로어 안
- 사이드바는 `position: fixed` 라 DOM 구조를 바꾸지 않는다(본문에 왼쪽 여백만). 목록은 복제하지 않고 `matchMedia('(min-width: 1024px)')` 로 드로어 ↔ 사이드바를 **옮긴다** — 랜드마크와 `aria-current` 가 두 벌이 되지 않게

</details>

<details>
<summary><b>v2.22.0</b> — 2026-09-08 · 변경</summary>

디자인 통일. v2.19.0–v2.21.0 이 새 화면(서비스 홈·앱 셸)을 얹으면서 `components/app-shell.css` 가 **오버레이처럼 기존 체계를 덮어써서** 한 화면 안에 두 가지 디자인이 겹쳐 보였다. 새 기능은 그대로 두고 겉모습만 원래 체계로 되돌린다.

### 변경
- **덮어쓰기 제거** — `app-shell.css` 가 다시 정의하던 `:root` 토큰(`--muted`·`--line`·`--surface`·`--hover`)·`body` 글꼴·기본 글자 크기(16px)·컨테이너 폭(1040px)·모서리 반경(18/24/28px)을 전부 뺐다. 이것 때문에 `tokens.css` 의 무채색 팔레트가 푸른 회색(#edf2fa 계열)으로 덮이고 **Montserrat 이 빠졌으며**(숫자·영문이 Pretendard 로 렌더), 같은 카드가 화면마다 회색/파랑으로 달라 보였다. 이제 색·글꼴·크기·폭의 원본은 `tokens.css`·`base.css`·`layout.css` 하나뿐이다
- **원칙: 오버레이로 덮지 말고 원본을 고친다** — 기존 컴포넌트 모양을 바꿔야 하면 그 컴포넌트 CSS 를 직접 고친다. 이번에 그렇게 옮긴 것: 아이콘 버튼 크기 → `drawer.css`, `<dialog>` 기본값 정리 → `modal.css`·`drawer.css`·`search.css`. `app-shell.css` 에는 v2.21.0 이 새로 만든 것(고정 상단 바·건너뛰기 링크·필터 아코디언)과 화면 전체 접근성 기본값만 남았다
- **서비스 홈** — 타일 정의가 `app-shell.css`·`home.css` 두 곳에 서로 다른 값으로 있던 것을 `home.css` 한 곳으로 합쳤다. 색은 직접 쓴 파랑(`#4679ad`·`#5281b8`·`#6b9bdb`) 대신 토큰으로, 카드 모양은 다른 카드(`.schedule`·`.account`·`.plan-card`)와 같게(1px 테두리 · 12px 반경 · `--surface`). 인사말 42px→26px 로 낮춰 `h1`(22px) 바로 위 단계에 맞췄다
- **아이콘 언어 통일** — 홈 타일이 혼자 기하 기호(◒ ▤ ◇ ✦ ⚔ ◈ ↗ ▦)를 쓰던 것을 앱이 이미 쓰던 이모지로(🎒 📕 🧭 ✨ ⚔️ 🃏 🌱 📅). 헤더 버튼도 텍스트("검색"·"메뉴"·"홈")에서 원래 아이콘(🔍 👤 ☰)으로 되돌렸다 — 옆의 `←` 아이콘 버튼과 생김새가 달라 따로 놀았다. 접근성 이름은 `aria-label` 로 그대로
- **탭 줄 복구** — v2.21.0 이 `#tabs` 를 통째로 감춰 D-MAX ↔ PvE ↔ PvP 를 오가려면 매번 메뉴를 열거나 홈으로 가야 했다. 셋은 형제 화면이라 탭 줄을 되살리고, 이동은 v2.21.0 의 주소 체계(`#/rank/…`)를 그대로 쓴다(상단 바 제목·뒤로가기가 같이 맞는다). 오른쪽 바로가기(📕 🧭 ★)도 함께. 서비스 홈에서는 숨김. 플래너 탭 라벨도 텍스트로 통일(`육성 현황` · `내 포켓몬`)
- **헤더 진입점 정리** — '홈' 버튼 제거. 드로어 맨 위 "서비스 홈" 과 같은 곳으로 가는 중복 진입점이라 v2.15.1 "화면 하나에 버튼 하나" 원칙에 맞춰 하나만 남겼다(그 링크가 `goHome()` 을 부르므로 GA `home` 이벤트도 유지). 계정 버튼 👤 은 헤더에 되돌렸다 — 승인 대기 상태를 여기서 보여 준다(v2.2.0)
- **상세 팝업** — 전체 화면 페이지에서 원래의 가운데 카드(좁은 화면은 바텀시트)로. 닫기 버튼도 `←` + "상세 정보" 줄에서 오른쪽 위 `✕` 하나로. 검색도 같은 시트 기하로 통일(제목·입력은 스크롤해도 고정)
- **손가락 대상** — `.icon-btn` 38px → 44px(`--tap` 토큰). v2.21.0 은 모든 버튼에 `min-height: 48px` 를 걸어 칩·세그먼트·목록까지 부풀렸는데, 대상 크기와 글자 크기를 분리해 **글자 크기 체계는 그대로** 두고 대상만 키웠다
- 토큰 추가: `--accent`(v2.15.0 플래너 배지 초록을 올린 값 — 드로어 현재 위치 표시에만 쓴다) · `--tap`. 원칙은 그대로 "색은 의미에만"(타입·폼·순위)
- 개인정보처리방침: v2.19.0 게스트 플래너가 브라우저에 저장하는 개체 정보(`plan_guest_mons`)를 자동수집 항목에 명시
- 기록 정리: `README` 버전 이력에 v2.19.0–v2.22.0 추가(v2.19.0–v2.21.0 이 빠져 있었다), `CHANGELOG` 머리말이 v2.20.0 아래로 밀려 있던 것 복구

</details>

<details>
<summary><b>v2.21.0</b> — 2026-09-08 · 기능 간 상단 탭 제거. 공통 앱 바와 홈에서 기능별 독립 화면으로 이동.</summary>

- 기능 간 상단 탭 제거. 공통 앱 바와 홈에서 기능별 독립 화면으로 이동.
- 메뉴를 서비스 링크 중심으로 단순화하고 검색·상세를 전체 화면으로 변경.
- native dialog, Tab 순환, Escape·뒤로가기, 포커스 복귀, 검색 결과 보존.
- 화면 확대 허용, 건너뛰기 링크, 랭킹 키보드 접근, 큰 터치 영역, 대비·간격 및 모바일 필터 개선.
- 기준: Material Design app bar 및 WAI-ARIA dialog pattern. 전체 WCAG 인증을 의미하지 않음.

</details>

<details>
<summary><b>v2.20.0</b> — 2026-09-08 · 서비스 홈 신설: 모바일 2열·PC 3열 기능 카드. 해시 없는 첫 진입과 로고는 홈으로 연결.</summary>

- 서비스 홈 신설: 모바일 2열·PC 3열 기능 카드. 해시 없는 첫 진입과 로고는 홈으로 연결.
- D-MAX 밑 중복 검색창·도감·상성·플래너·일정 줄 제거. 헤더 검색 유지.
- 랭킹 주소 `#/rank/max`, `#/rank/pve`, `#/rank/pvp` 추가. 전체 페이지와 랭킹에서 홈 복귀 지원.

</details>

<details>
<summary><b>v2.18.0</b> — 2026-09-07 · 추가 · 변경</summary>

노션 "🚀 상용·오픈소스 전환 점검" Phase 0 + 인수인계 문서 4-C **공개 준비 1·2·3**. 결정 근거: 수익화 없음 → 후원(단계적) · 코드 MIT · 스프라이트 고지 후 유지 · 승인제 유지 · 위치 기능 없음.

### 추가
- **공개 준비 1 — 라이선스·고지** — `LICENSE`(MIT, 코드만) · `NOTICE.md`(경로별 범위 표, 포켓몬 IP 고지, 출처별 라이선스 표 11건, 포크 시 조건, takedown 72시간 정책) · `CONTRIBUTING.md`(dev 브랜치 PR, DCO 서명, 불변 규칙 요약, 행동 규범) · `SECURITY.md`(제보 창구, 범위, 90일 공개 원칙). README에 라이선스 표와 고지 갱신(권리자 The Pokémon Company · Nintendo · Creatures · GAME FREAK, Pokémon GO 는 **Scopely Explore, Inc.**). 선행 확인 2건 통과: 배포 스프라이트는 96×96 축소본(평균 0.5KB)만, 게임마스터 원본(`gm.json`·`pm.json`)은 `dist/`에 미포함(`data/`는 미커밋). 하와이 시트 작성자 허락은 사람 액션으로 남김(NOTICE에 "확인 진행 중")
- **공개 준비 2 — 약관·방침** — `components/terms.js`: 📜 이용약관 페이지(`#/terms`, PAGES 등록, 7장: 서비스·계정과 승인·금지 행위·면책·지식재산·개인정보·준거법) + **첫 로그인 동의 팝업**(`openTermsConsent`: 약관·방침 동의 + 만 14세 체크 둘 다 켜져야 [동의하고 로그인], `localStorage pogo_terms_ok = TERMS_VER`, `requests` 문서에 `consent`·`consentAt` 기록, 약관 개정 시 재동의) + `IP_NOTICE` 한 곳 관리(`ipNoticeNode`, 푸터 `#ip-notice`와 전체 페이지 하단에 상시 노출). `privacy.js` 전면 개정: 수집 항목 표, 자동수집(GA 동의 조건·localStorage 키·SW 캐시·쿠키), 처리위탁·국외이전 표(Google LLC 미국, Firestore 서울 리전, GA 14개월), 보유기간·파기, 만 14세 미만 불허, 이용자 권리(셀프 삭제 안내), 안전성, 보호책임자(10일 회신), 고지(7일 전), 위치정보 미수집 명시
- **공개 준비 3 — 통계 동의·계정 삭제** — `components/consent.js`: 첫 방문 하단 배너(`#consent`, 스플래시 뒤에 표시: 저장소 안내 + 위치 미수집 + [통계 거부] [통계 동의]), `localStorage pogo_consent`, **동의 전 gtag 미삽입** — build.py `GA_SNIPPET`은 `window.GA_PENDING_ID`만 남기고 `loadAnalytics()`가 동의 시 Consent Mode v2 기본값(analytics granted · ad 전부 denied)과 함께 붙인다. 철회하면 `consent update denied` + `window.gtag` 제거로 `track()` 무력화. ☰ "🍪 통계·저장소 설정" 팝업(현재 선택 표시·변경, 저장된 것 목록, 🧹 캐시 비우고 새로고침 = `caches.delete` + SW unregister). **계정 삭제 셀프서비스** `auth.js deleteAccount()`: 확인 팝업(지워지는 것 3항목) → `users/{uid}` → `allowlist/{email}` → `requests/{email}` → `user.delete()`, `auth/requires-recent-login`이면 `reauthenticateWithPopup` 뒤 재시도, 관리자 제외, 승인 대기 상태도 가능. GA `consent`(value·from)·`terms_accept`·`account_delete`·`cache_clear`
- `firestore.rules`: `allowlist`·`requests` 본인 이메일 문서 **delete 허용** (관리자 권한은 그대로). ⚠️ 콘솔에 다시 게시해야 적용 — 게시 전에는 삭제 버튼이 permission-denied 안내
- `dev-mock.js`: `user.delete()`·`reauthenticateWithPopup()` 목. 새 파일 `styles/components/consent.css`(배너·팝업·방침 표·IP 고지·위험 버튼)

### 변경
- 푸터: 고지 두 줄 + 링크 줄(개인정보처리방침 · 이용약관 · 통계·저장소 설정 · GitHub). ☰ 메뉴에 📜 이용약관 · 🍪 통계·저장소 설정 항목. 계정 카드 비로그인 안내에 약관·방침 링크, 승인됨·대기 상태에 "계정 삭제" 버튼
- docs: DEVELOPMENT 7장(규칙 표·삭제 순서·동의 흐름), OPERATIONS 4장(계정 삭제·규칙 재게시)·7장(동의 뒤 집계). Playwright 35항목 통과(배너·설정·약관·방침·동의 팝업·재로그인·삭제·대기/관리자 분기)

</details>

<details>
<summary><b>v2.17.0</b> — 2026-09-07 · 변경</summary>

노션 "서비스명 변경 검토"에서 **POGO PLAN(포고플랜)** 확정 (인수인계 문서 3-1).

### 변경
- **서비스명 POGO SEARCH → POGO PLAN** — 화면 `<title>`("POGO PLAN — 뭘 키우고, 뭘 잡을지")·헤더 `<h1>`·스플래시·`apple-mobile-web-app-title`, `manifest.webmanifest`(이름 "POGO NOTE" 잔재 → "POGO PLAN — 포켓몬GO 육성 플래너", short_name "POGO PLAN"), 상세 팝업 공유 제목, 개인정보처리방침 서비스명, `robots.txt`·dev robots 머리말, README·docs 제목, `.env.example`·`.vscode` 주석. 서비스워커 캐시 접두사 `pogonote-v3` → `pogoplan-v4`(activate 가 옛 캐시를 지우므로 사용자 조치 없음). **유지**: 저장소명·배포 URL(PWA 설치·공유 링크 보호), 전역 이름·`state` 키·localStorage 키(`pogo_*`)·GA 이벤트명·Firestore 필드명(불변 규칙). 패치노트의 지난 항목에 남은 "POGO SEARCH" 표기는 기록이라 그대로 둔다

</details>

<details>
<summary><b>v2.16.1</b> — 2026-09-07 · 수정</summary>

### 수정
- **첫 화면에서 스프라이트가 안 뜨고 새로고침해야 나오던 문제** — 원인은 `cloneNode`. D-MAX 티어표 행(`views/max.js expandableRow`)과 솔플 내 덱(`ifsolo.js`)은 `row()` 결과를 `cloneNode(true)`로 복제하는데(상세 팝업 클릭을 떼기 위해), 복제본의 `<img>`에는 v2.14.0 스켈레톤이 단 `load` 리스너가 없어 `.loading`(회색 반짝임)이 영영 안 벗겨졌다 — 첫 화면이 바로 티어표라 "그림이 안 뜬다"로 보였다. 새로고침하면 캐시된 이미지가 생성 시점에 이미 완료 상태라 `complete` 검사로 벗겨져 정상처럼 보였다. 수정: (1) `sprite.js`의 load/error 처리를 요소 리스너가 아니라 **문서 캡처 리스너**(`document.addEventListener('load'/'error', …, true)`)로 옮겨 복제돼도 동작하게. (2) `loading="lazy"` 제거 — 스프라이트는 평균 1.1KB(1,172장 4.8MB)라 이득이 없고, 동적으로 붙인 lazy 이미지가 스크롤 전까지 안 받아지는 브라우저 회피. (3) 실패 시 캐시를 우회하는 주소(`?r=1`, `?r=2`, `data-retry`)로 두 번 재시도한 뒤에야 몬스터볼 자리표시로 교체. (4) 첫 화면 가림막(#splash)은 문서에 `.loading` 스프라이트가 없어질 때까지(최대 2.5초, `waitForSprites`) 유지. `sw.js`: 캐시 세대 `pogonote-v3`, 스프라이트는 쿼리를 뗀 키로 캐시 조회·저장(재시도 주소도 같은 파일), 네트워크 실패 시 `cache: 'reload'`로 한 번 더, `res.ok`일 때만 캐시. Playwright로 정상(38/38 로드, 스켈레톤 0)·첫 요청 실패 후 재시도 복구(38/38)·전부 실패(자리표시 38, 가림막 2.5초 내 해제) 확인

</details>

<details>
<summary><b>v2.16.0</b> — 2026-09-07 · 변경</summary>

### 변경
- **IF 탭·활용처 탭 해체 — 기능을 제자리로** (dev 피드백) — 탭은 D-MAX · PvE · PvP 셋. (1) **솔플 레이드 계산기**는 PvE 탭 세그먼트 오른쪽 `🧮` 도구 버튼(`toolButton`, `state.pveTool = 'solo'`)으로 — 누르면 티어표 자리에 `renderSoloCalc()`가 펼쳐지고 다시 누르면 접힌다(서브탭을 고르면 접힘). (2) **PvP 덱 짜기**는 PvP 탭 리그 세그먼트 오른쪽 `🃏` 버튼(`state.pvpTool = 'deck'`) — 덱 리그는 `state.league`를 그대로 써 `deckLeague`와 덱 안의 리그 세그먼트를 없앴다. (3) **활용처**는 🔍 검색 패널로 — 비어 있을 때 "🏆 활용처 순위"(`usageTopNodes`, 8마리 + 더보기, 누르면 상세) 를 보여 주고, 검색 후보 줄마다 "활용 N곳" 뱃지(`usageBadge`)를 붙인다. `renderIfTab`·`renderUsage`·`state.ifWho`·`deckLeague` 제거, `pogo_last_view`의 옛 `usage`/`if` 탭 값은 허용 목록에서 걸러져 D-MAX로. 새 줄 `.ctrl-row`(세그먼트 + 도구 버튼), `.tool-btn`(aria-pressed). GA `tool_solo`·`tool_pvpdeck`(on)·`usage_pick`

</details>

<details>
<summary><b>v2.15.1</b> — 2026-09-07 · 변경</summary>

### 변경
- **중복 진입 버튼 정리 — 화면 하나에 버튼 하나** — dev 미리보기 피드백: 같은 화면을 가리키는 버튼이 여럿("도감", "도감 모드로 전환", "도감에서 채우기"…)이라 기능 위주로 병합·삭제. 남긴 것 / 지운 것: 모드 전환 = 헤더 배지 / ☰ `#menu-mode`·플래너 홈 "🔎 도감 모드로" · 📕 도감 = 탭 줄 📕 / ☰ "📕 도감"·계정 카드 "📕 도감에서 채우기"·플래너 홈 "📕 도감에서 찾기" · 🧭 상성 = 탭 줄 🧭 / ☰ "🧭 상성 검색" · ★ 즐겨찾기 페이지 = 탭 줄 ★ / ☰ `#menu-favs`·즐겨찾기 카드 "PvE · PvP 나눠 보기 ▸" · 🎒 내 포켓몬 = 플래너 탭 / 홈의 "내 포켓몬 열기"·"전체 보기 · 비교"·"첫 개체 저장하기" · 로그인 = 헤더 👤 / 플래너 홈·내 포켓몬의 "Google로 로그인" 버튼(안내 문구로). 플래너 홈은 소개 + 요약 + 후속 안내만. 계정 카드 요약에 🎒 내 포켓몬 마릿수 추가. 도감 페이지 안의 "★ 즐겨찾기 N" 칩은 탭 줄이 보이지 않는 전체 페이지라 유지(`initFavsMenu`는 대상 요소가 없으면 조용히 끝난다)

</details>

<details>
<summary><b>v2.15.0</b> — 2026-09-07 · 추가 · 변경</summary>

노션 QA 트래커 [플래너] v2.15.0 MVP — QA-53(모드 전환 셸) · QA-54(내 포켓몬). 배경: 개발 → 🌱 육성 플래너 통합 방향 / 🚀 v2.15.0 MVP 작업 명세.

### 추가
- **🌱 플래너 모드 셸 (QA-53)** — 한 앱에 두 모드. 헤더 서문 옆 모드 배지(`#mode-toggle`, `🌱 플래너` ↔ `🔎 도감`)와 ☰ 메뉴 맨 위 항목(`#menu-mode`)으로 전환하면 탭 줄이 통째로 바뀐다(도감 = 기존 그대로 / 플래너 = [🏠 홈 | 🎒 내 포켓몬]). **모드는 해시가 정한다**: `#/plan`(홈)·`#/plan/collection`(내 포켓몬)만 플래너, 그 밖(없음·`#/dex` 등)은 전부 도감 — 그래서 딥링크·뒤로가기(history.js)가 그대로 동작하고 탭 이동도 뒤로가기로 되돌아간다. 해시 없이 처음 열 때만 마지막 모드(`localStorage pogo_plan_last`)가 플래너면 `#/plan`으로(replaceState), 기본은 도감 모드. `state.appMode`·`planTab`·`planParams` 추가(기존 `pogo_last_view` 키·필드 불변). 로고 탭 = 현재 모드의 홈. 플래너 모드에서는 ★ 즐겨찾기 요약 카드를 감춘다(`body[data-mode]`). GA `mode_switch`(to·from)·`plan_view`·`plan_tab`. 새 폴더 `frontend/scripts/planner/`(shell · home · collection) + `styles/components/planner.css`. 번들이 한 `<script>`라 `const state`가 TDZ인 첫 로드에 `renderPage()`가 먼저 도는 문제는 `_planShellReady` 플래그로 거른다
- **🎒 내 포켓몬 — 개체 단위 저장·비교 (QA-54)** — Firestore `users/{uid}.mons` 배열(`{ id, sprite, shadow, level, ivs:[atk,def,hp], fast, charged, status, memo, at }`), 즐겨찾기 `favs`(종 단위)와 별개 필드라 ★ 의 의미는 그대로. 규칙 변경 없음(`users/{uid}` 본인·승인 조건 그대로). 목록 = 상태 칩(전체·육성 중·완료·교환 후보) + 개체 카드(그림·이름·뱃지·Lv/IV/CP·기술·맥스 자격·메모, [☐ 비교]·수정·삭제). 추가/수정 팝업: 기존 검색 색인 재사용해 종 선택(다이맥스/거다이맥스 항목 제외, "섀도우 X"를 고르면 섀도우 체크) → 섀도우·레벨(1~50, 0.5)·개체값·**CP로 레벨 추정**(`planLevelFromCp`, 안 맞으면 가장 가까운 레벨과 차이 안내)·기술(`form.fast/charged` 목록, 모름 허용)·상태·메모. CP는 저장하지 않고 `calcCp`로 계산. 같은 종 2개체 [☐ 비교] → `openPlanCompare`: 지금 레벨·CP·개체값·만렙 CP·리틀/슈퍼/하이퍼 도달 CP(`planLeagueReach`, 상한 아래 최대 레벨)·기술·섀도우, 큰 쪽 강조, "순위 평가 아님" 명시. 비로그인·승인 대기는 계산만 되고 저장 버튼이 로그인/승인 안내. 상세 팝업 `.d-actions`에 ➕(`planAddFromDetail`) → `#/plan/collection?add=<sprite>&shadow=1`로 프리필. `AUTH.mons`(auth.js loadFavs가 읽고 onAuthChange가 초기화). GA `plan_mon_save`(add/edit)·`plan_mon_delete`·`plan_compare`·`plan_add_from_detail`
- **플래너 홈** — 소개 카드 + 내 포켓몬 요약(마릿수·상태별·최근 6마리) + 도감 모드 복귀 + 후속 기능 안내(육성 판단·목표 계산기·검색식·파티·일정 연결은 백로그)

### 변경
- 개인정보처리방침에 플래너 개체 정보 저장 항목·로컬 저장소(마지막 모드) 명시 (2026-09-07 개정)

</details>

<details>
<summary><b>v2.14.0</b> — 2026-09-07 · 변경 · 추가</summary>

노션 QA-52 "개선사항" 묶음.

### 변경
- **D-MAX 탭 [전체 | 딜러 | 탱커] + 하위 메뉴** — v2.13.0의 [딜러 | 탱커] 두 축에서 '딜러' 화면이 티어표와 보스 어태커 표를 함께 실어 길었다. 세 축으로 나눠 축마다 표 하나: 전체 = D-MAX 티어표(`renderMaxTier`, 칩은 맥스무브 속성), 딜러 = 보스 속성 상대 맥스 어태커(`renderMaxDealer`, `DMAX_DATA`, 전체는 중립), 탱커 = EHP(`renderMaxTank`). 세그먼트 아래에 "맥스무브 속성 / 보스 속성" 라벨이 붙은 속성 칩 줄(`maxSubmenu`, `.submenu`)을 하위 메뉴로 둔다. `state.maxAxis` 허용값 `all`·`dealer`·`tank`(기본 `all`). 티어표에서 속성을 고르면 "[딜러] · [탱커]에서" 안내, 보스 아코디언의 "칩으로 이동"과 상성 검색의 "전체 순위 보기"는 딜러 축으로 간다
- **★ 즐겨찾기 카드 → 아코디언** — `#fav-digest`를 일정표와 같은 `<details class="schedule">`로 바꿨다(`favdigest.js`). 제목 줄에 개수와 최근 순위 변동 마릿수, 본문에 그림 격자 + 더보기 + "PvE · PvP 나눠 보기". 펼침 여부는 `localStorage`(`pogo_fav_acc`)에 기억
- **웹폰트** — IBM Plex Sans KR → 영문·숫자 **Montserrat**(Google Fonts) + 한글 **Pretendard Variable**(jsdelivr 동적 서브셋). `index.html` 링크·스플래시, `base.css` 폰트 스택
- **상성 검색 화면 가독성** — 긴 이름("거다이맥스 에이스번")이 폼 뱃지와 붙어 깨지던 머리 카드(`.ts-head h2`를 flex-wrap + 줄 간격), 배율 줄(`.ts-row`)의 라벨을 상단 정렬하고 칩 줄 간격을 넓힘, 400px 미만에서는 라벨을 칩 위로. 추천 카드(`.boss-rec`)의 긴 이름은 카드 폭 안에서 가운데 줄바꿈. 입력창 안내문을 짧게

### 추가
- **스프라이트 스켈레톤** — `sprite()`가 `<img>`에 `.loading`(회색 반짝임)을 붙이고 `load`에서 뗀다. 받기 실패하면 몬스터볼 자리표시(`spritePlaceholder`)로 교체. `prefers-reduced-motion`이면 반짝임 없이 회색 판만

</details>

<details>
<summary><b>v2.13.1</b> — 2026-09-07 · 추가</summary>

### 추가
- **일정표 페이지 가독성** (`#/schedule`) — 달력 점만으로는 "무엇이 언제부터 언제까지"가 안 읽혔다. (1) 분류 칩(전체·이벤트·5성·메가·D-MAX·아워·섀도우, `chips()` 재사용 — `SCHEDULE_CATS`에 색 원천인 `type` 키 추가)으로 달력 점·타임라인·분류별 목록을 한꺼번에 거른다. 선택은 `localStorage`(`pogo_sched_cat`)에 기억, GA `sched_cat`. (2) **기간 막대 타임라인** `buildScheduleTimeline(cat)` — 한 줄에 일정 하나, 시작~종료를 분류 색 막대로 잇고 위에 날짜 눈금(1·5·10…, 주말 음영, 오늘 세로선). 막대 위치·폭은 일수 대비 백분율이라 화면 폭에 따라 맞춰진다. 시작이 달 후반(60% 이후)인 일정은 라벨을 막대 끝에 오른쪽 정렬해 잘리지 않게. 드로어(좁은 폭)는 달력 점을 그대로 두고 "자세히 보기" 페이지에서만 구현. `scheduleItemsOn(day, cat)`·`buildScheduleCal(cat)`·`scheduleMonthList(cat)`에 필터 인자를 더했고, 인자 없이 부르면 예전과 같다

</details>

<details>
<summary><b>v2.13.0</b> — 2026-09-07 · 수정 · 추가 · 변경</summary>

노션 QA 트래커 v2.13.0 묶음(QA-49 · QA-50 · QA-20)과 백로그(QA-42 · QA-43).

### 수정
- **배틀 유형별 참전 풀 구분 (QA-49)** — 상세 팝업 "보스로 나오면?"이 보스 종류와 무관하게 맥스 배틀 딜러(`DMAX_DATA`)만 보여 줘, 원시 가이오가 같은 레이드 보스에도 맥스 배틀 수치 기반 추천이 나왔다. `detail.js counterNode`를 분기: 이름이 다이맥스/거다이맥스로 시작하는 보스 → 기존 맥스 딜러 + 제목 "맥스 배틀 — 다이맥스·거다이맥스만 참전 가능" / 그 밖의 보스(일반·전설·메가 레이드) → 상성표에서 배율 1.6 이상 공격 타입(최대 2개)을 골라 레이드 성능표(시트, 없으면 자체 계산) 상위 5마리(종 중복 제거) + 제목 "레이드 — X 딜러 추천". D-MAX 티어표·보스 딜러 목록은 원래 `max_released` 기준으로 걸러져 있어 손대지 않았다
- **미출시 리전 폼이 다이맥스/거다이맥스로 등재 (QA-50)** — `value_build.py in_release_set`이 `(pokemon_id, None)` 등재만으로 폼과 무관하게 통과시켜 `D MOLTRES`(관동) 한 줄로 가라르 파이어까지 자격을 얻었다. 다이맥스 7건(가라르 프리져·썬더·파이어, 히스이 윈디·가디, 알로라 라이츄, 가라르 달막화)·거다이맥스 2건(알로라·가라르 나옹)이 티어표·딜러 목록에 노출. 규칙을 "기본 폼은 폼 없음/`X_NORMAL` 줄 또는 우리 데이터가 따로 갈라 두지 않은 폼 줄(`DARMANITAN_STANDARD`·`TOXTRICITY_AMPED`)과 같고, 그 밖의 폼은 폼까지 적힌 줄만 인정"으로 바꿨다. 결과 다이맥스 146 → 139, 거다이맥스 19 → 17(공식 17종과 일치). 스트린더·우라오스·불비달마·다이맥스 갸라도스는 그대로 유지
- **10월 일정표 전환 (QA-20)** — `schedule.js`가 9월 하나만 담고 있어 10/1부터 달력이 비게 될 문제. `SCHEDULE_MONTHS['YYYY-MM'] = { ym, note, items }` 달별 구조로 바꾸고 로드 시 오늘이 속한 달을 골라 기존 전역(`SCHEDULE_YM`·`SCHEDULE_ITEMS`)에 넣는다(소비자 `max.js`·`pages.js` 무수정). 오늘 달 데이터가 없으면 가장 가까운 과거 달로 폴백. 달 하드코딩 4곳 제거(상세 제목 `9/${day}`, 보스 아코디언 `9/${s}–${e}`, 일정 페이지 분류별 날짜, 드로어 제목 "9월 일정표"). 일정표가 오늘 달이 아니면 보스 아코디언은 지난 달 보스를 "이번 주"로 내밀지 않고 숨긴다. 10월 확정분 등재: 수확 축제(~10/5)·피카츄의 가을 소풍(~10/11)·커뮤니티 데이(10/10)·제르네아스·메가 우츠보트(~10/6)·D-MAX 울머기(~10/4, water)·섀도우 볼트로스(~10/6). 10/6 이후 로테이션은 달별 `note`로 미발표임을 명시

### 추가
- **순위표 행 "활용 N곳" 뱃지 (QA-42)** — pogomate ★N에 해당하는 우리 식 범용성 표시. `value_build.py`가 전 항목의 등재 내역을 압축해 `VALUE_DATA.usage_places`(이름 → [[곳, 순위]], 약 20KB)로 싣고, `row.js`가 모든 순위표 줄에 자동으로 붙인다(`usageBadge`). 상세 팝업 활용처도 같은 데이터를 읽으므로(예전엔 상위 80만 보여 81위 밖 포켓몬의 활용처가 팝업에서 빠졌다) 뱃지 숫자와 팝업 칩 개수가 같다. 2곳 이상만 표시, 5곳 이상은 파란 강조
- **D-MAX 탱커 축 + 보스 파티 카드 (QA-43)** — `value_build.py tank_rank`: EHP = 체력 × 방어 ÷ 1000 ÷ (보스 타입 자속 기술을 받는 배율), 다이맥스·거다이맥스 출시 개체만, 보스 속성별 상위 30(`data/dynamax_tank.json` → `DMAX_TANK`). D-MAX 탭에 [딜러 | 탱커] 세그먼트(`state.maxAxis`, 마지막 보기 기억, GA `sub_max_*`), 탱커 행은 EHP·받는 배율·체력×방어 표시. 이번 주 보스 아코디언 맨 위에 "🧩 추천 파티 — 딜러 2 + 탱커 1" 카드(탱커는 딜러와 다른 종)

### 변경
- D-MAX 탭 각주의 출시 수를 139종으로 (미출시 리전 폼 제외 명시)

</details>

<details>
<summary><b>v2.12.1</b> — 2026-09-06 · 변경</summary>

### 변경
- **검색 패널 UI** — v2.12.0의 타입 칩이 페이지의 속성 필터 칩과 똑같이 생겨 "검색용인지" 구분이 안 됐다. 검색을 카드(`section.psearch`, surface 배경·테두리)로 묶고 제목 줄(검색 · 안내 · ✕), "타입" 라벨이 붙은 칩 줄(선택 시 검정 채움), 세로 결과 목록(행마다 그림·이름·순위 근거)으로 바꿨다. 빈 상태 안내, 결과 머리말("물 타입 165마리 중 앞 8마리 · 타입 지우기"), 하단 "전부 보기 · 상성 검색" 링크. 🔍 버튼은 열려 있을 때 눌린 상태로 표시, ✕로 닫으면 입력·칩을 비운다(`toggleSearchPanel`)

</details>

<details>
<summary><b>v2.12.0</b> — 2026-09-06 · 추가</summary>

### 추가
- **전역 검색 = 이름 + 타입** (`search.js`) — 검색창 아래 타입 칩 줄(18개, 최대 2개 선택). 검색어에서 타입 이름 토큰("물 풀", "물·풀", "물타입")을 떼어 내 칩과 합친다(`parseSearchQuery`). 타입이 있으면 후보군을 `typeMonList`(도감 폼, 출시 → 미출시)로 바꾸고 이름 검색은 그 안에서 교집합. 타입만 고르면 12마리 + "상성 검색에서 전부 보기", 결과 없음 문구도 타입용으로 분리. GA `search_type`(t), `search_pick`·`search_none`에 `t` 추가

</details>

<details>
<summary><b>v2.11.1</b> — 2026-09-06 · 추가</summary>

### 추가
- **상성 검색: 타입 조합 포켓몬 목록** (`typeMonList`) — `DEX_DATA.forms`(기본 폼 + 순위표·보스 목록에 등장한 메가·리전 폼)에서 타입 2개면 정확히 같은 집합, 1개면 그 타입 포함 전부를 뽑아 배율표 아래에 보여준다. 출시 → 미출시(`rel`, 흐리게 + 미구현 태그) 순, 도감번호순, 24마리씩 더보기. 해당 포켓몬이 없으면 "없습니다"를 명시한다. 누르면 상세 팝업

</details>

<details>
<summary><b>v2.11.0</b> — 2026-09-06 · 추가</summary>

### 추가
- **뒤로가기 연동** (`components/history.js`) — 팝업(상세)·드로어(☰)를 처음 열 때 `history.pushState`로 항목을 하나 넣는다. 폰의 뒤로가기는 사이트를 나가는 대신 열린 것을 닫고(popstate), X·배경·Esc로 닫으면 `history.back()`으로 그 항목을 되돌린다. 팝업 위에서 다른 팝업(진화 줄)·드로어→팝업(관리자 패널)은 항목 하나를 재사용(silent 닫기). 오버레이가 열린 채 페이지로 이동하면 `navigateHash`가 `location.replace`로 그 항목을 덮어써 "닫힌 팝업" 항목이 남지 않는다. `openPage`·`openTypeSearch`가 이 함수를 쓴다
- **로고 = 홈** — 헤더 POGO SEARCH 클릭 → 열린 것 닫고 D-MAX 탭 전체로, 맨 위로 스크롤. GA `home`
- **폼 색 토큰** (`tokens.css`) — 맥스 `--c-max`(자홍→보라), 메가 `--c-mega`(보라→파랑), 섀도우 `--c-shadow`. 이름 뱃지는 메가·맥스가 채움(흰 글씨), 상세 팝업 `.d-sprite`에 같은 색 테두리+광택, 진화 줄 ⚡메가 칩도 메가 색으로 통일(기존 금색 제거)

</details>

<details>
<summary><b>v2.10.1</b> — 2026-09-06 · 추가 · 변경</summary>

"누가 어떤 버튼을 눌렀나"를 사람 단위로 보기 위한 계측 보강. 노션 QA-44 후속.

### 추가
- **GA User-ID** — 로그인하면 `track.js setTrackingUser()`가 Firebase uid를 GA `user_id`로, 승인 상태를 사용자 속성 `login_status`(ok/pending/anon)로 보낸다. 로그아웃하면 null로 되돌린다. `login` 이벤트보다 먼저 붙여 그 이벤트부터 사람 단위로 잡힌다. GA 스니펫이 `window.GA_MEASUREMENT_ID`를 노출해 config 재호출에 쓴다(`send_page_view:false`로 page_view 중복 방지). GA 콘솔에서 보고 ID를 "혼합/관측"으로 바꾸고 사용자 속성 `login_status`를 맞춤 정의에 등록해야 보인다 — 운영 문서 7장
- **상성 검색 계측** — `type_search`에 `how`(chip/search/preset/clear), 추천 딜러 클릭에 `types_rec_click`(list=raid/max · boss · rank · mon). 미리 채워진 채 열린 경우(상세 팝업·공유 링크)도 `how=preset`으로 1건 남긴다
- **계정 카드에 uid** — `requests/{email}` 문서를 승인 뒤에도 지우지 않고 로그인마다 `uid`·`status`를 갱신한다. 승인 시 allowlist에도 uid를 복사. 관리자 패널 "승인된 친구" 줄에 uid를 표시해 GA 사용자 탐색기의 User-ID와 대조한다. 규칙 변경 없음(본인 이메일 문서는 승인 여부와 무관하게 쓸 수 있다)

### 변경
- 개인정보처리방침(`#/privacy`·노션) 개정 — GA에 uid·승인 상태를 보낸다는 항목, 계정 카드가 로그인마다 갱신된다는 문구

</details>

<details>
<summary><b>v2.10.0</b> — 2026-09-06 · 추가 · 변경 · 수정</summary>

노션 QA 트래커 QA-44("오류") 묶음. 스크린샷·요청 5건 + 차별화 제안.

### 추가
- **🧭 상성 검색 페이지** (`#/types`, 탭 줄 🧭 · ☰ 메뉴) — 상대 타입 1~2개(칩) 또는 포켓몬 이름으로 타입을 채우면 (1) 기술 타입별 배율을 이중약점·약점·내성·이중내성/무효로 묶어 보여주고 (2) 그 속성 보스 기준 추천 딜러(레이드 `PVE_DATA` · 맥스 `DMAX_DATA` 상위 5, 해당 탭 바로가기) (3) 반대로 그 타입 기술이 잘 통하는 상대를 보여준다. 주소 `#/types?t=water,dark&mon=10070`으로 공유 가능. 상세 팝업 타입 상성 섹션에서 같은 조합으로 바로 이동. GA `type_search`(t·mon)
- **이중약점 표시** — 상세 팝업 타입 상성에서 ×2.56은 '이중' 표기 + 빨간 테두리, ×0.39(이중내성·무효)는 초록 테두리. 각주에 뜻을 적었다
- **폼 라벨 뱃지** (`components/name.js`) — 이름 앞의 메가·원시·섀도우·다이맥스·거다이맥스·리전 폼을 작은 알약으로 떼고 종 이름만 굵게. 라벨 목록은 `backend/names.py FORM_KO` 값에서 빌드가 `FORM_LABELS`로 주입(단일 원본). 순위표 행·상세 헤더·검색 후보·보스 추천 버튼에 적용. D-MAX 행의 G-MAX/D-MAX 뱃지는 이름의 폼 라벨로 대체

### 변경
- **다이맥스/거다이맥스 이름 분리** (`value_build.py max_name`) — 맥스 표의 행 이름을 `거다이맥스 에이스번` / `다이맥스 에이스번`으로. 일반 에이스번과 이름이 같아 검색 색인(이름 기준 중복 제거)에서 하나만 남고 활용처(이름 기준 합산)가 뭉개지던 문제. 상세 팝업 활용처는 세 이름을 함께 모아 보여주되 맥스 칩에 D/G 표시(`usagePlacesFor`). IF 탭 보스 검색은 맥스 행을 제외(레이드 보스가 아님). `rank_diff` 키가 바뀌어 맥스 표·활용처의 ▲▼는 다음 순위 갱신부터 다시 붙는다

### 수정
- 공유 링크 `#/mon/130`이 '섀도우 갸라도스'로 열리던 문제 — 섀도우·다이맥스는 일반 폼과 스프라이트 id가 같아 검색 색인에서 먼저 만난 항목이 잡혔다. 접두어 없는 항목을 우선한다
- **빠진 스프라이트 45장** (`sprites.py`) — 메가 샤크니아·메가 캥카·메가 요가램·메가 깜까미·메가 다부니, 캐스퐁 날씨 폼, 알로라·가라르·히스이 폼 일부가 몬스터볼 자리표시로 나왔다. 원인은 스프라이트 수집이 순위표 7개 파일만 훑어서 `bosses.json`(IF 탭 보스 목록)·`pve_full.json`·`pvp_all.json`·`dex.json`의 forms/megas(도감 ⚡메가 버튼)에만 있는 번호를 놓친 것. 세 파일과 dex.json forms·megas를 수집 대상에 추가. `dex_build.py`도 같은 세 파일을 읽어 forms를 만들므로 IF 탭 보스 목록에서 연 알로라 폼 상세가 기본 폼 종족값으로 되돌아가던 오정보도 함께 해결. `verify_deploy.sh`에 10070.png 검사 추가

</details>

<details>
<summary><b>v2.9.0</b> — 2026-09-06 · 추가</summary>

GA 첫 3일 데이터(사용자 9명·세션 33)로 정한 개선 묶음. 근거는 노션 🛠 개발 → 📈 GA 첫 3일 데이터로 본 다음 기능 제안.

### 추가
- **마지막 보기 기억** — 탭·리그·속성 칩·PvE 모드·IF 서브탭·덱 리그를 `localStorage`(`pogo_last_view`)에 남기고 시작 시 복원. 시작 탭이 100% D-MAX인데 클릭은 PvP·PvE로 몰렸고 세션당 3.5회 다시 열던 것에 대응. 값은 허용 목록으로 검증한 뒤에만 반영
- **상세 팝업 딥링크·공유** — `#/mon/<스프라이트 id>`로 열면 메인 위에 그 상세가 뜬다. 메인에서 상세를 열면 주소가 `replaceState`로 바뀌고 닫으면 지워진다(뒤로가기 영향 없음). 팝업 헤더에 🔗 공유 버튼(Web Share → 클립보드 폴백). 1순위 상호작용(detail_open 74건)인데 링크가 없어 친구에게 보낼 수 없던 문제
- **탭 줄 바로가기** — 오른쪽 끝에 📕 도감, 로그인 시 ★ 즐겨찾기 N. 좁은 화면은 아이콘만. ☰ 안에만 있을 때 page_open이 탭 클릭의 1/7이던 것에 대응
- **검색 후보에 순위 근거** — "레이드 전체 1위 · 마스터리그 35위". 즐겨찾기 페이지와 같은 `ROLES` 데이터, 폼 키 우선·종 단위 폴백
- **IF 탭 ★ 즐겨찾기에서 덱 채우기** — 솔플 계산기 내 덱 검증에서 즐겨찾기(보유 종) 중 이 보스 상대 평가 가능한 포켓몬을 효율순으로 빈 자리만큼 채운다
- **GA 계측 보강** — `search_pick`(mon) · `search_none`(q) · `fav_toggle`(on, dex) · `login`(status) · `pwa_install` · `share`(mon) · `solo_fill_favs`(n) · `tab_start`에 `standalone` · `page_open`에 `from`(menu/tabbar/card) · `detail_open`에 `from=link/search`. GA 콘솔에서 맞춤 정의(tab·page·mon·boss·from·status) 등록은 운영 문서 7장

</details>

<details>
<summary><b>v2.8.0</b> — 2026-09-05 · 변경</summary>

### 변경
- **저장소 공개 전환 준비** — 운영 식별자를 코드에서 뺐다. `GA_ID` · `ADMIN_UID` · `ADMIN_EMAIL` · `CONTACT_EMAIL` · `FIREBASE_CONFIG_JSON`을 `build.py`가 `.env`(로컬, 커밋 안 함) 또는 GitHub Actions Repository variables에서 읽는다. 템플릿은 `.env.example`. 이 값들은 어차피 빌드 결과에 들어가 방문자에게 보이는 공개 식별자라 보안 조치가 아니라 **공개 저장소 코드에 운영 식별자를 남기지 않기 위한 분리**다
- `firestore.rules`의 관리자 uid를 `__ADMIN_UID__` 자리표시자로 — `scripts/render_rules.sh`가 `.env` 값으로 채운 `firestore.rules.local`(커밋 안 함)을 만들고, 그것을 콘솔에 붙여넣는다
- 푸터·개인정보처리방침의 문의 이메일은 `CONTACT_EMAIL`로 주입, 비어 있으면 문구가 빠진다
- 화면 문구·주석의 개인 이름을 일반 표현으로 — "관리자가 승인", "실측 제공자". 배포 검증 스크립트에 문의 링크 항목 추가
- git 히스토리 전체에서 이메일·Firebase 식별자·관리자 uid·트레이너 코드·개인 이름을 치환(`git filter-repo --replace-text`)하고 세 브랜치를 다시 푸시. 저장소 `pogo-rank`·`pogo-rank-dev`를 public으로 전환

</details>

<details>
<summary><b>v2.7.4</b> — 2026-09-05 · 추가</summary>

### 추가
- `scripts/verify_deploy.sh <주소> <prod|dev> [버전]` — 배포된 주소가 "그 채널의, 그 버전" 빌드인지 검사한다. 버전 배지, 채널 표식(-dev · noindex · GA · robots), data.js의 ROLES·90150, 아머드 뮤츠 전용 스프라이트(일반 뮤츠와 바이트 수 비교), PWA 파일. CDN 캐시를 피하려고 매 요청에 쿼리를 붙인다. 릴리스 절차의 dev 확인·deploy 확인 단계에서 쓴다

</details>

<details>
<summary><b>v2.7.3</b> — 2026-09-05 · 변경</summary>

### 변경
- 브랜치 전략 개편 — `dev`(작업·미리보기) → `main`(통합) → `deploy`(실서비스). `deploy.yml`은 이제 **`deploy` 브랜치만** 배포하고(스케줄 실행도 `ref: deploy`로 체크아웃), `main` 푸시는 배포를 일으키지 않는다. GitHub Pages 환경 `github-pages`의 허용 브랜치에 `deploy` 추가
- `deploy-dev.yml` 신설 — `dev` 푸시 시 `BUILD_CHANNEL=dev`로 빌드해 별도 저장소 **MinsangKwak/pogo-rank-dev**의 `gh-pages`로 밀어 넣는다(Pages는 저장소당 하나라 URL `…/pogo-rank-dev/`는 별도 저장소가 필요). 쓰기용 deploy key는 `scripts/setup_dev_deploy_key.sh`가 생성·등록·시크릿(`DEV_DEPLOY_KEY`) 저장까지 한 번에 처리
- `build.py`에 `BUILD_CHANNEL` 도입 — `dev`면 화면 버전 배지에 `-dev`, GA 스니펫 미삽입, `robots.txt` 전체 차단 + `<meta name="robots" content="noindex">`

</details>

<details>
<summary><b>v2.7.2</b> — 2026-09-05 · 추가</summary>

### 추가
- 아머드 뮤츠 전용 그림 — v2.7.0에서는 일반 뮤츠 png를 복사해 써서 화면에서 둘이 구분되지 않았다. 포켓몬 GO 게임 에셋(PokeMiners/pogo_assets)의 `pm150.fA.icon.png`를 `sprite.py`의 `LOCAL_SPRITE_URL`에 등록해 `sprites.py`가 내려받는다. 다운로드가 실패하면 예전처럼 원본 복사로 폴백. CI `actions/cache`가 되살린 예전 복사본은 "원본과 바이트가 같으면 다시 받는다"로 걸러 낸다
- 90000번대 스프라이트는 픽셀 아트가 아니라 3D 렌더 아이콘이라 `sprite.js`가 `sprite-hd` 클래스를 붙이고 `image-rendering: auto`로 부드럽게 축소 (기존 픽셀 스프라이트는 `pixelated` 유지)

</details>

<details>
<summary><b>v2.7.1</b> — 2026-09-05 · 추가</summary>

### 추가
- 로컬 테스트용 로그인 목(mock) 모드 — `frontend/static/dev-mock.js` 신설. `localhost`에서 `?mock=1`(관리자) · `?mock=friend`(승인된 친구) · `?mock=pending`(승인 대기)으로 열면 Google 팝업 없이 로그인된 상태로 즐겨찾기 페이지·역할 보정·관리자 패널·트레이너 코드를 확인할 수 있다. 가짜 Firestore는 `localStorage`(`pogo_mock_db`)에 남고 `?mock=reset`으로 초기화. `auth.js`의 `initAuth()`가 hostname·쿼리 조건을 모두 만족할 때만 이 파일을 받으므로 배포 환경에는 영향이 없다

</details>

<details>
<summary><b>v2.7.0</b> — 2026-09-05 · 추가 · 수정</summary>

### 추가
- ★ 즐겨찾기 전용 페이지 (`#/favs`) — `[전체 | PvE | PvP | 기타]` 세그먼트와 근거 순위 표시. 별은 계속 "보유" 하나이고, 갈래는 순위표에서 자동으로 정한다. 기존 `favs`(도감번호 배열) 구조·데이터를 그대로 두므로 마이그레이션이 없다
- `backend/roles_build.py` · `data/roles.json` 신설 — PvE/PvP 자동 분류 근거. `pve_full.json`의 19개 표 상위 60위와 `pvp_all.json`의 4리그 상위 100위에서 `도감번호|폼라벨` 키별 최고 순위를 뽑는다 (PvE 286폼/178종 · PvP 328폼/225종 · 29KB). 기존 `VALUE_DATA.usage`는 상위 30 기준 80종뿐이라 즐겨찾기 대부분이 '기타'로 떨어졌다
- 역할 수동 보정 — 상세 팝업 `★ 즐겨찾기 분류`. 자동 분류와 다를 때만 `users/{uid}.roles`에 예외로 저장하고 `[자동으로 되돌리기]`로 지운다. `users/{uid}`는 본인+승인자 통짜 허용이라 보안 규칙 변경 불필요
- **아머드 뮤츠 지원** — 그동안 데이터가 있는데도 화면 어디에도 나오지 않았다

### 수정
- 아머드 뮤츠 누락 교정 — 게임마스터 폼 표기가 `MEWTWO_ARMORED`가 아니라 `MEWTWO_A`라 `label_from_gm()`이 `None`을 돌려줬고, `pve_build.py`가 "라벨 표에 없는 폼(코스튬 등)"으로 보고 후보에서 제외해 PvE 계산·도감·보스 목록에서 통째로 빠져 있었다. 접미사 `_A`는 안농(`UNOWN_A`)과 겹치므로 `FORM_KO`가 아니라 종까지 함께 보는 `GM_FORM_EXCEPTIONS`에서 처리
- 아머드 뮤츠 스프라이트 id 분리 — PokeAPI `pokemon.csv`에 `mewtwo-armored` 행이 없어 `sprite_id()`가 일반 뮤츠(150)를 돌려줬다. 그대로 두면 도감 폼 표·기술 변경 영향 표·맥스 배틀 표에서 일반 뮤츠와 같은 칸을 써 서로를 덮어쓴다. `sprite.py`에 저장소 배정 번호(`LOCAL_FORMS`, 90000번대)를 두고 그림은 기본 폼 png를 복사해 쓴다
- `pvp_all.json`이 만들어지기만 하고 아무 데도 쓰이지 않던 문제 — `roles_build.py`의 PvP 근거로 쓰이면서 용도를 얻었다. 행에 `dex`·`form`을 추가
- 순위 변동 비교 키를 스프라이트 id → `스프라이트|이름`으로 교정 — 섀도우와 일반은 같은 그림(같은 id)을 써서, 이전 방식이면 "섀도우 뮤츠"의 변동이 "뮤츠"에 붙을 수 있었다
- 기술 변경 안내에 적용 범위 명시 — 시즌 공지의 위력 수치는 트레이너 배틀(PvP) 값이다. 게임마스터는 레이드·체육관용 위력(`moveSettings.power`)과 배틀용 위력(`combatMove.power`)을 따로 갖고 있고 이번 조정의 '이전 값'은 전부 후자와 일치한다(아이언헤드 공지 70 → 레이드용 60·배틀용 70). 레이드 티어표까지 바뀌는 것은 사이코부스트(70 → 130)와 새로 배우는 기술뿐

</details>

<details>
<summary><b>v2.6.1</b> — 2026-09-05 · 변경</summary>

### 변경
- ⭐ 내 즐겨찾기 카드를 "순위 변동 있을 때만"에서 "로그인 + 즐겨찾기가 하나라도 있으면 항상 전체 목록"으로 변경 — v2.6.0 방식은 평소엔 카드가 아예 안 떠서, 드로어의 "★ 즐겨찾기 N마리" 문구를 봐도 그 목록을 확인할 방법이 없었다. 이제는 항상 전체 목록을 보여주고(12개 넘으면 더보기), 그중 최근(14일 이내) 순위가 움직인 종에만 ▲▼ 뱃지를 덧붙인다. 이름·스프라이트는 종(도감번호) 자체가 곧 기본 폼 스프라이트 id라 순위표 랭킹 여부와 무관하게 항상 뜬다

</details>

<details>
<summary><b>v2.6.0</b> — 2026-09-05 · 추가 · 변경 (코드 품질) · 수정</summary>

### 추가
- 🔒 개인정보처리방침 페이지 (`#/privacy`) — 로그인 시 수집하는 개인정보(이메일·이름·프로필사진·즐겨찾기·uid), 목적, Firebase/Firestore 저장 위치, 승인 해제 시 데이터 보관 정책, GA4 처리위탁을 명시. 푸터·☰메뉴·로그인 전 계정 영역에서 진입, 노션에 같은 내용의 상세 페이지(표·콜아웃 포함)를 만들어 하단에 링크
- ⭐ 내 즐겨찾기 순위 변동 카드 (로그인 전용) — 즐겨찾기(도감번호 단위)와 순위 변동 ▲▼(스프라이트 단위) 두 기존 시스템을 새 데이터 없이 엮어, 찜한 종이 최근(14일 이내) 순위표에서 움직였으면 헤더 아래에 카드로 보여준다. 움직인 게 없으면 카드 자체를 그리지 않는다. 같은 종의 폼이 여러 표에 걸리면 가장 크게 움직인 한 줄만 남긴다
- ⚔️ 기술 변경 안내 페이지 (`#/changes`) — 시즌 기술 조정과 새로 배우는 기술을 위력 수치·비고까지 정리. 목록은 `backend/config/move_changes.txt`(공식 공지에서 옮겨 적는 유일한 수동 파일)에서 오고, 한글 기술명·포켓몬명·스프라이트·"그 기술을 쓰는 종"은 `backend/change_build.py`가 게임마스터에서 자동으로 채운다
- 기술 변경 예고 뱃지 — 영향받는 포켓몬 줄에 `9/8 기술↑`. 적용일을 지나면 코드 수정 없이 사라진다. 예측("상향 예정")이 아니라 공지된 사실(어떤 기술이 얼마로 바뀌는지)만 표시
- 순위 변동 ▲▼ 뱃지 — `backend/rank_diff.py`가 직전 빌드 순위(`snapshot/ranks.json`)와 비교해 각 행에 변동 폭을 심는다. PvP 4리그·PvE 자체계산·PvE 일반·시트 성능표·D-MAX 티어·활용처 79개 표가 대상. 2계단 미만은 일상적인 흔들림이라 무시하고, 순위가 실제로 달라진 빌드에서만 스냅샷을 갱신하며, 기록 후 14일이 지나면 표시하지 않는다
- 상세 팝업에 그 포켓몬에 걸린 기술 변경 섹션 (레거시 전용 기술은 따로 표기)
- 상세 팝업 🎯 포획 CP 표 — 잡은 개체가 100%인지 바로 대조하도록 포획 경로별 CP를 정리. 레이드 보상(평시 Lv20 · 날씨부스트 Lv25), 맥스 배틀(Lv20 고정 · 날씨부스트 없음), 야생 스폰(Lv30 · Lv35), 만렙(Lv50). 개체값 하한이 있는 레이드·맥스 배틀은 100% CP와 최저 CP(10/10/10)를 함께 표시
- `data/max_pool.json` 신설 — 맥스 배틀 포획 가능 종 168건(스프라이트 id → `G` 거다이맥스 · `D` 다이맥스). 기존 `dynamax.json`은 상위 30만 담아 포획 안내에 쓸 수 없어 전체 목록을 따로 산출. 전역 `MAX_POOL`로 주입
- `.vscode/` 신설 — Codespaces 세션마다 편집기 설정을 다시 잡지 않도록 저장소에 커밋. `settings.json`(기본 2칸·파이썬 4칸 들여쓰기, 자바스크립트·파이썬·마크다운은 저장 시 자동 서식 해제, `data`·`dist`·`snapshot` 검색 제외, `*.rules` 문법 강조), `tasks.json`(Ctrl+Shift+B 빌드 5종 + 미리보기 서버), `extensions.json`(권장·비권장 확장)
- `.claude/settings.json` 신설 — 빌드·검사 명령의 공용 허용 목록. 개인 설정(`settings.local.json`)은 계속 커밋 제외
- `robots.txt` 신설 — 일반 검색 엔진 허용(Crawl-delay 10) · AI 학습 크롤러(GPTBot·ClaudeBot·Google-Extended·CCBot·Bytespider) 차단 · 공격적 SEO 크롤러 차단. 빌드가 `dist/robots.txt`로 배포
- `docs/INFRA.md` 신설 — 트래픽·남용 대응 문서. 정적 호스팅에서 쓰로틀링·IP 차단이 불가능한 이유, 지금 규모의 수치(첫 방문 1.4MB → 월 100GB로 약 7만 회분), 운영자가 할 조치와 사이트에 적용한 조치, 트리거 기반 로드맵(도메인 → Cloudflare → App Check), 하지 않기로 한 것과 이유

### 변경 (코드 품질)
- 프론트엔드 자바스크립트 25개 파일 가독성 리팩토링 — 축약어·한 글자 지역 변수를 전부 원문 단어로(`e`→`event`/`error`, `p`→`pokemon`, `st`→`stats`, `ta`→`textarea`, `FV`→`fieldValue` 등), 파일마다 역할·제공 전역·의존 전역 헤더 주석, 계산 근거(부활 사이클 모델·보정 상수·상성 계수·추천 덱 3종 알고리즘 차이)와 설계 이유(접근 제어 주체는 보안 규칙, SDK 지연 로드, PWA 리다이렉트 분기)를 주석으로 명시 (2,062줄 → 3,227줄). 번들 전체에 `'use strict'` 적용
- 리팩토링 검증: 렌더 결과 22개 항목(탭 5종·서브탭·티어 근거 펼침·상세 팝업 2종·도감·일정표·패치노트·솔플 계산·추천 덱·커스텀 덱·전역 검색·드로어)을 리팩토링 전후로 비교해 **전부 문자 단위 동일**, 콘솔 에러 0. 전역 이름·state 속성·DOM class/id·localStorage 키·GA 이벤트명·Firestore 필드명·사용자 문구는 하나도 바뀌지 않음
- 백엔드 파이썬 9개 파일 가독성 리팩토링 — 축약어·한 글자 변수를 전부 원문 단어로(`g`→`game_master`, `fm`/`cms`→`fast_move`/`charged_moves`, `ps`→`pokemon_settings` 등), 파일마다 역할·입출력·파이프라인 위치 주석, 계산 상수의 출처와 수식의 의미, 게임 데이터 예외 처리의 이유를 주석으로 명시 (996줄 → 1,624줄). 세미콜론 한 줄 압축 해소
- 리팩토링 검증: 파이프라인 산출물 12종(pve·pvp·dynamax·value·sheet·dex·bosses·data.js·index.html)이 **바이트 단위로 동일**함을 md5로 확인. JSON 키·설정 상수·HTML 자리표시자·모듈 공개 이름은 하나도 바뀌지 않음

### 수정
- 네크로즈마 폼 한글 라벨 교정 — `dawnwings`↔`duskmane`이 서로 바뀌어 있어 "황혼의갈기/새벽의날개"가 뒤바뀌어 표시되던 문제 (PokeAPI 원본 대조: 10155=necrozma-dusk=황혼의갈기, 10156=necrozma-dawn=새벽의날개). `names.py`와 `sprite.py`를 함께 교정해 이름·스프라이트가 모두 일치

</details>

<details>
<summary><b>v2.4.0</b> — 2026-09-04 · 추가 · 변경 · 변경 (문서)</summary>

### 추가
- 상세 팝업 CP를 상황별로 분리 표시 — 레이드 보상(평시 Lv20 · 날씨부스트 Lv25), 야생 스폰(평시 Lv30 · 날씨부스트 Lv35). 기존엔 레이드 보상 CP가 아예 없어 야생 부스트(Lv35) 값을 레이드로 착각하기 쉬웠음
- 메가/원시 진화 가능 종은 "진화 단계"에 메가 폼을 이어서 표시, 누르면 그 폼의 스탯으로 바로 이동 (랭킹에 없는 메가도 항상 노출)
- 메가X/메가Y가 둘 다 있는 종(뮤츠·리자몽 등)은 "⚡ 메가X vs 메가Y 비교" 좌우 비교표 — 타입·종족값·CP 만렙 차이를 자동 요약

### 변경
- 푸터·README 고지 문구 개편 — "포켓몬고 응애 친구들을 위해 만들어진 서비스입니다" + 저작권 + 문의 세 줄 구성, "상업적 목적이 없습니다" 문구 삭제

### 변경 (문서)
- 산재된 md 문서를 **두 축으로 재편** — 📘 `docs/DEVELOPMENT.md`(설계 원칙·의사결정 기록·산식·구조) / 📗 `docs/OPERATIONS.md`(릴리스·배포 대응·로그인 운영·통계·노션 운영). README는 관문 역할로 축소(263줄 → 71줄)
- `docs/AUTH_SETUP.md` 삭제 — 내용을 운영 문서로 흡수
- 개발 문서에 "어떤 고민을 했고 어떻게 바꿨나" 의사결정 기록 10건 신설 (React 미채택, pogomate 티어 보정, 일정표 한국화, 솔플 실측 보정, 출시 판정, 이름 병합 축, 로그인 설계, 개인정보 제거, GA 계측 설계, CI 실패 대응)

</details>

<details>
<summary><b>v2.3.0</b> — 2026-09-03 · 추가 · 변경</summary>

### 추가
- 첫 화면 로딩 가림막 — 데이터(data.js)를 읽는 동안 흰 화면 대신 "POGO SEARCH · 불러오는 중" 표시, 첫 렌더가 끝나면 사라짐 (다크 모드 대응)

### 변경
- 트레이너 코드: 비로그인·승인 대기 상태에서는 메뉴에서 **항목 자체를 숨김** (이전에는 안내문이 보였음)
- 관리자 판정을 이메일 → **uid**로 교체 (backend/build.py `ADMIN_UID`, firestore.rules `isAdmin()`) — 공개 저장소에서 관리자 지메일 제거

</details>

<details>
<summary><b>v2.2.0</b> — 2026-09-03 · 추가 · 제거</summary>

### 추가
- 🔐 Google 로그인 (Firebase Authentication) — 헤더 👤 버튼 / ☰ 메뉴 상단. 홈 화면 설치(PWA) 환경은 리다이렉트 방식 자동 전환
- 관리자 승인제 — 처음 로그인한 사람은 "승인 대기", 관리자가 ☰ → 🔑 가입 승인에서 승인/해제. 관리자는 승인 없이 사용 가능
- 즐겨찾기 ★ (계정 저장) — 승인된 사용자가 도감 행·상세 팝업의 ★를 누르면 Firestore `users/{uid}.favs`에 저장, 어느 기기에서든 같은 도감이 채워짐. 도감에 "★ 즐겨찾기 N" 필터 칩
- Firestore 보안 규칙(firestore.rules) + 콘솔 설정 가이드(docs/AUTH_SETUP.md). `FIREBASE_CONFIG` 비어 있으면 로그인 UI 미표시, SDK는 첫 렌더 뒤 지연 로드로 초기 속도 유지

### 제거
- 기존 보유 체크 ☆(localStorage, 행 오른쪽 별) 및 "★ 보유만" 필터 전부 제거 — 계정 즐겨찾기로 대체
- 푸터 헌정 문구("To. 친구들") 삭제 — 공개 사이트에 친구 실명이 노출되지 않도록
- 코드 관리 저장 결과 표시 — 성공/형식오류/실패 사유(permission-denied 등)를 버튼 위에 바로 노출, 응답 없으면 10초 후 중단
- 트레이너 코드 10개를 소스에서 제거 — Firestore `trainers` 컬렉션으로 이동, 승인된 로그인 사용자만 조회(관리자만 등록·삭제). ⚠️ 기존 커밋 히스토리에는 남아 있음

</details>

<details>
<summary><b>v2.1.0</b> — 2026-09-03 · 추가 · 제거 · 변경</summary>

### 추가
- IF 탭 개편 — 실험 기능 모음으로: 서브탭 [솔플 레이드 계산기(기존) | PvP 덱 짜기(신규, QA-16 발전형)]. PvP 덱 짜기: 상대 1~3마리 추가 → 리그 순위 × 타입 상성(공격÷피격 배율) 근사로 추천 덱 3 + 상대별 카운터 3. 실드·기술 사이클 미반영 명시
- PvP 덱 짜기 UI: [+][+][+] 슬롯 3칸 — 빈 슬롯 탭 → 검색 포커스, 채우면 스프라이트+이름 카드(탭으로 제거), 3칸 차면 검색창 숨김
- PvP 덱 짜기 화면 순서: 실험 기능 표기 → 추천 덱 3(아코디언, 접기 가능) → 슬롯 → 분석 → 카운터
- PvP 덱 짜기 분석 카드: 슬롯 3칸을 다 채우면 상대 덱 타입 요약, 공격 기술 추천(약점 빈도순), 받이 타입(자속 반감), 추천 덱 역할 분담·구멍 경고까지 자동 분석
- PvP 덱 짜기 카운터 이유 줄: "고스트·페어리 타입은 강철이 약점 → 강철 자속 ×1.6 · 받는 공격 ×1"처럼 왜 카운터인지 설명
- GO배틀리그 규칙 반영: 같은 종은 파티에 1마리만(섀도우·일반도 같은 종, Niantic 공식) — 추천 덱·카운터를 종 단위로 중복 제거 (엠페르트+섀도우 엠페르트 동시 추천 방지)
- PvP 추천 덱 3종: 상대 입력 없이 리그 메타 기준으로 각 3마리 조합 — ① 정석 코어(점수 + 약점 상호 보완 그리디) ② 안티 메타(상위 10마리 상대 평균 상성순) ③ 타입 분산(방어 타입 안 겹침), 아코디언마다 "왜 이 조합인지" 이유 문장(약점 커버 관계·겹치는 약점 경고) 자동 생성
- 기존 상대 슬롯 기반 기능은 "PvP 커스텀 덱 짜기" 섹션으로 분리 (추천 덱 아래 배치)
- PvP 덱 짜기 목록에서 보유 체크 ☆ 숨김 — 덱 추천 화면에선 불필요 (보유 체크는 PvP 탭 유지)
- 푸터에 문의 이메일 추가 (mailto 링크)
- GA4 사용 통계 연결 (G-XXXXXXXXXX) — 일일 활성 사용자 + 기능별 이벤트, github.io 배포 도메인에서만 로드
- 탭 재배치 판단용 계측: 탭 클릭마다 tab_max/tab_pve/tab_pvp/tab_usage/tab_if (이벤트명에 탭 포함 → GA 이벤트 목록에서 설정 없이 순위 확인), 접속 시 첫 탭은 tab_start로 분리, 서브탭은 sub_pve_*/sub_if_*, 그 외 page_open·detail_open·solo_calc_boss·pvp_deck_foe

### 제거
- 활용처 탭 '★ 보유만' 필터 제외 (PvP 탭에는 유지) — 현 단계 단순화

### 변경
- 헤더 단순화: "POGO SEARCH v2.1.0 / 편하게 검색하세요" — 기존 서문·To. 줄 제거 (헌정은 푸터 유지)
- 브라우저 탭 제목·홈화면 앱 이름도 POGO SEARCH로 통일 (GA 페이지 이름에 반영)

</details>

<details>
<summary><b>v2.0.1</b> — 2026-09-03 · 추가 · 수정 (D-MAX 티어표 pogomate 대조 보정)</summary>

### 추가
- 도감 레이아웃 토글: 1열 목록 ↔ 2열 격자 (⊞/☰ 버튼, 선택 기억)

### 수정 (D-MAX 티어표 pogomate 대조 보정)
- 타입 탭 분류를 "그 타입 보유"에서 "그 타입 맥스무브 딜러"로 변경 — 할비롱(드래곤 무브)이 노말 탭에 오르던 문제, 이제 드래곤 탭으로
- 이중 자속(연격 우라오스 물·격투 등)은 자속 타입마다 행 생성 — 물·격투 양쪽 탭에 표시, 전체 탭은 중복 제거
- 보정 후 노말·물·불꽃·풀·격투 상위권이 pogomate와 ±2%p 안에서 일치
- 도감 [미구현] 태그: 포켓몬 GO 미출시 종에 태그 표시 + 행 흐림 — PvPoke released 기준에 수동 보정 파일(backend/config/dex_released_extra.txt, 메타몽 등 15종) 결합
- 개발 문서 docs/DEVELOPMENT.md: 데이터 소스(API)·수집·가공·병합 파이프라인·프론트 구조·캐싱 상세 기록

</details>

<details>
<summary><b>v2.0.0</b> — 2026-09-03 · 변경 · 추가 · 변경 (배포 속도)</summary>

성능 개편 — "의존성 없는 단일 HTML" 구조를 종료하고 리소스를 분리했습니다 (breaking: 산출물이 여러 파일).

### 변경
- 산출물 분리: index.html 2.99MB → 112KB + data.js(1.2MB, 매일 갱신) + sprites/ 개별 png(lazy 로딩, 보이는 것만 다운로드). 초기 전송 3.0MB → 1.2MB, 모바일급 CPU 기준 첫 목록 1.47s → 1.05s
- 서비스워커 v2: 페이지·data.js는 네트워크 우선(실패 시 캐시), 스프라이트는 불변이라 캐시 우선 — 재방문·오프라인 모두 커버
- 채팅/로컬 미리보기용 `SPRITE_INLINE=1 python3 backend/build.py`로 옛 단일 HTML 빌드 유지

### 추가
- 헤더 타이틀 옆 버전 표기 (backend/build.py APP_VERSION에서 주입 — 릴리스 시 한 곳만 수정)

### 변경 (배포 속도)
- GitHub Actions에 스프라이트 캐시(actions/cache) — 매 배포 1,100여 개 재다운로드 제거, 신규분만 받음
- 원본 데이터 다운로드 병렬화(fetch_data.sh), 스프라이트 다운로드 16개 병렬(sprites.py)

</details>

<details>
<summary><b>v1.5.1</b> — 2026-09-03 · 변경</summary>

### 변경
- 상세 팝업을 여는 곳에 따라 구성 분리: 일반(순위표·검색)은 능력치 없이 "배울 수 있는 기술"을 전체 폭으로, 도감에서 열면 [능력치 육각형 | 기술] 표시
- 기술 섹션 간격 정리 — 스피드/차지 라벨 바로 아래 칩 (세로 배치에서 라벨 flex-basis가 높이로 적용되던 문제 수정)

</details>

<details>
<summary><b>v1.5.0</b> — 2026-09-03 · 추가 · 변경</summary>

### 추가
- 📕 도감 페이지 (메뉴 → 도감, #/dex): 넘버링순 전 종 목록(스프라이트·이름·타입 색점), 이름/번호 검색 + 1~9세대 점프, 100개씩 더보기, 행 클릭 시 상세 팝업
- 도감용 데이터 확장: 전 종 기본 폼 종족값·기술(forms 617→1,121종), 전 종 스프라이트 내장(926→1,139개)

### 변경
- 메뉴의 CP 계산기 페이지 제거 (상세 팝업 내 계산기로 대체)
- 상세 팝업 도감형 재배치: 헤더(타입→이름(영문)→CP 만렙|야생·부스트) → 🧮 내 개체 CP 계산기(아코디언, 레벨·IV 슬라이더) → [능력치 육각형 | 배울 수 있는 기술] 2열 → 타입 상성(약점/내성 세로 평면 칩) → 활용처 → 진화 → "보스로 나오면"(맨 아래)
- 도감 상단 간격 보정

</details>

<details>
<summary><b>v1.4.1</b> — 2026-09-03 · 수정 · 변경</summary>

### 수정
- 모바일(640px 미만)에서 목록의 포켓몬 이름이 세로로 흘러내리는 레이아웃 버그 — ☆ 보유 버튼이 이름 칸을 선점하던 문제(.star grid-column: 4)
- 입력 포커스 시 화면 확대 방지 — viewport user-scalable=no + 입력 폰트 16px

### 변경
- 검색 통합: 전역 검색에 빌드 보스 목록(메가·섀도우 1,586종) 포함, 공용 필터 monSearch(공백 무시·영문·정확도순)를 전역 검색·IF 보스 검색·내 덱 검색이 공유
- 상세 팝업 정보 우선순위 개편 (능력치 육각형·CP 헤더·내 개체 CP 계산기 도입 과정 포함)

</details>

<details>
<summary><b>v1.4.0</b> — 2026-09-03 · 추가 · 변경</summary>

### 추가
- 오른쪽 드로어 메뉴(☰): 일정표·기준 안내·트레이너 코드·패치노트·QA 제보 정리, 헤더에 🔍 전역 검색
- 해시 라우팅 전체 페이지: #/release 패치노트(자동 팝업 → ☰ 빨간 점 뱃지), #/schedule 일정표, 뒤로가기·링크 공유 지원
- 전역 포켓몬 검색, 상세 팝업 최대 CP(만렙/야생/부스트)·진화형 이동
- 보유 체크(☆, localStorage, 보유만 필터), 트레이너 코드 원터치 복사, ↑ 맨 위로 버튼
- PWA: manifest·아이콘·서비스워커(네트워크 우선) — 홈 화면 설치·오프라인 열람

### 변경
- 서문 교체: "뭘 키우고, 뭘 잡을지. 여기서 끝."

</details>

<details>
<summary><b>v1.3.1</b> — 2026-09-02 · 변경</summary>

### 변경
- 솔플 계산기 고도화: 보스 이름 검색형(전 종 1,586 bosses.json), 티어 자동 판정(전설=4성, 배지 탭 수동 보정), 개체별 레이드 CP·최대 CP 표시
- 운용 모델을 실측(실측 제공자) 방식으로: 기절 직전 이탈 → 부활(5~6초) → 같은 덱 재진입, 정예 1~2마리. 실측 보정 계수(생존 3배 · DPS +20%), 보스 실제 방어·공격 반영, 풀강·버프 토글
- 내 덱 검증 모드, 판정에 딜 총량 결론("최대 딜 13,922 / 체력 15,000 → 1,078 모자라서 못 잡음")

</details>

<details>
<summary><b>v1.3.0</b> — 2026-09-02 · 추가</summary>

### 추가
- IF 탭 신설 — 솔플 레이드 계산기. 부활 운용 사이클 시뮬레이션으로 필요 개체·순서·예상 시간 제시, 불가능하면 "사람 손으로는 무리" 배너

</details>

<details>
<summary><b>v1.2.0</b> — 2026-09-02 · 변경 · 추가</summary>

### 변경
- D-MAX 티어표 기준을 pogomate와 동일하게: 공격 종족값 × 맥스무브 위력(거다이 450·다이 350) × 자속 1.2, 내구 미반영, 다이/거다이 별도 행, 1위 대비 % 표시 (수치 역산 검증)

### 추가
- 티어표 행 클릭 → 선정 근거 인라인 + "얘가 보스면 →" 카운터 5종, 상세 팝업 "보스로 나오면?" 섹션
- 탭 위 "⚔️ 이번 주 보스" 아코디언 — 일정 기준 자동 갱신, 추천 딜러 딜량순 5개씩 더보기

</details>

<details>
<summary><b>v1.1.1</b> — 2026-09-02 · 수정</summary>

### 수정
- 일정을 한국 기준으로 보정: LeekDuck 원본 JSON 전환, 메가 피날레 특별 기간(데일리 디스커버리 휴식) 반영, 피카츄의 한국 나들이·달맞이댄스·가을 소풍 등 한국 전용 이벤트 추가, 주말 섀도우 레이드·스포트라이트 요일(목) 검증

</details>

<details>
<summary><b>v1.1.0</b> — 2026-09-02 · 추가 · 변경</summary>

### 추가
- 9월 일정표: 상단 아코디언 + 달력 그리드, 날짜 클릭 상세, 접힘 상태 오늘 일정 한 줄
- 업데이트 소식 팝업(30일 안 보기), 푸터 QA·버그 제보(노션) 링크

### 변경
- 서비스명 POGO NOTE (울애들을 위한 포켓몬 노트)로 리브랜딩
- PvE 일반·전체를 한 탭으로 통합, 다이맥스 표기를 D-MAX로, 기준 안내를 화면 하단 아코디언으로

</details>

<details>
<summary><b>v1.0.0</b> — 2026-09-01 · 수정 · 변경 · 데이터</summary>

첫 정식판. 랭킹 도감(다이맥스 · PvE · PvP · 활용처), 포켓몬 상세 팝업(진화·기술·상성·종족값·활용처), 매일 00시 자동 빌드·배포.

---

아래는 버전 체계 도입(v1.0.0 = 2026-09-01) 이전의 날짜별 기록 원본입니다.

## 2026-09-01

### 수정
- 속성 칩을 선택하면 목록 재렌더링으로 칩 가로 스크롤이 처음으로 리셋되어 선택한 칩이 화면 밖으로 밀려나던 문제 — 선택 칩이 가운데 오도록 스크롤 복원
- 제라오라·나무킹 등 시트 순위에만 등장하는 약 90종의 스프라이트가 안 나오던 문제 — 스프라이트 수집이 sheet.json을 빼놓고 있었음 (301 → 389종). 이미지가 아예 없는 항목은 몬스터볼 자리표시로 표시

### 변경
- 헤더의 데이터 출처·기준일을 푸터로 이동해 안내 정보를 한곳에 정리, "To. 친구들" 헌정 문구 추가
- 시트의 이벤트 메모("2026년 9월 5일 메가피날레")를 "메가피날레(2026한정)" 형식의 뱃지로 표시, 모든 뱃지(G-MAX·다이맥스·이벤트)를 이름 위 전용 줄로 정렬
- 탭 순서를 PvE → PvP → 맥스 → 가성비로 변경, 첫 화면도 PvE로
- `미출시` 태그 표시 제거 (PvPoke 출시 반영이 늦어 실제 출시된 포켓몬에도 붙던 문제)
- 이름·기술의 말줄임(…) 제거 — 전부 표시하고 줄바꿈. 640px 미만에서는 점수·상세를 이름 아래로 내린 세로형 레이아웃
- 저장소 구조 개편: 화면을 `frontend/`(styles·scripts·views 분리), 파이썬 파이프라인을 `backend/`, 셸 스크립트를 `scripts/`, 수동 설정을 `backend/config/`로 분리. 배포 형태(단일 `dist/index.html`)는 동일
- 실행 스크립트가 어느 위치에서 실행해도 저장소 루트 기준으로 동작하도록 수정

## 2026-08-25

### 데이터
- 다이맥스 138종 · 거다이맥스 17종 출시 목록 갱신 (Bulbapedia 기준)

## 2026-08 이전

- PvP · PvE · 맥스 · 가성비 4개 탭 및 GitHub Pages 매일 자동 배포 구축 (자세한 이력은 git log 참고)

</details>

